---
phase: 02-spaces-rooms
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/ffi/gcs_core_ffi.cpp
  - test/test_gcs_ffi_sdk.cpp
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03]

must_haves:
  truths:
    - "Publishing create_space / create_room / update_space commands mutates the entity store and pushes an updated SpaceTree"
    - "Rooms whose parent space has autoJoinRooms appear in the pushed RoomList (derived joins), and leave it when the toggle turns off (D-04: derived join state, pure local recomputation)"
    - "A pushed SpaceTree survives a session restart (FFI-level persistence)"
  artifacts:
    - path: "src/ffi/gcs_core_ffi.cpp"
      provides: "g_entities global, 3 new command arms, BuildSpaceTreeEvent, RefreshDerivedJoins, subscribe-time SpaceTree push, shutdown reset"
      contains: "kCreateSpace"
    - path: "test/test_gcs_ffi_sdk.cpp"
      provides: "FFI two-session entity persistence + SpaceTree push-capture test"
  key_links:
    - from: "src/ffi/gcs_core_ffi.cpp gcs_publish switch"
      to: "gcs::EntityStore Create*/Update*"
      via: "g_entities (unique_ptr, guarded by g_mutex)"
      pattern: "g_entities->Create"
    - from: "src/ffi/gcs_core_ffi.cpp gcs_subscribe"
      to: "BuildSpaceTreeEvent"
      via: "PostToDart before RoomList before Readiness"
      pattern: "BuildSpaceTreeEvent"
---

<objective>
Wire the new `gcs::EntityStore` into the FFI dispatch: add the three create/update command arms, a `SpaceTree` event builder + push sites, and the derived-join refresh that feeds `RoomList`; then extend the FFI two-session test to prove entity persistence across restart at the ABI level.

Purpose: This is the only place Dart commands reach the catalog. It converts data-only Dart commands into C++-owned CRDT writes (D-01/D-04) and pushes the state Dart renders. Success criteria 1–4 all pass through this file. Note (Pitfall 3): entity writes are local-restart persistence only — `GcsGlobalDb::Put` hard-codes `kNoTopics`, so records never gossip cross-node this phase; do NOT add any cross-node sync expectation.

Output: `gcs_publish` accepts create_space/create_room/update_space, `gcs_subscribe` pushes SpaceTree, and `test_gcs_ffi_sdk` proves restart persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/phases/02-spaces-rooms/02-CONTEXT.md
@.planning/workstreams/app/phases/02-spaces-rooms/02-RESEARCH.md
@.planning/workstreams/app/phases/02-spaces-rooms/02-PATTERNS.md

<interfaces>
<!-- EntityStore contract created by Plan 02-01 — consume it directly, do not re-explore. -->

From src/lib/gcs_entity_store.hpp:
```cpp
namespace gcs {
class EntityStore {
public:
  explicit EntityStore(CoreSession& session);
  outcome::result<void> LoadFromStore();
  outcome::result<SpaceRecord> CreateSpace(const std::string& name, bool isPublic, bool autoJoinRooms);
  outcome::result<RoomRecord> CreateRoom(const std::string& name, const std::string& parentSpaceId);
  outcome::result<SpaceRecord> UpdateSpace(const SpaceRecord& desired);
  std::vector<SpaceRecord> Spaces() const;              // tombstoned skipped
  std::vector<RoomRecord> Rooms() const;                // tombstoned skipped
  std::vector<std::string> DerivedJoinedTopics() const; // "gcs/chat/<roomId>" for rooms in autoJoin spaces
  bool IsValidParentSpace(const std::string& id) const;
};
}
```

Existing FFI patterns to copy (src/ffi/gcs_core_ffi.cpp):
- Command-arm shape: switch on `command.payload_case()`, validate → `PostErrorNotice(raw)` + `GCS_ERROR_INVALID_ARGUMENT`, store failure → `GCS_ERROR_GENERIC`, success → `PostToDart(...)`.
- `BuildRoomListEvent()` (lines 112-121) — `BuildSpaceTreeEvent()` mirrors it, calling `g_entities->Spaces()`/`Rooms()` into `event.mutable_space_tree()->add_space(...)` / `add_room(...)`.
- `gcs_subscribe` push ordering (lines 390-400) — insert SpaceTree BEFORE the RoomList push.
- `gcs_init` (lines 210-248) — `g_session = std::move(session);` happens AFTER the smoke-topic block; construct `g_entities` from `*g_session` AFTER that move (never before — the helpers use the `g_session` global).
- `gcs_shutdown` (lines 403-414) — reset `g_entities` and clear `g_derivedTopics` alongside `g_roomTopics.clear()`.
- Payload-size narrowing guard: `payloadLength > std::numeric_limits<int>::max()` → INVALID_ARGUMENT (existing IN-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire EntityStore into FFI (globals, arms, SpaceTree push, derived joins)</name>
  <files>src/ffi/gcs_core_ffi.cpp</files>
  <read_first>src/ffi/gcs_core_ffi.cpp, src/lib/gcs_entity_store.hpp</read_first>
  <action>
    Edit src/ffi/gcs_core_ffi.cpp. Add `#include "lib/gcs_entity_store.hpp"` beside the existing `lib/gcs_core.hpp` include.

    Globals (anonymous namespace, beside g_session):
    - `std::unique_ptr<gcs::EntityStore> g_entities;` (guarded by g_mutex)
    - `std::vector<std::string> g_derivedTopics;` — the current derived-joined topics that are ALSO in g_roomTopics (so a false toggle can remove them from the projection while pubsub registrations stay sticky, Pitfall 4).

    New helpers (anonymous namespace; both assume g_mutex held and `g_session` non-null):
    - `gcs::chat::GcsEvent BuildSpaceTreeEvent()` — mirrors BuildRoomListEvent: `auto* tree = event.mutable_space_tree();` then for each `g_entities->Spaces()` call `tree->add_space()` with a copy of the record, and for each `g_entities->Rooms()` call `tree->add_room()` with a copy.
    - `void RefreshDerivedJoins()` — recompute the joined set's third source (D-04 / RESEARCH Pattern 4):
      1. `const auto derived = g_entities->DerivedJoinedTopics();`
      2. For each topic in `derived` not already in g_derivedTopics: call `g_session->AddListenTopic(topic)` then `g_session->AddBroadcastTopic(topic)` (listen-first ordering, D-07); on success push it into g_derivedTopics and (if absent) into g_roomTopics; on failure `spdlog::error(...)` and do NOT add it.
      3. Erase from g_derivedTopics AND g_roomTopics every topic that is in g_derivedTopics but no longer in `derived` (this is what makes toggle-false observable; the underlying pubsub registration stays — there is no Remove*Topic API). Keep smoke + explicit join topics untouched (they are never in g_derivedTopics).

    gcs_init: insert AFTER the existing `g_session = std::move( session );` line and BEFORE the final `return`:
    - `g_entities = std::make_unique<gcs::EntityStore>( *g_session );`
    - `auto loadResult = g_entities->LoadFromStore();` — if `loadResult.has_error()` then `spdlog::error(...)` and continue (LoadFromStore is designed to treat a missing manifest as an empty catalog; never fail init here).
    - `RefreshDerivedJoins();` — registers derived topics at startup (no pushes here; the port is not yet registered, so posts would be silent no-ops). NOTE: this ordering is required — RefreshDerivedJoins uses `g_session`, which is only valid after the move.

    gcs_publish switch — add three arms (copy the kJoinTopic arm's validate→mutate→push shape):
    - `case gcs::chat::GcsCommand::kCreateSpace:` — `const auto& cmd = command.create_space();` if `cmd.name().empty()` → `PostErrorNotice("create_space rejected: name is empty")` + `return GCS_ERROR_INVALID_ARGUMENT;` call `g_entities->CreateSpace(cmd.name(), cmd.is_public(), cmd.auto_join_rooms())`; on error `PostErrorNotice("create_space store write failed for '" + cmd.name() + "'")` + `return GCS_ERROR_GENERIC;` on success `PostToDart(BuildSpaceTreeEvent());` + `return GCS_OK;` (a new space has no rooms yet, so RoomList does not change).
    - `case gcs::chat::GcsCommand::kCreateRoom:` — empty name → reject ("create_room rejected: name is empty"); if `!cmd.parent_space_id().empty() && !g_entities->IsValidParentSpace(cmd.parent_space_id())` → `PostErrorNotice("create_room rejected: parent space '" + cmd.parent_space_id() + "' not found")` + INVALID_ARGUMENT; call `g_entities->CreateRoom(cmd.name(), cmd.parent_space_id())`; on error → "create_room store write failed for '<name>'" + GENERIC; on success `RefreshDerivedJoins()` (the new room may be auto-joined), then `PostToDart(BuildSpaceTreeEvent()); PostToDart(BuildRoomListEvent());` + GCS_OK.
    - `case gcs::chat::GcsCommand::kUpdateSpace:` — `const auto& cmd = command.update_space();` if `cmd.space_id().empty()` or `cmd.name().empty()` → reject with a specific message ("update_space rejected: ..."); build a `gcs::chat::SpaceRecord desired` with `set_id(cmd.space_id()), set_name(cmd.name()), set_is_public(cmd.is_public()), set_auto_join_rooms(cmd.auto_join_rooms())`; call `g_entities->UpdateSpace(desired)`; on error PostErrorNotice("update_space failed for '<space_id>'") + GCS_ERROR_GENERIC; on success `RefreshDerivedJoins()` (toggle may change the derived set), then `PostToDart(BuildSpaceTreeEvent()); PostToDart(BuildRoomListEvent());` + GCS_OK.

    gcs_subscribe: insert `PostToDart(BuildSpaceTreeEvent());` IMMEDIATELY BEFORE the existing `PostToDart(BuildRoomListEvent());` (tree before membership before readiness).

    gcs_shutdown: inside the existing teardown block, after `g_roomTopics.clear();` add `g_derivedTopics.clear();` and `g_entities.reset();`.
  </action>
  <acceptance_criteria>
    - `src/ffi/gcs_core_ffi.cpp` contains `g_entities` (a `std::unique_ptr<gcs::EntityStore>`), `g_derivedTopics`, `BuildSpaceTreeEvent`, and `RefreshDerivedJoins`
    - `grep -c "kCreateSpace\|kCreateRoom\|kUpdateSpace" src/ffi/gcs_core_ffi.cpp` equals 3 (three new switch arms)
    - `grep -n "BuildSpaceTreeEvent()" src/ffi/gcs_core_ffi.cpp` shows it called in gcs_subscribe before `BuildRoomListEvent()`, and in the create/update arms
    - `g_entities = std::make_unique<gcs::EntityStore>` appears AFTER `g_session = std::move` in gcs_init (never before)
    - `grep -n "g_entities.reset()" src/ffi/gcs_core_ffi.cpp` present in gcs_shutdown
    - `ninja -C build/OSX/Debug gcs_ffi` exits 0
  </acceptance_criteria>
  <verify>
    <automated>ninja -C build/OSX/Debug gcs_ffi</automated>
  </verify>
  <done>gcs_ffi links with EntityStore; create_space/create_room/update_space are dispatched, SpaceTree is pushed at subscribe + after mutations, and derived joins feed RoomList.</done>
</task>

<task type="auto">
  <name>Task 2: FFI two-session entity persistence test (extend test_gcs_ffi_sdk.cpp)</name>
  <files>test/test_gcs_ffi_sdk.cpp</files>
  <read_first>test/test_gcs_ffi_sdk.cpp, src/ffi/gcs_core_ffi.cpp</read_first>
  <action>
    Extend test/test_gcs_ffi_sdk.cpp with a new TEST_F in the `GcsFfiSdk` fixture (reuse `InstallFakeApiDlTable`, `GeniusSDKInit` + `GTEST_SKIP` on boot failure, `PublishCommand`, `PushedEventLog`/`g_pushedEvents.Take()`, `kFakeDartPort`).

    Add a helper that, given captured pushed `GcsEvent` bytes, extracts the latest `space_tree` event: iterate `g_pushedEvents.Take()`, `ParseFromString`, return the event when `event.has_space_tree()`.

    New test `CreateSpaceAndRoomPersistAcrossSessionCyclesOnSharedDb`:
    - InstallFakeApiDlTable(); GeniusSDKInit (skip if null); `dbPath = m_tempPath + "/db"`.
    - Cycle A: `gcs_init(dbPath)` -> `gcs_subscribe(handle, kEventTopic, kFakeDartPort)` -> publish a `create_space` GcsCommand (name "ops", is_public true, auto_join_rooms true) -> drain pushed events and read the latest SpaceTree to capture the minted space id (assert it starts with "space-") -> publish a `create_room` GcsCommand (name "general", parent_space_id = captured space id) -> drain again and assert the SpaceTree now carries one room named "general" whose parent_space_id equals the space id, and that a RoomList event pushed after the room contains "gcs/chat/" + roomId (derived join). `gcs_shutdown(handle)`.
    - Cycle B: `gcs_init(dbPath)` again -> `gcs_subscribe` -> drain pushed events and assert the SpaceTree push still carries the space "ops" and the room "general" (persistence across restart, criterion 4). `gcs_shutdown(handle)`.

    Construct the commands exactly like the existing join/send helpers: `gcs::chat::GcsCommand c; c.mutable_create_space()->set_name("ops"); c.mutable_create_space()->set_is_public(true); c.mutable_create_space()->set_auto_join_rooms(true);` (and `mutable_create_room()->set_name(...)/set_parent_space_id(...)`). Use `PublishCommand(handle, c)`.

    Do NOT use sleep_for — the existing `PublishCommand` is synchronous under g_mutex, so pushed events are available immediately after `Take()`; use `WaitForCondition` if any asynchronous visibility is needed.
  </action>
  <acceptance_criteria>
    - `test/test_gcs_ffi_sdk.cpp` contains `CreateSpaceAndRoomPersistAcrossSessionCyclesOnSharedDb`
    - `test/test_gcs_ffi_sdk.cpp` references `mutable_create_space()` and `mutable_create_room()`
    - `ninja -C build/OSX/Debug test_gcs_ffi_sdk && ctest --test-dir build/OSX/Debug -R test_gcs_ffi_sdk --output-on-failure` exits 0 (the new test green, or GTEST_SKIP only when the SDK cannot boot)
  </acceptance_criteria>
  <verify>
    <automated>ninja -C build/OSX/Debug test_gcs_ffi_sdk && ctest --test-dir build/OSX/Debug -R test_gcs_ffi_sdk --output-on-failure</automated>
  </verify>
  <done>FFI-level create + derived-join + restart persistence proven through the real ABI (fake Dart port capture).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart → gcs_publish ABI | Untrusted command bytes cross the C ABI here |
| gcs_core_ffi → Dart push port | Error strings cross back as raw text (D-29) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | gcs_publish GcsCommand parse | mitigate | ParseFromArray failure → PostErrorNotice + GCS_ERROR_INVALID_ARGUMENT before any state change (existing pattern; new arms inherit it) |
| T-02-05 | Tampering | create_room / update_space arg validation | mitigate | empty name, unknown parent_space_id, empty space_id all rejected with PostErrorNotice + GCS_ERROR_INVALID_ARGUMENT before any Put |
| T-02-06 | Tampering | payload length size_t→int narrowing | mitigate | `payloadLength > std::numeric_limits<int>::max()` → GCS_ERROR_INVALID_ARGUMENT (existing IN-01 guard) |
| T-02-07 | Information Disclosure | error strings crossing FFI | accept | D-29 locked decision: raw error strings on the push port by design (no PII in Phase 2) |
| T-02-SC | Tampering | package installs | accept | zero new packages this phase — slopcheck gate not applicable |
</threat_model>

<verification>
Full backend check:
`ninja -C build/OSX/Debug && ctest --test-dir build/OSX/Debug -R "gcs" --output-on-failure`
</verification>

<success_criteria>
- gcs_publish dispatches create_space/create_room/update_space to gcs::EntityStore under g_mutex, validating inputs first
- gcs_subscribe pushes SpaceTree before RoomList before Readiness; create/update push SpaceTree (+ RoomList when derived joins change)
- Derived-joined rooms enter g_roomTopics on autoJoin and leave it on toggle-false (pubsub registration sticky)
- test_gcs_ffi_sdk proves create + derived-join + restart persistence end to end
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/02-spaces-rooms/02-02-SUMMARY.md` when done
</output>
