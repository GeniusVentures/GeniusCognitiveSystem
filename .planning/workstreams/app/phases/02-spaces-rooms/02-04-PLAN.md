---
phase: 02-spaces-rooms
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - src/app/lib/shell/space_room_dialog.dart
  - src/app/test/shell/space_room_dialog_test.dart
  - src/app/scaffold/.planning/workstreams/scaffold/TICKET-text-entry-field-light-mode-contrast.md
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03]

must_haves:
  truths:
    - "The one reusable create/edit dialog publishes the correct GcsCommand per mode and closes on confirm (no local optimism)"
    - "An empty name shows inline validation and does not publish; a failed publish shows an error toast and stays open"
  artifacts:
    - path: "src/app/lib/shell/space_room_dialog.dart"
      provides: "showSpaceRoomDialog entry + SpaceRoomDialogMode + create/edit StatefulWidget (D-05)"
      contains: "showSpaceRoomDialog"
    - path: "src/app/test/shell/space_room_dialog_test.dart"
      provides: "dialog widget tests (create space/room, edit space, empty-name, publish-false toast)"
  key_links:
    - from: "space_room_dialog.dart"
      to: "GcsCommandTransport.publishCommand"
      via: "confirm handler"
      pattern: "publishCommand"
    - from: "space_room_dialog.dart"
      to: "ResponsiveDrawer.show / TextEntryFieldWidget / ScaffoldSelectionIndicatorRadio / ScaffoldSelectionIndicatorToggle / showToast"
      via: "scaffold atoms (D-05)"
      pattern: "ResponsiveDrawer.show"
---

<objective>
Create the single reusable create/edit dialog (D-05) composed entirely from verified scaffold atoms, add its widget tests, and file the scaffold-workstream contrast ticket for the TextEntryFieldWidget light-mode limitation.

Purpose: This is the creation/edit surface behind CORE-01/CORE-02/CORE-03 and the only place Dart builds create/update commands. It also defines the frozen entry-point contract Plan 02-05 (rail affordances) will call.

Output: `space_room_dialog.dart` with a frozen `showSpaceRoomDialog` API, a passing dialog widget test, and the filed contrast ticket.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/phases/02-spaces-rooms/02-UI-SPEC.md
@.planning/workstreams/app/phases/02-spaces-rooms/02-RESEARCH.md
@.planning/workstreams/app/phases/02-spaces-rooms/02-PATTERNS.md

<interfaces>
<!-- FROZEN entry-point contract — Plan 02-05 (room_rail.dart) consumes exactly this. Do NOT rename or re-sign. -->
```dart
// src/app/lib/shell/space_room_dialog.dart exports:
enum SpaceRoomDialogMode { createFromHeader, createRoomInSpace, editSpace }

Future<void> showSpaceRoomDialog({
  required BuildContext context,
  required GcsCommandTransport transport,
  required SpaceRoomDialogMode mode,
  RailSpace? space,   // editSpace: the space to edit (prefill id/name/isPublic/autoJoinRooms)
  RailSpace? parent,  // createRoomInSpace: the containing space (dialog title + parent_space_id)
})
```
`RailSpace` comes from `../cubits/rail_cubit.dart` (Plan 02-03); `GcsCommandTransport` from `../cubits/session_cubit.dart`.

Verified scaffold atom signatures (read from src/app/scaffold/lib/components/):
```dart
static Future<T?> ResponsiveDrawer.show<T>({required BuildContext context, required String title, required List<Widget> children, Widget? footer, VoidCallback? onClose})
TextEntryFieldWidget({required TextFormFieldLogic logic})   // logic owns its controller
ScaffoldSelectionIndicatorRadio({required bool value, ValueChanged<bool>? onChanged, bool disabled = false})  // bool-based, NOT group-value
ScaffoldSelectionIndicatorToggle({required bool value, ValueChanged<bool>? onChanged})
void showToast(BuildContext context, String message, {String? title, ToastType type = ToastType.success, Duration? duration, VoidCallback? onClose})
ScaffoldBadge({variant: BadgeVariant.text, text: ..., badgeColor: ...})
```
</interfaces>

<note>
Accepted deviation from UI-SPEC dialog behavior item 4 (pushed `ErrorNotice` → error toast with the rejection reason): pushed-`ErrorNotice` errors surface via the existing `SessionState.error` path, not a toast. Phase 1 routes `hasError` to `SessionState.error` only, and no `showToast` error surface exists. This is accepted for Phase 2 per the checker's reachability analysis — every C++ rejection (empty name, unknown parent/space_id) is pre-validated client-side and ids derive from the pushed `SpaceTree`, so they always resolve; the only reachable failure (transport down, `publishCommand == false`) IS toasts and is test-proven by Task 2 `PublishFalseShowsToast`. A toast surface for pushed errors is a Phase 3+ candidate if messaging-phase error paths make it reachable.
</note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build space_room_dialog.dart (create/edit dumb form) + file contrast ticket</name>
  <files>src/app/lib/shell/space_room_dialog.dart, src/app/scaffold/.planning/workstreams/scaffold/TICKET-text-entry-field-light-mode-contrast.md</files>
  <read_first>src/app/lib/shell/room_rail.dart, src/app/lib/cubits/rail_cubit.dart, src/app/lib/cubits/session_cubit.dart, src/app/scaffold/lib/components/text_entry_field_widget.dart</read_first>
  <action>
    Create `src/app/lib/shell/space_room_dialog.dart` (library doc header, `k`-prefixed named constants for every number, doc comments on every public member — same conventions as room_rail.dart). Implement the FROZEN contract above verbatim: the `SpaceRoomDialogMode` enum and the `showSpaceRoomDialog({...})` top-level function, plus a private `StatefulWidget` holding the dialog's local form state (name, isPublic, autoJoinRooms, and — only for createFromHeader — a type selector).

    Mode → fields mapping (UI-SPEC dialog table):
    - createFromHeader: type selector (two ScaffoldSelectionIndicatorRadio bool-coordinated pair, "Space" default / "Standalone room"); when type=Space show visibility radio pair (Public default / Private) + autoJoinRooms ScaffoldSelectionIndicatorToggle; name hint "Space name" or "Room name" per selected type.
    - createRoomInSpace: no type selector, no visibility, no toggle — name field only, hint "Room name", dialog title "New room in <parent.name>".
    - editSpace: no type selector; name (prefilled from space.name), visibility (prefilled from !space.isPublic → Private), autoJoinRooms (prefilled from space.autoJoinRooms); dialog title "Edit space"; confirm label "Save changes".

    Composition (D-05, all atoms verified present):
    - Show via `ResponsiveDrawer.show(context, title: <per-mode>, children: [...], footer: <footer row>)`.
    - Name: `TextEntryFieldWidget(logic: TextFormFieldLogic(context, controller: ..., hintText: <per-mode>, autofocus: true, onFieldSubmitted: ...))` with `maxLength: 64`.
    - Visibility + type selectors: two `ScaffoldSelectionIndicatorRadio` where `onChanged(true)` on one sets the other false in dialog state; each radio + Body label wrapped in one tappable `ScaffoldPressable`.
    - autoJoinRooms: `ScaffoldSelectionIndicatorToggle` + Body label "Auto-join rooms" + Label description "You'll automatically join every room in this space. Rooms stay visible either way." at `textSecondary`.
    - Footer: end-aligned Cancel (transparent ScaffoldPressable, "Cancel") then Confirm (accent-filled 40px pill, `ScaffoldColors.btnText` foreground, `disabled: true` while name empty). Confirm label per mode: "Create space" / "Create room" / "Save changes".
    - Spacing: field gap `space4`, label-to-control `space2`; do not re-pad dialog chrome (BottomDrawer-owned).

    Behavior (UI-SPEC behavior contract — no local optimism):
    1. Confirm with empty name → inline "Enter a name." under the field, dialog stays open, no publish.
    2. Valid confirm → build `GcsCommand()` with the matching oneof arm (createSpace / createRoom / updateSpace):
       - createSpace: `createSpace` with `name`, `isPublic`, `autoJoinRooms`
       - createRoom: `createRoom` with `name` and `parentSpaceId` (empty for standalone; `parent!.id` for createRoomInSpace)
       - updateSpace: `updateSpace` with `spaceId = space!.id`, `name`, `isPublic`, `autoJoinRooms`
       then call `transport.publishCommand(command)` and close immediately (pop the drawer) — no spinner, no waiting for SpaceTree.
    3. `publishCommand == false` → `showToast(context, "The command didn't reach the chat core. Try again.", title: "Couldn't create space|room|update space", type: ToastType.error)` (title per mode), dialog stays open.
    4. No success toast — the rail updating under the pushed SpaceTree is the confirmation.

    Then file the scaffold contrast ticket: create `src/app/scaffold/.planning/workstreams/scaffold/TICKET-text-entry-field-light-mode-contrast.md` with a short body documenting: TextEntryFieldWidget hardcodes `Colors.white` input text on a `grayPrimary` fill (`text_entry_field_widget.dart:18-25`); correct on dark palette, white-on-near-white contrast failure on light palette; owned by the scaffold workstream (read-only submodule here — do not fork/wrap-fix); entered-text-only limitation, hint text unaffected. This is the phase's scaffold-workstream handoff so the issue is not dropped.
  </action>
  <acceptance_criteria>
    - `src/app/lib/shell/space_room_dialog.dart` contains `enum SpaceRoomDialogMode` and `Future<void> showSpaceRoomDialog(` with the exact four parameters in the FROZEN contract
    - `grep -c "ResponsiveDrawer.show\|TextEntryFieldWidget\|ScaffoldSelectionIndicatorRadio\|ScaffoldSelectionIndicatorToggle\|showToast" src/app/lib/shell/space_room_dialog.dart` >= 5
    - The dialog references `createSpace` / `createRoom` / `updateSpace` and `parentSpaceId` / `spaceId` when building GcsCommand
    - `grep -c "Enter a name." src/app/lib/shell/space_room_dialog.dart` >= 1 and `grep -c "Couldn't create" src/app/lib/shell/space_room_dialog.dart` >= 1
    - `src/app/scaffold/.planning/workstreams/scaffold/TICKET-text-entry-field-light-mode-contrast.md` exists and mentions `text_entry_field_widget.dart:18-25` and `Colors.white`
    - `cd src/app && flutter analyze lib/shell/space_room_dialog.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/app && flutter analyze lib/shell/space_room_dialog.dart</automated>
  </verify>
  <done>The frozen dialog entry contract exists; create/edit modes publish correct GcsCommands and file the scaffold contrast ticket.</done>
</task>

<task type="auto">
  <name>Task 2: Dialog widget tests (create space/room, edit, empty-name, publish-false)</name>
  <files>src/app/test/shell/space_room_dialog_test.dart</files>
  <read_first>src/app/test/chat_shell_test.dart, src/app/test/cubits/shell_cubits_test.dart, src/app/lib/shell/space_room_dialog.dart</read_first>
  <action>
    Create `src/app/test/shell/space_room_dialog_test.dart` (create the `shell/` test dir if absent). Copy the `_RecordingTransport` double from shell_cubits_test.dart (implements `GcsCommandTransport`, records `List<GcsCommand>`, `publishCommand` returns true). Wrap the widget in `MaterialApp(theme: GcsTheme.light, ...)` per the chat_shell_test harness; construct cubits in test bodies with `addTearDown`.

    Tests (drive `showSpaceRoomDialog` with the fake transport):
    - CreateSpacePublishesCreateSpace: mode createFromHeader, enter a name, toggle Private, toggle autoJoinRooms on, tap confirm → `transport.commands.single.hasCreateSpace()` is true, `createSpace.name/isPublic/autoJoinRooms` match the form.
    - CreateStandaloneRoomPublishesEmptyParent: mode createFromHeader, select "Standalone room", name it → published `createRoom.parentSpaceId` is empty.
    - CreateRoomInSpacePublishesParentId: mode createRoomInSpace with a `parent` RailSpace(id 'space-abc', ...) → published `createRoom.parentSpaceId == 'space-abc'`.
    - EditSpacePublishesUpdateSpace: mode editSpace with a `space` RailSpace(id 'space-xyz', name 'ops', isPublic false, autoJoinRooms true) → confirm publishes `updateSpace.spaceId == 'space-xyz'` with prefilled values.
    - EmptyNameDoesNotPublish: tap confirm with empty name → no command recorded, "Enter a name." visible.
    - PublishFalseShowsToast: a transport whose `publishCommand` returns false → confirm → `find.text` for the toast message "The command didn't reach the chat core. Try again." appears.

    Use the bounded-frame `_pumpUntil` pattern (never `sleep`); reuse `find.byType`/`find.text` assertions.
  </action>
  <acceptance_criteria>
    - `src/app/test/shell/space_room_dialog_test.dart` contains the six test names above (or equivalent single-purpose tests)
    - The file defines a `_RecordingTransport` (or equivalent) implementing `GcsCommandTransport`
    - `cd src/app && flutter test test/shell/space_room_dialog_test.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/app && flutter test test/shell/space_room_dialog_test.dart</automated>
  </verify>
  <done>Dialog create/edit/validation/error paths are covered by widget tests against a fake transport.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dialog form → GcsCommand publish | User-entered name/visibility/config leave the Dart UI here (crosses to C++ later) |
| C++ ErrorNotice → showToast | Pushed raw error strings surface in a toast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | dialog name input | mitigate | client `maxLength: 64` + empty-name inline validation; C++ re-validates empty name / unknown parent as defense in depth (Plan 02-02) |
| T-02-10 | Information Disclosure / Access Control | is_public toggle | accept | `is_public` is display metadata ONLY in Phase 2 — no access enforcement exists until Phase 4/encryption; do not imply privacy enforcement |
| T-02-SC | Tampering | package installs | accept | zero new packages this phase — slopcheck gate not applicable |
</threat_model>

<verification>
Full Dart-side check for this plan:
`cd src/app && flutter analyze lib test && flutter test test/shell/space_room_dialog_test.dart`
</verification>

<success_criteria>
- The frozen showSpaceRoomDialog contract ships exactly as specified (Plan 02-05 will call it)
- Create space, create standalone room, create room in space, and edit space each publish the correct GcsCommand oneof arm
- Empty-name inline validation and publish-false error toast are test-proven; no local optimism
- Scaffold contrast ticket filed
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/02-spaces-rooms/02-04-SUMMARY.md` when done
</output>
