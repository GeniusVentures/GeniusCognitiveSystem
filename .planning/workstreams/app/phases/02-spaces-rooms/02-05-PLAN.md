---
phase: 02-spaces-rooms
plan: 05
type: execute
wave: 4
depends_on: [02-03, 02-04]
files_modified:
  - src/app/lib/shell/room_rail.dart
  - src/app/test/chat_shell_test.dart
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03]

must_haves:
  truths:
    - "The rail renders a Spaces tree section + a standalone Rooms section (hidden when empty) from pushed state"
    - "Catalog-but-not-joined room rows render dimmed/disabled; toggling autoJoinRooms dims/un-dims them (success criterion 3 observable)"
    - "The rail shows a loading skeleton until the first SpaceTree push"
  artifacts:
    - path: "src/app/lib/shell/room_rail.dart"
      provides: "toolbar + Spaces tree + standalone Rooms sections + badges + '+'/edit affordances + loading/empty states"
      contains: "ScaffoldBadge"
    - path: "src/app/test/chat_shell_test.dart"
      provides: "rail tree render widget tests (sections, badge, empty CTA, disabled rows)"
  key_links:
    - from: "room_rail.dart"
      to: "RailCubit state (spaces/standaloneRooms/rooms/activeRoom/treeReceived)"
      via: "BlocBuilder"
      pattern: "BlocBuilder<RailCubit"
    - from: "room_rail.dart '+'/edit affordances"
      to: "space_room_dialog.dart showSpaceRoomDialog"
      via: "tap handler with context.read<SessionCubit>() as transport"
      pattern: "showSpaceRoomDialog"
---

<objective>
Grow the left rail from the flat Phase 1 room list into the spaces/rooms tree: a persistent toolbar "+" affordance, a Spaces section with expandable space nodes (private badge, per-node "+" and edit affordances, nested room rows), a standalone Rooms section hidden when empty, joined-vs-unjoined room dimming, and the loading skeleton until the first SpaceTree push. Add rail widget tests.

Purpose: This is the user-visible surface for all three requirements (CORE-01 spaces list, CORE-02 nested + standalone rooms, CORE-03 the dim/un-dim observable). It consumes the Plan 02-03 tree state and the Plan 02-04 dialog entry.

Output: A tree-rendering `RoomRail` and rail widget tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/phases/02-spaces-rooms/02-UI-SPEC.md
@.planning/workstreams/app/phases/02-spaces-rooms/02-PATTERNS.md

<interfaces>
<!-- State surface to consume (from Plan 02-03, src/app/lib/cubits/rail_cubit.dart). -->
```dart
class RailSpace { final String id, name; final bool isPublic, autoJoinRooms; final List<RailRoom> rooms; }
class RailRoom  { final String id, name; String get topic => 'gcs/chat/$id'; }
class RailState { final List<RailSpace> spaces; final List<RailRoom> standaloneRooms; final List<String> rooms; final String? activeRoom; final bool treeReceived; }
```

<!-- Dialog entry to call (from Plan 02-04, FROZEN). -->
```dart
enum SpaceRoomDialogMode { createFromHeader, createRoomInSpace, editSpace }
Future<void> showSpaceRoomDialog({required BuildContext context, required GcsCommandTransport transport, required SpaceRoomDialogMode mode, RailSpace? space, RailSpace? parent})
```

<!-- Current row/section conventions to reuse (room_rail.dart). -->
- Section header: `Text('Rooms', style: textTheme.titleSmall?.copyWith(color: palette.textSecondary, letterSpacing: kHeaderLetterSpacing))`
- Row: `ScaffoldPressable(onPressed: ...)` → `DecoratedBox(btnFilterSelected tint when selected, radiusMd)` → `Padding(space6/space2)` → `ConstrainedBox(minHeight: minTouchTarget - space2*2)` → Row(icon + ellipsized Body text)
- Empty state: `ScaffoldStateView(state: 'empty', emptyHeadline: ..., emptyBody: ...)`
- The rail is inside GCSChat's `MultiBlocProvider`, so `context.read<SessionCubit>()` yields the `GcsCommandTransport`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the spaces/rooms tree rail (sections, badges, affordances, dimming, skeleton)</name>
  <files>src/app/lib/shell/room_rail.dart</files>
  <read_first>src/app/lib/shell/room_rail.dart, src/app/lib/cubits/rail_cubit.dart, src/app/lib/shell/space_room_dialog.dart, src/app/lib/cubits/session_cubit.dart</read_first>
  <action>
    Rewrite src/app/lib/shell/room_rail.dart's build (keep the file conventions: library doc header, `kWidth`, `kHeaderLetterSpacing`, `roomDisplayName`). Import `../cubits/session_cubit.dart` (for GcsCommandTransport via SessionCubit) and `space_room_dialog.dart`.

    Structure, top-to-bottom (UI-SPEC rail contract):
    - Toolbar: always present in every rail state — `Padding(space8)` row, start-aligned "Spaces" label (`textTheme.titleSmall`, `textSecondary`, `kHeaderLetterSpacing`), end-aligned 40px circular accent "+" (`ScaffoldSurface(shape: BoxShape.circle, color: lightGreenPrimary)` + `Icons.add` tinted `ScaffoldColors.btnText`), wrapped in `ScaffoldPressable(semanticLabel: 'Create space or room')` with a 48px hit area. Tap calls `showSpaceRoomDialog(context: context, transport: context.read<SessionCubit>(), mode: SpaceRoomDialogMode.createFromHeader)`.
    - Loading: when `!rail.treeReceived` → below the toolbar render `ScaffoldStateView(state: 'loading')` (skeleton; toolbar stays visible).
    - Empty: when `rail.treeReceived` and `rail.spaces.isEmpty && rail.standaloneRooms.isEmpty` → `ScaffoldStateView` empty variant: emptyHeadline "No spaces yet", emptyBody "Spaces organize your rooms. Create your first space to get started.", `emptyAction` "Create space" (accent button) that opens the createFromHeader dialog.
    - Spaces section: for each `RailSpace` render a space node row + nested child rows (see below).
    - Standalone Rooms section: only when `rail.standaloneRooms.isNotEmpty` — a "Rooms" section header (same style as the toolbar label) preceded by a `space12` break, then each standalone room as a room row. Hidden entirely when empty (no empty-section noise).

    Space node row (start→end): chevron (`AnimatedRotation`, `textSecondary`) → space name (Body, ellipsized) → `ScaffoldBadge(variant: text, text: 'Private', badgeColor: palette.textSecondary)` only when `!space.isPublic` (gap `space2`) → spacer → edit icon (`Icons.edit_outlined`, `textSecondary`) → "+" icon (`Icons.add`, `textSecondary`). Edit calls `showSpaceRoomDialog(mode: SpaceRoomDialogMode.editSpace, space: space, ...)`; "+" calls `showSpaceRoomDialog(mode: SpaceRoomDialogMode.createRoomInSpace, parent: space, ...)`. Whole-row tap toggles expansion (spaces are not selectable in Phase 2). Expansion is view-local: convert `RoomRail` to hold a `Map<String, bool>` of space id → expanded (default expanded for new spaces, keyed by id so it survives `setTree` full replacements); animate with `AnimatedSize` (`ScaffoldMotionDurations.medium`) + chevron `AnimatedRotation` (`ScaffoldMotionDurations.short`), zero-duration under `ScaffoldMotion.reducedMotion`. Do NOT use `ScaffoldDisclosure` (its title slot cannot carry badge/trailing actions).

    Nested room rows: indent one `space6` beyond the space row. Joined status = `rail.rooms.contains(room.topic)`:
    - joined + selected (room.topic == rail.activeRoom): `ScaffoldColors.btnFilterSelected` tint, `radiusMd`, `textPrimary`.
    - joined, not selected: no tint, `textSecondary`, tap → `context.read<RailCubit>().selectRoom(room.topic)`.
    - catalog-not-joined: `ScaffoldPressable(disabled: true)` — `textSecondary` + `ScaffoldDisabledOverlay` at 0.40 + `Semantics(enabled: false)`, non-tappable. (This dim/un-dim transition IS the criterion-3 observable; standalone rooms are permanently dimmed in Phase 2.)

    Expanded space with no rooms: inline "No rooms yet" row (Body, `textSecondary`, indented one level, non-interactive).

    Keep `roomDisplayName` used for the legacy flat room list only if any joined-topic path still needs it; the tree renders `RailRoom.name` directly (never the topic-derived name).
  </action>
  <acceptance_criteria>
    - `src/app/lib/shell/room_rail.dart` references `rail.treeReceived`, `rail.spaces`, `rail.standaloneRooms`, and `showSpaceRoomDialog`
    - `grep -c "ScaffoldBadge\|ScaffoldStateView\|showSpaceRoomDialog\|ScaffoldPressable" src/app/lib/shell/room_rail.dart` >= 4
    - Contains the exact copy strings "No spaces yet", "Spaces organize your rooms. Create your first space to get started.", "Create space", "Create space or room", "No rooms yet", and "Private"
    - Contains a disabled-row path: `ScaffoldPressable(disabled: true)` (or equivalent) for catalog-but-not-joined rooms
    - The "+"/edit affordances pass `mode: SpaceRoomDialogMode.createFromHeader|createRoomInSpace|editSpace` per UI-SPEC
    - `cd src/app && flutter analyze lib/shell/room_rail.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/app && flutter analyze lib/shell/room_rail.dart</automated>
  </verify>
  <done>The rail renders the toolbar + Spaces tree + standalone Rooms + loading/empty states, with dimmed unjoined rows and wired affordances.</done>
</task>

<task type="auto">
  <name>Task 2: Rail tree widget tests (sections, badge, empty CTA, disabled rows)</name>
  <files>src/app/test/chat_shell_test.dart</files>
  <read_first>src/app/test/chat_shell_test.dart, src/app/lib/shell/room_rail.dart, src/app/lib/cubits/rail_cubit.dart</read_first>
  <action>
    Extend src/app/test/chat_shell_test.dart (reuse the existing `_pumpUntil` bounded-frame helper, `MaterialApp(theme: GcsTheme.light, ...)` wrapper, and cubit-injection harness — no sleeps). Add rail-tree cases:

    - LoadingSkeletonUntilTreeReceived: a fresh RailCubit (treeReceived false) renders `ScaffoldStateView(state: 'loading')` (assert via `find.byType(ScaffoldStateView)` / loading variant) while the toolbar is still present.
    - EmptyCatalogShowsCreateCta: push `setTree([], [])` then pump — assert "No spaces yet" headline and a "Create space" action are visible.
    - SpacesSectionWithPrivateBadge: push `setTree([SpaceRecord(name 'ops', isPublic false)], [])` — assert "ops" and "Private" render.
    - NestedRoomAndStandaloneRoomRender: push a SpaceRecord + two RoomRecords (one with parent = space id, one with empty parent) — assert the nested room name renders under the space and the standalone room renders under a "Rooms" header; assert the "Rooms" header is hidden when no standalone rooms exist.
    - UnjoinedRoomRowIsDisabled: with a nested room whose topic is NOT in `rail.state.rooms`, assert its `ScaffoldPressable` has `disabled == true` (or the row renders with `Semantics(enabled: false)`); then `rail.setRooms(['gcs/chat/<id>'])` and assert the same row is now tappable (this mirrors the criterion-3 dim/un-dim observable at the widget level).

    Construct proto objects via the regenerated pb (`SpaceRecord()..name=..`, `RoomRecord()..parentSpaceId=..`) and drive the cubit directly; use `addTearDown` per cubit.
  </action>
  <acceptance_criteria>
    - `src/app/test/chat_shell_test.dart` contains tests asserting the loading skeleton, the "No spaces yet" empty state, the "Private" badge, nested + standalone rendering, and the disabled unjoined row
    - `cd src/app && flutter test test/chat_shell_test.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/app && flutter test test/chat_shell_test.dart</automated>
  </verify>
  <done>Rail tree rendering — sections, private badge, empty CTA, and the joined/unjoined dimming transition — is widget-test-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rail rows → RailCubit.selectRoom | Row taps drive selection; unjoined rows must not be selectable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | unjoined room row selection | mitigate | `ScaffoldPressable(disabled: true)` + `Semantics(enabled: false)` on catalog-but-not-joined rows, plus the existing `selectRoom` joined-only guard (defense in depth) |
| T-02-SC | Tampering | package installs | accept | zero new packages this phase — slopcheck gate not applicable |
</threat_model>

<verification>
Full Dart-side check for this plan:
`cd src/app && flutter analyze lib test && flutter test test/chat_shell_test.dart`
</verification>

<success_criteria>
- Toolbar "+" always visible; Spaces tree + standalone Rooms (hidden when empty) render from pushed state
- Private badge shows only on private spaces; unjoined rows dim/disable; loading skeleton precedes the first SpaceTree
- Affordances open the correct dialog mode; rail widget tests cover all states
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/02-spaces-rooms/02-05-SUMMARY.md` when done
</output>
