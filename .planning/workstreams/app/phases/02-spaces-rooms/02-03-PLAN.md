---
phase: 02-spaces-rooms
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/lib/generated/proto/gcs_chat.pb.dart
  - src/app/lib/generated/proto/gcs_chat.pbenum.dart
  - src/app/lib/generated/proto/gcs_chat.pbjson.dart
  - src/app/lib/cubits/rail_cubit.dart
  - src/app/lib/cubits/session_cubit.dart
  - src/app/test/cubits/shell_cubits_test.dart
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03]

must_haves:
  truths:
    - "Regenerated Dart pb exposes the new entity messages (SpaceRecord, RoomRecord, SpaceTree, create/update command accessors)"
    - "A pushed SpaceTree event populates the rail's tree state (spaces grouped, standalone rooms separate, treeReceived flag set)"
  artifacts:
    - path: "src/app/lib/generated/proto/gcs_chat.pb.dart"
      provides: "regenerated Dart protobuf classes"
      contains: "class SpaceRecord"
    - path: "src/app/lib/cubits/rail_cubit.dart"
      provides: "RailSpace/RailRoom models + RailState.spaces/standaloneRooms/treeReceived + setTree"
      contains: "setTree"
    - path: "src/app/lib/cubits/session_cubit.dart"
      provides: "hasSpaceTree dispatch arm"
      contains: "hasSpaceTree"
    - path: "src/app/test/cubits/shell_cubits_test.dart"
      provides: "SpaceTree dispatch + setTree grouping tests"
  key_links:
    - from: "session_cubit.dart _dispatchEvent"
      to: "RailCubit.setTree"
      via: "event.spaceTree.space / event.spaceTree.room"
      pattern: "hasSpaceTree"
    - from: "rail_cubit.dart setTree"
      to: "SpaceRecord/RoomRecord (pb)"
      via: "mapping + parent_space_id grouping"
      pattern: "parentSpaceId"
---

<objective>
Regenerate the Dart protobuf bindings from the extended `gcs_chat.proto` (pinned protoc_plugin 22.5.0), evolve `RailState` from a flat room list to a spaces/rooms tree, add the `SpaceTree` dispatch arm to `SessionCubit`, and test both.

Purpose: This is the Dart-side contract bridge — every UI change in Plans 02-04/02-05 consumes the regenerated pb classes and the tree-shaped `RailState`. It must land before any dialog/rail work.

Output: Regenerated + committed pb files, a tree-capable `RailCubit`, a `SessionCubit` that routes `SpaceTree` pushes, and green cubit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/phases/02-spaces-rooms/02-RESEARCH.md
@.planning/workstreams/app/phases/02-spaces-rooms/02-PATTERNS.md
@.planning/workstreams/app/phases/02-spaces-rooms/02-UI-SPEC.md

<interfaces>
<!-- Current RailState/RailCubit surface being extended (src/app/lib/cubits/rail_cubit.dart). -->
```dart
class RailState {
  const RailState({this.rooms = const <String>[], this.activeRoom});
  final List<String> rooms;      // joined topics (RoomList pushes) — KEEP unchanged
  final String? activeRoom;      // topic string — KEEP unchanged
  RailState copyWith({List<String>? rooms, String? activeRoom, bool clearActiveRoom = false});
}
class RailCubit extends Cubit<RailState> {
  void setRooms(List<String> rooms);   // unchanged (joined view)
  void selectRoom(String roomTopic);   // unchanged (joined-only guard)
}
```

Current dispatch (src/app/lib/cubits/session_cubit.dart, _dispatchEvent):
```dart
void _dispatchEvent(GcsEvent event) {
  if (event.hasRoomList()) { _railCubit?.setRooms(event.roomList.roomTopic); return; }
  if (event.hasReadiness()) { ... }
  if (event.hasMessage()) { ... }
  if (event.hasError()) { ... }
}
```

Regenerated pb accessors to expect (from the extended proto, Plan 02-01):
- `event.hasSpaceTree()`, `event.spaceTree.space` (List<SpaceRecord>), `event.spaceTree.room` (List<RoomRecord>)
- `GcsCommand.createSpace` / `createRoom` / `updateSpace` (for Plans 02-04/02-05)
- `SpaceRecord.id/name/isPublic/autoJoinRooms`, `RoomRecord.id/name/parentSpaceId`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate Dart pb with pinned protoc_plugin 22.5.0</name>
  <files>src/app/lib/generated/proto/gcs_chat.pb.dart, src/app/lib/generated/proto/gcs_chat.pbenum.dart, src/app/lib/generated/proto/gcs_chat.pbjson.dart</files>
  <read_first>src/app/lib/generated/proto/gcs_chat.pb.dart, src/proto/gcs_chat.proto</read_first>
  <action>
    Regenerate the Dart protobuf bindings from the extended `src/proto/gcs_chat.proto` using the PINNED plugin only (Pitfall 1 — latest protoc_plugin 25.x emits `BuilderInfo.aE`, which breaks against protobuf 4.2.0). First ensure the pin: `dart pub global activate protoc_plugin 22.5.0`, then verify the header at `$HOME/.pub-cache/bin/protoc-gen-dart` reports 22.5.0. Then run the exact command (verbatim from 01-05-PLAN.md):

    cd src/app && /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/thirdparty/build/OSX/Debug/protobuf/bin/protoc \
      -I ../proto --dart_out=lib/generated/proto \
      --plugin=protoc-gen-dart=$HOME/.pub-cache/bin/protoc-gen-dart \
      ../proto/gcs_chat.proto

    This regenerates `lib/generated/proto/gcs_chat.pb.dart`, `.pbenum.dart`, and `.pbjson.dart` in place. These files are tracked by design — the regeneration output is committed, not generated at build time.
  </action>
  <acceptance_criteria>
    - `grep -c "class SpaceRecord" src/app/lib/generated/proto/gcs_chat.pb.dart` equals 1; same for `class RoomRecord`, `class EntityManifest`, `class CreateSpaceCommand`, `class CreateRoomCommand`, `class UpdateSpaceCommand`, `class SpaceTree`
    - `grep -c "hasSpaceTree" src/app/lib/generated/proto/gcs_chat.pb.dart` equals 1
    - `grep -c "createSpace\|createRoom\|updateSpace" src/app/lib/generated/proto/gcs_chat.pb.dart` >= 3 (command oneof accessors)
    - `grep -c "BuilderInfo.aE" src/app/lib/generated/proto/gcs_chat.pb.dart` equals 0 (no unpinned-plugin breakage)
    - `cd src/app && flutter analyze lib/generated/proto` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/app && flutter analyze lib/generated/proto</automated>
  </verify>
  <done>Dart pb bindings expose all seven new messages + oneof accessors; analyze is clean.</done>
</task>

<task type="auto">
  <name>Task 2: RailState flat → tree (RailSpace/RailRoom + setTree)</name>
  <files>src/app/lib/cubits/rail_cubit.dart</files>
  <read_first>src/app/lib/cubits/rail_cubit.dart</read_first>
  <action>
    Extend src/app/lib/cubits/rail_cubit.dart (keep setRooms/selectRoom/rooms/activeRoom exactly as-is — the joined view is a separate lifecycle, D-02). Import `package:flutter_app/generated/proto/gcs_chat.pb.dart`.

    Add two immutable model classes (doc-comment every member with `///`, per house style):
    - `class RailRoom { final String id; final String name; String get topic => 'gcs/chat/$id'; }` with a `const RailRoom({required this.id, required this.name});`
    - `class RailSpace { final String id; final String name; final bool isPublic; final bool autoJoinRooms; final List<RailRoom> rooms; }` with a const constructor.

    Extend RailState:
    - new fields `final List<RailSpace> spaces;` and `final List<RailRoom> standaloneRooms;` (default `const <RailSpace>[]` / `const <RailRoom>[]`), plus `final bool treeReceived;` (default `false` — this is the loading-vs-empty flag from UI-SPEC "Rail state during startup").
    - extend `copyWith` with nullable-list replacement for spaces/standaloneRooms and a `treeReceived` override.

    Add to RailCubit a full-replacement `setTree`:
    - signature `void setTree(List<SpaceRecord> spaces, List<RoomRecord> rooms);`
    - map each SpaceRecord → RailSpace (id/name/isPublic/autoJoinRooms, empty rooms list), each RoomRecord → RailRoom (id/name).
    - group rooms: rooms with non-empty `parentSpaceId` are appended to the matching RailSpace.rooms (preserve pushed order); rooms with empty `parentSpaceId` become standaloneRooms (D-01: empty parent = standalone).
    - emit `state.copyWith(spaces: ..., standaloneRooms: ..., treeReceived: true)` as a FULL replacement (no merge). Do NOT touch `rooms`/`activeRoom` — RoomList pushes still drive those via setRooms.
  </action>
  <acceptance_criteria>
    - `src/app/lib/cubits/rail_cubit.dart` contains `class RailRoom`, `class RailSpace`, `final List<RailSpace> spaces`, `final List<RailRoom> standaloneRooms`, `final bool treeReceived`, and `void setTree(List<SpaceRecord> spaces, List<RoomRecord> rooms)`
    - `setTree` references `parentSpaceId` (grouping) and `treeReceived: true` in the emit
    - `setRooms`/`selectRoom` bodies are unchanged (rooms + activeRoom remain the joined view)
    - `cd src/app && flutter analyze lib/cubits/rail_cubit.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/app && flutter analyze lib/cubits/rail_cubit.dart</automated>
  </verify>
  <done>RailState carries a tree (spaces + standaloneRooms + treeReceived) while rooms/activeRoom keep their joined-view semantics.</done>
</task>

<task type="auto">
  <name>Task 3: SpaceTree dispatch arm + cubit tests</name>
  <files>src/app/lib/cubits/session_cubit.dart, src/app/test/cubits/shell_cubits_test.dart</files>
  <read_first>src/app/lib/cubits/session_cubit.dart, src/app/test/cubits/shell_cubits_test.dart</read_first>
  <action>
    Edit src/app/lib/cubits/session_cubit.dart `_dispatchEvent`: add the SpaceTree arm FIRST, above the existing `hasRoomList` arm (tree renders before membership):
    `if (event.hasSpaceTree()) { _railCubit?.setTree(event.spaceTree.space, event.spaceTree.room); return; }`

    Extend src/app/test/cubits/shell_cubits_test.dart (use the existing `_RecordingTransport` and `handlePushedBytes` patterns):
    - Test that `handlePushedBytes` on a `GcsEvent` with `spaceTree` set (build a `SpaceTree` with one `SpaceRecord` "ops" + one `RoomRecord` "general" whose `parentSpaceId` = the space id) leaves `rail.state.spaces` with one RailSpace carrying the grouped room, `rail.state.standaloneRooms` empty, and `rail.state.treeReceived == true`.
    - Test standalone grouping: a RoomRecord with empty `parentSpaceId` lands in `standaloneRooms`, not in any space.
    - Test that a subsequent `setRooms` (RoomList) still replaces `rail.state.rooms` without clearing the tree fields.

    Construct proto objects exactly like the existing tests do (`GcsEvent()..spaceTree = (SpaceTree()..space.add(...)..room.add(...))` then `.writeToBuffer()`); do not use sleeps.
  </action>
  <acceptance_criteria>
    - `src/app/lib/cubits/session_cubit.dart` contains `if (event.hasSpaceTree()) { _railCubit?.setTree(event.spaceTree.space, event.spaceTree.room); return; }` above the `hasRoomList` arm
    - `src/app/test/cubits/shell_cubits_test.dart` contains tests asserting `rail.state.treeReceived == true`, `rail.state.spaces`, and `rail.state.standaloneRooms` grouping
    - `cd src/app && flutter test test/cubits/shell_cubits_test.dart` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/app && flutter test test/cubits/shell_cubits_test.dart</automated>
  </verify>
  <done>Pushed SpaceTree bytes populate the rail tree; grouping (parent vs standalone) and the treeReceived flag are test-proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| C++ push port → SessionCubit.handlePushedBytes | Pushed event bytes are decoded here (trusted C++ output, but still guarded) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | handlePushedBytes GcsEvent decode | mitigate | existing try/catch emits a raw error string, never crashes (T-01-11-01); the SpaceTree arm is reached only after a successful decode |
| T-02-SC | Tampering | package installs | accept | zero new packages this phase — slopcheck gate not applicable |
</threat_model>

<verification>
Full Dart-side check for this plan:
`cd src/app && flutter analyze lib test && flutter test test/cubits/shell_cubits_test.dart`
</verification>

<success_criteria>
- Dart pb regenerated with the pinned 22.5.0 plugin and committed
- RailCubit.setTree maps + groups pushed SpaceTree records into RailSpace/RailRoom, sets treeReceived
- SessionCubit routes hasSpaceTree to setTree first; shell_cubits_test proves the dispatch + grouping
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/02-spaces-rooms/02-03-SUMMARY.md` when done
</output>
