---
phase: 03-messaging
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/lib/generated/proto/gcs_chat.pb.dart
  - src/app/lib/generated/proto/gcs_chat.pbenum.dart
  - src/app/lib/generated/proto/gcs_chat.pbjson.dart
  - src/app/lib/cubits/message_flow_cubit.dart
  - src/app/lib/cubits/session_cubit.dart
  - src/app/lib/cubits/composer_cubit.dart
  - src/app/lib/shell/gcs_shell.dart
  - src/app/test/cubits/shell_cubits_test.dart
autonomous: true
requirements: [CORE-04]
must_haves:
  truths:
    - "The Dart proto bindings carry sender + tombstone fields and the MessageHistory batch"
    - "MessageFlowCubit upserts by instanceId so pending -> complete replaces in place instead of duplicating"
    - "SessionCubit dispatches a pushed messageHistory event as a full-list replaceAll"
    - "A refused/failed message send surfaces the 'Send failed' toast (D-07 transport-error surface)"
  artifacts:
    - path: src/app/lib/cubits/message_flow_cubit.dart
      provides: "upsert + replaceAll + sender-aware bubble mapping"
      contains: "void upsert("
  key_links:
    - from: src/app/lib/cubits/session_cubit.dart
      to: src/app/lib/cubits/message_flow_cubit.dart
      via: "_dispatchEvent messageHistory -> flow.replaceAll"
      pattern: "replaceAll"
    - from: src/app/lib/shell/gcs_shell.dart
      to: "frontend_scaffold showToast (toast_manager.dart)"
      via: "showToast on refused publish + ERROR-state message arrival"
      pattern: "showToast"
---

<objective>
Regenerate the Dart protobuf bindings for the Phase 3 schema and update the thin Dart cubits to render pushed message events correctly.

Purpose: D-06: MessageHistory batch -> replaceAll. D-07: pending/complete/error upsert by id. D-04: render C++-stamped sender as truncated short-form on peer bubbles. No Dart-side authority or synthesis — every field is a pushed snapshot.

Output: regenerated `gcs_chat.pb*.dart`, updated `message_flow_cubit.dart` + `session_cubit.dart`, extended `shell_cubits_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/lib/cubits/message_flow_cubit.dart
@src/app/lib/cubits/session_cubit.dart
@src/app/lib/generated/chat/chat_message_flow_cubit.dart
@src/app/lib/generated/chat/chat_message_flow.dart
@src/app/test/cubits/shell_cubits_test.dart
@src/app/README.md

<interfaces>
<!-- Generated base the app cubit delegates to (never hand-edit). -->
```
// ChatMessageFlowCubit (generated): static List<ChatFlowItem> cappedItems(List<ChatFlowItem> items);
// ChatFlowItemTextBubble { String instanceId; String role; String state; String text; String? senderName; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate Dart proto + add MessageFlowCubit upsert/replaceAll + SessionCubit messageHistory dispatch + transport-error toast</name>
  <files>src/app/lib/generated/proto/gcs_chat.pb.dart, src/app/lib/cubits/message_flow_cubit.dart, src/app/lib/cubits/session_cubit.dart, src/app/lib/cubits/composer_cubit.dart, src/app/lib/shell/gcs_shell.dart</files>
  <read_first>
    - src/app/README.md (proto regen command, lines ~34-36; pinned protoc_plugin 22.5.0)
    - src/app/lib/cubits/message_flow_cubit.dart (append + buildChatFlowItemTextBubble, lines 47-62)
    - src/app/lib/cubits/session_cubit.dart (_dispatchEvent lines 364-387)
    - src/app/lib/generated/chat/chat_message_flow_cubit.dart (cappedItems / replaceAll)
    - src/app/lib/cubits/composer_cubit.dart (send path, returns void today)
    - src/app/lib/shell/gcs_shell.dart (_ComposerBar/_SendButton submit paths)
    - src/app/lib/shell/space_room_dialog.dart (refused-publish showToast precedent, lines 293-306)
    - src/app/scaffold/lib/components/toast/toast_manager.dart (showToast signature + ToastType)
    - src/app/lib/generated/chat/chat_message_flow.dart (ChatFlowItem sealed shape for the error listener)
  </read_first>
  <action>
    1. Regenerate the Dart bindings for the extended schema: from `src/app`, run the README command with the pin `protoc_plugin 22.5.0` targeting `../proto/gcs_chat.proto` into `lib/generated/proto`. Regenerate `gcs_chat.pb.dart`, `gcs_chat.pbenum.dart`, `gcs_chat.pbjson.dart` (never hand-edit them). Confirm the regenerated `gcs_chat.pb.dart` exposes `ChatMessageState.sender` and a `MessageHistory` class + `GcsEvent.messageHistory`.
    2. In `message_flow_cubit.dart` add two methods and a sender helper (keep `append` for compatibility):
       - `void upsert(ChatFlowItem item)` — rebuild the list with any existing entry whose `instanceId == item.instanceId` removed, append `item`, then `emit(ChatMessageFlowCubit.cappedItems(next))` (uses the generated cap invariant; ~10 lines).
       - `void replaceAll(List<ChatFlowItem> items)` — `emit(ChatMessageFlowCubit.cappedItems(items))`.
       - In `buildChatFlowItemTextBubble`, set `senderName: message.sender.isEmpty ? null : _truncateSender(message.sender)` using a file-level helper `String _truncateSender(String address)` with named constants `const int kSenderShortPrefixLength = 8; const int kSenderShortSuffixLength = 8;` that returns `'0x' + address.substring(2, 2 + kSenderShortPrefixLength) + '…' + address.substring(address.length - kSenderShortSuffixLength)` when `address.length > kSenderShortPrefixLength + kSenderShortSuffixLength + 4`, else `address` as-is. Do NOT map the role here — the C++ side stamps `user_self`/`user_peer` on BOTH the live path (`Messaging::PushMessage`) and the history replay path (`Messaging::QueryHistory`, 03-04), so `message.role` is already correct when it arrives here; the Dart side renders `message.role` directly via `kRoleVariants`.
    3. In `session_cubit.dart` `_dispatchEvent`: change the `hasMessage()` arm to call `flow.upsert(...)` instead of `flow.append(...)` (D-07). Insert a new arm after it: `if (event.hasMessageHistory()) { final flow = _messageFlowCubit; if (flow != null) { flow.replaceAll([for (final m in event.messageHistory.message) flow.buildChatFlowItemTextBubble(m)]); } return; }` (D-06 replaceAll semantics). Keep SpaceTree-first ordering unchanged.
    4. Wire the D-07 "Send failed" toast (UI-SPEC copy: title "Send failed", message "Message not sent. Try again.", `ToastType.error`). In `composer_cubit.dart` change `void send()` to `bool send()` — return `true` when the draft is published (draft cleared) or there is nothing sendable (no-op), and `false` ONLY when a sendable draft's `publishCommand` was refused (transport failure). In `gcs_shell.dart`: add `import 'package:frontend_scaffold/components/toast/toast_manager.dart';`, add the copy constants `const String kSendFailedToastTitle = 'Send failed'; const String kSendFailedToastMessage = 'Message not sent. Try again.';`, and (a) in `_ComposerBar.onSubmit` and the `_SendButton.onPressed` fallback path call `showToast(context, kSendFailedToastMessage, title: kSendFailedToastTitle, type: ToastType.error)` when `send()` returns false (mirrors `space_room_dialog.dart` refused-publish toast); and (b) wrap the `ChatMessageFlow` in a `BlocListener<MessageFlowCubit, List<ChatFlowItem>>` that, on each emission, toasts once per NEW item whose `state == 'error'` (the D-07 terminal transport-failure signal the C++ side pushes when a topic publish throws — 03-04) by tracking toasted `instanceId`s in a shell-owned `Set<String>`; pattern-match the generated sealed `ChatFlowItem` to read `ChatFlowItemTextBubble.state`/`.instanceId`. The C++ validation failures (empty/too-long/not-joined) stay inline via `PostErrorNotice -> SessionState.error` composer hint (unchanged) — they never push an ERROR-state message, so they never toast.
  </action>
  <acceptance_criteria>
    - `grep -c "get sender" src/app/lib/generated/proto/gcs_chat.pb.dart` is 1 (sender field exists on ChatMessageState).
    - `grep -c "class MessageHistory" src/app/lib/generated/proto/gcs_chat.pb.dart` is 1.
    - `src/app/lib/cubits/message_flow_cubit.dart` contains `void upsert(`, `void replaceAll(`, and `kSenderShortPrefixLength`.
    - `src/app/lib/cubits/session_cubit.dart` contains `hasMessageHistory()` and `flow.upsert(`.
    - `src/app/lib/cubits/composer_cubit.dart` contains `bool send(`.
    - `src/app/lib/shell/gcs_shell.dart` contains `showToast` and `kSendFailedToastTitle`.
    - No edits under `src/app/scaffold/` or `src/app/lib/generated/chat/`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd src/app && dart analyze --fatal-infos'</automated>
  </verify>
  <done>Dart analyze is clean; the regenerated bindings, upsert/replaceAll, sender truncation, and messageHistory dispatch compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend shell_cubits_test.dart for upsert + messageHistory dispatch + sender label + composer send</name>
  <files>src/app/test/cubits/shell_cubits_test.dart</files>
  <read_first>
    - src/app/test/cubits/shell_cubits_test.dart (MessageFlowCubit group lines 157-216, handlePushedBytes dispatch test 334-374, _RecordingBindings/_RecordingTransport fakes)
    - src/app/lib/cubits/message_flow_cubit.dart (upsert/replaceAll/buildChatFlowItemTextBubble just added)
    - src/app/lib/cubits/composer_cubit.dart (send() bool return just added)
  </read_first>
  <behavior>
    - Test 1 (upsert): append a `ChatFlowItemTextBubble(instanceId:'m1', state:'pending')`, then `upsert` a `ChatFlowItemTextBubble(instanceId:'m1', state:'complete')`; assert the list has length 1 and the single item has state 'complete' (no duplicate).
    - Test 2 (replaceAll): `replaceAll` with two items then with one item; assert the list is fully replaced each time.
    - Test 3 (sender label): `buildChatFlowItemTextBubble` on a `ChatMessageState` with `sender = '0x' + 128 hex chars` yields a non-null `senderName` starting with '0x' and containing '…', and a `ChatMessageState` with empty sender yields a null `senderName`.
    - Test 4 (messageHistory dispatch): `handlePushedBytes` with a `GcsEvent` whose `messageHistory` carries two `ChatMessageState`s (built via `..messageHistory = (MessageHistory()..roomTopic='gcs/chat/a'..message.addAll([...]))`) results in `flow.state` having exactly those two items in order.
    - Test 5 (messageHistory role preservation): the batch in Test 4 carries one `ChatMessageState` with role MESSAGE_ROLE_USER_SELF and one with MESSAGE_ROLE_USER_PEER; assert the resulting flow items have `role == 'user_self'` and `role == 'user_peer'` respectively (Dart renders `message.role` directly — C++ owns the flip).
    - Test 6 (composer transport refusal): a `ComposerCubit` wired to a fake transport whose `publishCommand` returns false, seeded with a sendable draft + active room, returns `false` from `send()` and keeps the draft.
    - Test 7 (composer publish success): a `ComposerCubit` wired to a fake transport whose `publishCommand` returns true returns `true` from `send()` and clears the draft.
  </behavior>
  <action>
    Add the seven tests above to `shell_cubits_test.dart`: extend the existing `MessageFlowCubit` group with upsert/replaceAll/sender tests, extend the `handlePushedBytes` dispatch test with the messageHistory event (serialize via `.writeToBuffer()` exactly like the existing variants) and its role-preservation assertion, and add a `ComposerCubit` group driven by the existing fake `GcsCommandTransport` seam (one fake returning false, one returning true). Use `pumpEventQueue` for async propagation; never sleeps.
  </action>
  <acceptance_criteria>
    - `grep -c "upsert" src/app/test/cubits/shell_cubits_test.dart` is >= 1.
    - `grep -c "messageHistory" src/app/test/cubits/shell_cubits_test.dart` is >= 1.
    - `grep -c "senderName" src/app/test/cubits/shell_cubits_test.dart` is >= 1.
    - `grep -c "ComposerCubit" src/app/test/cubits/shell_cubits_test.dart` is >= 1 (transport refusal + success tests).
    - `grep -c "sleep" src/app/test/cubits/shell_cubits_test.dart` is 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd src/app && flutter test test/cubits/shell_cubits_test.dart'</automated>
  </verify>
  <done>flutter test passes for the cubit suite with upsert, replaceAll, sender-label, and messageHistory-dispatch tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pushed GcsEvent bytes -> Dart | Untrusted/remote message bytes decoded by generated protobuf; a malformed or oversized batch must not crash the renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Spoofing | sender label | accept | sender is display-only truncated text (D-04 unsigned MVP); no authorization is gated on it. |
| T-03-06 | DoS | MessageHistory batch -> replaceAll | mitigate | `cappedItems` (kMaxFlowItems) caps the retained list, dropping oldest — unbounded history replay cannot exhaust the client list. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new external packages; protoc_plugin 22.5.0 is the already-pinned dev dependency. |
</threat_model>

<verification>
- `cd src/app && dart analyze --fatal-infos` clean
- `cd src/app && flutter test test/cubits/shell_cubits_test.dart` green
</verification>

<success_criteria>
Dart renders pushed messages with by-id upsert (pending->complete), full-list history replace, and truncated peer sender labels, with green cubit tests and a clean fatal-infos analyze.
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/03-messaging/03-03-SUMMARY.md` when done
</output>
