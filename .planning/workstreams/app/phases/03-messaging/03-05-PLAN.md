---
phase: 03-messaging
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - src/ffi/gcs_core_ffi.cpp
autonomous: true
requirements: [CORE-04]
must_haves:
  truths:
    - "The send_text FFI arm delegates to Messaging::SendMessage (pending -> live publish + archive -> complete)"
    - "join_topic and derived joins replay the room's history as a pushed MessageHistory batch AND arm the raw live GossipSub subscribe"
    - "CRDT-synced and live GossipSub message arrivals funnel into Messaging::OnMessageArrived / OnLiveMessage under g_mutex"
  artifacts:
    - path: src/ffi/gcs_core_ffi.cpp
      provides: "send_text rewrite + join replay + live-subscribe + receive bridge"
      contains: "g_messaging"
  key_links:
    - from: src/ffi/gcs_core_ffi.cpp
      to: src/lib/gcs_messaging.hpp
      via: "g_messaging->SendMessage / OnLiveMessage / OnMessageArrived / QueryHistory"
      pattern: "g_messaging->"
    - from: src/ffi/gcs_core_ffi.cpp
      to: CoreSession::Subscribe (03-02)
      via: "SubscribeLive on join_topic + derived joins -> Messaging::OnLiveMessage"
      pattern: "SubscribeLive"
---

<objective>
Wire the Messaging component into the FFI command dispatcher so Dart's existing send_text/join_topic commands drive the real P2P messaging path and pushed events flow back.

Purpose: D-03/D-04 (sender stamped via GeniusSDKGetAddress, live publish + archive send), D-06 (join-time history replay on join_topic AND derived joins, plus arming the raw GossipSub live subscribe so the node receives live messages), D-07 (pending/complete/error echo). The FFI becomes a thin validate-and-delegate dispatcher over `gcs::Messaging`; no new ABI functions (the four-function ABI is unchanged — payload/schema change only).

Output: rewritten `gcs_core_ffi.cpp` send_text arm, join_topic + RefreshDerivedJoins history replay AND live-subscribe wiring, receive-bridge registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/phases/03-messaging/03-API-SIGNATURES.md
@src/ffi/gcs_core_ffi.cpp
@src/lib/gcs_messaging.hpp
@src/lib/gcs_core.hpp

<interfaces>
<!-- Messaging component contract this plan consumes (from 03-04). -->
```
gcs::Messaging(CoreSession &session, std::string senderAddress, gcs::Messaging::EventSink sink);
outcome::result<void> SendMessage(const std::string &roomTopic, const std::string &text);
void OnLiveMessage(const std::string &topic, const std::string &valueBytes);   // D-03 raw GossipSub live path
void OnMessageArrived(const std::string &key, const std::string &valueBytes);  // D-03 CRDT heal path
outcome::result<chat::MessageHistory> QueryHistory(const std::string &roomTopic);
static constexpr const char *gcs::Messaging::kMessagesKeyPrefix = "gcs/messages/";
```
<!-- CoreSession raw pubsub surface (from 03-02): Publish(topic, data) is used INSIDE
     Messaging::SendMessage; the FFI only needs Subscribe(topic, callback) to arm the live path. -->
```
outcome::result<void> CoreSession::Subscribe(
    const std::string &topic,
    std::function<void(const std::string &topic, const std::string &data)> callback);
```
<!-- GeniusSDK sender (D-04): GeniusAddress GeniusSDKGetAddress(); -> .address (char[131]). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite send_text arm to delegate to Messaging::SendMessage</name>
  <files>src/ffi/gcs_core_ffi.cpp</files>
  <read_first>
    - src/ffi/gcs_core_ffi.cpp (send_text arm lines 667-720, globals lines 84-100, NextMessageId 449-458, PostToDart 294-312, gcs_init 463-580)
    - src/lib/gcs_messaging.hpp (SendMessage/EventSink contract)
  </read_first>
  <action>
    Add `#include "lib/gcs_messaging.hpp"` and a new global `std::unique_ptr<gcs::Messaging> g_messaging;` beside `g_entities` (guarded by `g_mutex`). In `gcs_init`, immediately after `g_session = std::move(session);` construct the messaging component: `g_messaging = std::make_unique<gcs::Messaging>(*g_session, std::string(GeniusSDKGetAddress().address), [](const gcs::chat::GcsEvent &e){ PostToDart(e); });` (the EventSink is PostToDart; callers already hold g_mutex). Reset `g_messaging` in `TeardownSessionAndNode()` alongside `g_entities`.
    Rewrite the `kSendText` arm: keep the four existing validation guards (empty room_topic, `> kMaxTopicLength`, not-in-`g_roomTopics`, text `> kMaxMessageTextLength` — each still `PostErrorNotice` + return the same code). Replace the entire body from `gcs::chat::GcsEvent event;` through the `PostToDart(event)` with a single call: `if (!g_messaging->SendMessage(sendText.room_topic(), sendText.text()).has_value()) { PostErrorNotice("send_text failed for room '" + sendText.room_topic() + "'"); return GCS_ERROR_GENERIC; } return GCS_OK;`. Delete the now-superseded `NextMessageId()` free function and the `kMessageIdPrefix` constant (id minting now lives in gcs_messaging). Do NOT add new ABI symbols.
  </action>
  <acceptance_criteria>
    - `grep -c "g_messaging" src/ffi/gcs_core_ffi.cpp` is >= 4 (global, init, send, receive/replay).
    - `grep -c "g_messaging->SendMessage" src/ffi/gcs_core_ffi.cpp` is 1.
    - `grep -c "GeniusSDKGetAddress" src/ffi/gcs_core_ffi.cpp` is 1.
    - `grep -c "NextMessageId" src/ffi/gcs_core_ffi.cpp` is 0 (superseded).
    - The four send_text validation guards remain (`grep -c "kMaxMessageTextLength" src/ffi/gcs_core_ffi.cpp` is 1).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd build/OSX/Debug && ninja gcs_ffi'</automated>
  </verify>
  <done>gcs_ffi builds; send_text validates then delegates to Messaging::SendMessage with the wallet-address sender and PostToDart sink.</done>
</task>

<task type="auto">
  <name>Task 2: Join-time history replay + live-subscribe + receive-bridge registration</name>
  <files>src/ffi/gcs_core_ffi.cpp</files>
  <read_first>
    - src/ffi/gcs_core_ffi.cpp (join_topic arm 621-666, RefreshDerivedJoins 374-421, gcs_init 463-580, PostToDart 294-312)
    - src/lib/gcs_messaging.hpp (QueryHistory / OnLiveMessage / OnMessageArrived)
    - .planning/workstreams/app/phases/03-messaging/03-API-SIGNATURES.md (RegisterNewElementCallback + Subscribe + the io-thread / strand-thread notes)
  </read_first>
  <action>
    1. Add a helper in the anonymous namespace: `void PushMessageHistory(const std::string &roomTopic)` — callers hold `g_mutex`; it calls `g_messaging->QueryHistory(roomTopic)`, and on success builds a `gcs::chat::GcsEvent` with `mutable_message_history()` set from the returned `chat::MessageHistory`, then `PostToDart(event)`; on error log `spdlog::error` (do not post an error notice for a failed scan). The returned `MessageHistory` already carries the self/peer role flip applied by `Messaging::QueryHistory` (03-04) — post it verbatim, no FFI-side re-mapping.
    2. Add a helper in the anonymous namespace: `void SubscribeLive(const std::string &roomTopic)` — callers hold `g_mutex`. It calls `g_session->Subscribe(roomTopic, [](const std::string &topic, const std::string &data){ std::lock_guard<std::mutex> lock(g_mutex); if (g_messaging) { g_messaging->OnLiveMessage(topic, data); } })`; on failure log `spdlog::error`. The lambda re-acquires `g_mutex` because the raw GossipSub callback fires on the GossipPubSub strand thread (03-API-SIGNATURES.md), NOT the FFI command thread — same discipline as the CRDT bridge.
    3. In the `kJoinTopic` arm, after the existing `PostToDart(BuildRoomListEvent());` add `PushMessageHistory(roomTopic);` then `SubscribeLive(roomTopic);` (D-06: history batch first, THEN arm the live subscribe so any live message lands after the batch and is absorbed by the dedupe set — Pitfall 3).
    4. In `RefreshDerivedJoins()`, inside the loop where a newly derived topic is pushed to `g_roomTopics`, add `PushMessageHistory(topic);` then `SubscribeLive(topic);` after `g_roomTopics.push_back(topic);` so auto-joined rooms also replay history and receive live messages (D-06 derived joins).
    5. Register the CRDT receive bridge in `gcs_init` after `g_messaging` is constructed: `if (!g_session->RegisterNewElementCallback(gcs::Messaging::kMessagesKeyPrefix, [](const std::string &key, const std::string &value){ std::lock_guard<std::mutex> lock(g_mutex); if (g_messaging) { g_messaging->OnMessageArrived(key, value); } }).has_value()) { spdlog::error("gcs_ffi: message receive-callback registration failed"); }`. The lambda takes `g_mutex` because the callback fires on the io thread (03-API-SIGNATURES.md), and PostToDart requires `g_mutex`. Both receive routes (CRDT heal via this callback, live via `SubscribeLive`) funnel into `gcs::Messaging`'s single id-keyed dedupe (03-04).
  </action>
  <acceptance_criteria>
    - `grep -c "PushMessageHistory" src/ffi/gcs_core_ffi.cpp` is >= 3 (helper def + join_topic call + RefreshDerivedJoins call).
    - `grep -c "SubscribeLive" src/ffi/gcs_core_ffi.cpp` is >= 3 (helper def + join_topic call + RefreshDerivedJoins call).
    - `grep -c "g_session->Subscribe" src/ffi/gcs_core_ffi.cpp` is 1.
    - `grep -c "OnLiveMessage" src/ffi/gcs_core_ffi.cpp` is 1.
    - `grep -c "RegisterNewElementCallback" src/ffi/gcs_core_ffi.cpp` is 1.
    - `grep -c "OnMessageArrived" src/ffi/gcs_core_ffi.cpp` is 1.
    - `grep -c "g_mutex" src/ffi/gcs_core_ffi.cpp` is >= 8 (receive lambdas acquire it).
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd build/OSX/Debug && ninja gcs_core gcs_ffi && ctest -R "test_gcs_messaging|test_gcs_ffi" --output-on-failure'</automated>
  </verify>
  <done>gcs_ffi builds; join_topic and derived joins replay MessageHistory and arm the live GossipSub subscribe; CRDT arrivals funnel into OnMessageArrived and live arrivals into OnLiveMessage, both under g_mutex; existing test_gcs_ffi* and test_gcs_messaging remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart -> gcs_publish -> send_text/join_topic | Untrusted command bytes parse into command envelopes; room_topic/text are the hostile inputs |
| CRDT io thread + GossipSub strand -> receive callbacks | Remote message arrivals cross into FFI session state (mutex discipline) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering | send_text/join_topic input | mitigate | Existing guards unchanged: room_topic empty/`>kMaxTopicLength`/not-joined and text `>kMaxMessageTextLength` all reject with INVALID_ARGUMENT before any write. |
| T-03-13 | DoS | oversized publish | mitigate | `kMaxMessageTextLength` (4096) and `kMaxTopicLength` (128) bound the serialized payload. |
| T-03-14 | Elevation of Privilege | receive callback threads | mitigate | BOTH receive lambdas (CRDT io thread + GossipSub strand) acquire `g_mutex` before touching `g_messaging`/`PostToDart` (threading notes in 03-API-SIGNATURES.md). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new external packages. |
</threat_model>

<verification>
- `cd build/OSX/Debug && ninja gcs_core gcs_ffi && ctest -R "test_gcs_messaging|test_gcs_ffi" --output-on-failure` green
</verification>

<success_criteria>
The FFI dispatches send_text through the Messaging component, replays history AND arms the raw live GossipSub subscribe on explicit and derived joins, and bridges both CRDT heal and live arrivals into the deduped receive paths under proper mutex discipline, with existing FFI and messaging tests green.
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/03-messaging/03-05-SUMMARY.md` when done
</output>
