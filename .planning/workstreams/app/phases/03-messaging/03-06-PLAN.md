---
phase: 03-messaging
plan: 06
type: execute
wave: 5
depends_on: [03-05, 03-03]
files_modified:
  - test/test_gcs_messaging_multinode.cpp
  - test/CMakeLists.txt
autonomous: true
requirements: [CORE-04]
must_haves:
  truths:
    - "A two-node in-process integration test proves live cross-node delivery and history convergence through the real stack (CoreSession + Messaging + real GossipSub transport) with the REAL gcs::crypto seam injected and enabled — production D-08 posture (D-01, D-03, D-08)"
    - "The at-rest opacity assertion binary-scans BOTH nodes' GlobalDB directories for a known plaintext marker and requires zero hits while both histories display the message (D-08)"
    - "Tearing down and re-initializing a node on the same db dir replays the full message history decrypted and sorted (D-06 restart replay)"
    - "All waits use the project wait-condition template — no sleep_for anywhere (testing discipline)"
  artifacts:
    - path: test/test_gcs_messaging_multinode.cpp
      provides: "MessagingMultinodeTest fixture (two nodes, explicit AddPeers connect, WaitForCondition mesh readiness) + 3 tests: LiveDeliveryAndHistoryConvergeAcrossNodes, AtRestOpacityBothSides, HistoryReplaysAfterRestart"
      contains: "MessagingMultinodeTest"
    - path: test/CMakeLists.txt
      provides: "test_gcs_messaging_multinode target registration (own binary, like SuperGenius globaldb_integration_test)"
      contains: "test_gcs_messaging_multinode"
  key_links:
    - from: test/test_gcs_messaging_multinode.cpp
      to: src/lib/gcs_messaging.hpp
      via: "two gcs::Messaging instances (nodeA/nodeB) each constructed with the real crypto seam"
      pattern: "gcs::Messaging"
    - from: test/test_gcs_messaging_multinode.cpp
      to: src/lib/gcs_crypto.hpp
      via: "CryptoSeam{ &gcs::crypto::EncryptPayload, &gcs::crypto::DecryptPayload, enabled = true } injected per Messaging instance (D-08 production posture)"
      pattern: "EncryptPayload"
    - from: test/test_gcs_messaging_multinode.cpp
      to: src/lib/gcs_core.hpp
      via: "CoreSession::Initialize(pubsub, graphsyncNetwork) injected seam — NO GeniusSDK, two sessions in one process"
      pattern: "Initialize( "
    - from: test/test_gcs_messaging_multinode.cpp
      to: ../SuperGenius test pattern
      via: "pubsub->AddPeers({ other->GetInterfaceAddress() }) + connection-count WaitForCondition (globaldb_integration.cpp TestNodeCollection)"
      pattern: "AddPeers"
---

<objective>
Automated two-node integration verification of the end-to-end messaging path — live cross-node delivery, history convergence, restart replay, and D-08 at-rest ciphertext opacity — using the established SuperGenius multi-node test pattern, plus an optional non-gating human visual smoke.

Purpose: The unit suites (test_gcs_crypto, test_gcs_messaging, flutter_test) prove single-session logic including both encryption paths; this plan proves the CROSS-node behavior (roadmap SC 1-3 + D-08 live/at-rest/restart) against the real GossipSub transport with two real nodes, exactly as ../SuperGenius test/src/crdt/globaldb_integration.cpp does. The user directed this automation on 2026-09-23 ("there's no reason we can't have a test for this, as we do launch multiple nodes in ../SuperGenius testing"). SC4 visual chrome (pending opacity, sender labels, error toast) remains covered by the flutter tests; the human smoke is optional and non-gating.

Output: test_gcs_messaging_multinode.cpp (own binary) green in ctest; optional smoke result recorded in STATE if performed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/phases/03-messaging/03-API-SIGNATURES.md
@test/test_gcs_messaging.cpp
@test/test_graphsync_network.hpp
@test/test_wait_condition.hpp
@src/lib/gcs_messaging.hpp
@src/lib/gcs_crypto.hpp
@src/lib/gcs_core.hpp
@src/ffi/gcs_core_ffi.cpp

<interfaces>
<!-- Two-node construction is ALREADY proven in-repo — reuse these exact pieces: -->
```
// Per node (test_gcs_messaging.cpp MakeStartedPubSub, but DEFAULT gossip config —
// echo_forward_mode must be FALSE here so delivery is genuinely cross-node):
sgns::crdt::KeyPairFileStorage keyStore(keyDir);
auto pubsub = std::make_shared<sgns::ipfs_pubsub::GossipPubSub>(keyStore.GetKeyPair().value());
pubsub->Start(0, {}, "0.0.0.0", {}).get();            // port 0 = ephemeral

// Per node graphsync (test_graphsync_network.hpp — bundles io+scheduler+network
// with the pubsub-shared asio backend and correct lifetime):
TestGraphsyncContext graphsync = MakeGraphsyncContext(pubsub);

// Per node session (gcs_core.hpp:88 injected seam — NO GeniusSDK):
gcs::CoreSession::Config cfg{};  cfg.m_dbPath = <node tmp dir> + "/db";
gcs::CoreSession session(cfg);
session.Initialize(pubsub, graphsync.network);

// Cross-connect (SuperGenius globaldb_integration.cpp TestNodeCollection::connectNodes):
nodeA.pubsub->AddPeers({ nodeB.pubsub->GetInterfaceAddress() });
// then WaitForCondition on BOTH hosts' getConnectionManager().getConnections().size() >= 1
```
<!-- Receive wiring mirrors the FFI join path (src/ffi/gcs_core_ffi.cpp kJoinTopic arm):
     SubscribeLive(room) then RegisterNewElementCallback(gcs::Messaging::kMessagesKeyPrefix, ...).
     If CRDT heal additionally requires room listen/broadcast topic registration, mirror exactly
     what the FFI/phase-2 join does — read the kJoinTopic arm, do not invent new wiring. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-node multinode integration test (live delivery, convergence, at-rest opacity, restart replay)</name>
  <files>test/test_gcs_messaging_multinode.cpp, test/CMakeLists.txt</files>
  <read_first>
    - /Users/Shared/SSDevelopment/Development/GeniusVentures/GeniusNetwork/SuperGenius/test/src/crdt/globaldb_integration.cpp (TestNodeCollection + connectNodes — the pattern the user pointed at; note AddPeers + connection-count wait)
    - test/test_gcs_messaging.cpp (MakeStartedPubSub, MakeGraphsyncContext usage, WaitForCondition, Config/dbPath discipline)
    - test/test_graphsync_network.hpp (TestGraphsyncContext lifetime bundle)
    - test/test_wait_condition.hpp (the project wait template — no sleep_for)
    - src/lib/gcs_messaging.hpp (Messaging ctor, CryptoSeam, SendMessage/OnLiveMessage/OnMessageArrived/QueryHistory, kMessagesKeyPrefix)
    - src/lib/gcs_crypto.hpp (EncryptPayload/DecryptPayload — the real seam callables)
    - src/ffi/gcs_core_ffi.cpp (kJoinTopic arm: SubscribeLive + receive-bridge wiring to mirror)
    - test/CMakeLists.txt (existing target registration pattern)
  </read_first>
  <action>
    Create `test/test_gcs_messaging_multinode.cpp` with fixture `MessagingMultinodeTest` and three tests, and register the `test_gcs_messaging_multinode` target (own binary, mirroring the existing test target pattern; link gcs_core + gcs_test util headers).

    Fixture (per test): two nodes A and B, each = own tmp subdir (key + db), DEFAULT gossip config GossipPubSub on port 0 (echo_forward_mode FALSE — cross-node delivery must be real), MakeGraphsyncContext(pubsub), CoreSession with cfg.m_dbPath = nodeTmp + "/db", Initialize(pubsub, graphsync.network). Then per node construct `gcs::Messaging` with sender addresses "0xAAAA..."(A)/"0xBBBB..."(B) (short distinct constants are fine — sender is display-only per D-04), an event-sink lambda capturing into a per-node std::vector (guarded by the sink's own mutex — sinks fire on GossipSub/DagWorker threads), and the REAL production seam: CryptoSeam with encrypt=&gcs::crypto::EncryptPayload, decrypt=&gcs::crypto::DecryptPayload, enabled=true (member assignment, not aggregate init — CryptoSeam has a user-provided default ctor). Cross-connect A.AddPeers(B.GetInterfaceAddress()) and WaitForCondition BOTH hosts' connection-manager size >= 1. Mirror the FFI join wiring per node: session.Subscribe(room, -> Messaging::OnLiveMessage) and session.RegisterNewElementCallback(gcs::Messaging::kMessagesKeyPrefix, -> Messaging::OnMessageArrived) — plus, if the FFI join arm registers the room as a CRDT listen/broadcast topic, the same registration here (read the arm; mirror, don't invent).

    1. `LiveDeliveryAndHistoryConvergeAcrossNodes` — A.SendMessage(room, text with a unique marker). WaitForCondition: B's captured sink contains a ChatMessageState whose text == the marker text (live path, no refresh analog). Then assert both QueryHistory(room) contain the message; assert the two histories are equal in size and per-index (id, timestamp order, text). Reverse: B sends, A receives. This is SC1-SC3 through the real transport.
    2. `AtRestOpacityBothSides` — same send flow with a distinct marker; after BOTH histories display it (QueryHistory confirms), binary-scan every regular file under BOTH nodes' db directories (std::filesystem::directory_iterator recursive + ifstream binary read into a string, search for the marker bytes — portable, no grep spawn). EXPECT zero occurrences on both sides (D-08: disk carries nonce||ciphertext||tag only). Scope is the message text marker only — the room topic string is a public routing key by design (D-02), not a secret.
    3. `HistoryReplaysAfterRestart` — after test-1-style exchange of >= 2 messages (one per direction), TearDown path: Shutdown() node A's session, construct a NEW CoreSession+Messaging on the SAME db dir (new pubsub optional — reuse B's wiring), QueryHistory(room) returns all messages decrypted (texts match), sorted oldest->newest (D-06 restart replay; also proves the archived envelopes decrypt with the topic-derived key across process-lifetime boundaries).

    All waits via WaitForCondition with a named constexpr timeout (mirror test_gcs_messaging.cpp's kWaitTimeout discipline). C++17 only, Allman braces, Doxygen headers, constexpr kCamelCase constants, unsigned ordinals.
  </action>
  <acceptance_criteria>
    - `ctest -R test_gcs_messaging_multinode --output-on-failure` green (all 3 tests).
    - `grep -c "sleep_for" test/test_gcs_messaging_multinode.cpp` is 0.
    - `grep -c "EncryptPayload" test/test_gcs_messaging_multinode.cpp` is >= 1 (real seam injected).
    - `grep -c "AddPeers" test/test_gcs_messaging_multinode.cpp` is >= 1 (explicit cross-node connect).
    - `grep -c "echo_forward_mode" test/test_gcs_messaging_multinode.cpp` is 0 (default config; if the default-ctor path is taken a comment must state echo is off — then the grep becomes a code-review check, document in SUMMARY).
    - `grep -c "Initialize( " test/test_gcs_messaging_multinode.cpp` is >= 2 (both nodes use the injected seam, no GeniusSDK).
    - Existing suites stay green: `ctest -R "test_gcs_messaging|test_gcs_crypto"` and `ctest -R test_gcs_ffi`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd build/OSX/Debug && ninja test_gcs_messaging_multinode && ctest -R "test_gcs_messaging_multinode|test_gcs_messaging$" --output-on-failure'</automated>
  </verify>
  <done>All three multinode tests green: live cross-node delivery + convergence both directions, zero plaintext hits in both db dirs, restart replay decrypted and sorted; no sleep_for; no regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="optional">
  <name>Task 2: Optional human visual smoke (non-gating)</name>
  <files></files>
  <action>
    Optionally launch two Flutter app instances (macOS) each with its own TMPDIR (the app derives dbPath from Directory.systemTemp, so `TMPDIR=/tmp/gcsA .../flutter_app.app/Contents/MacOS/flutter_app &` and likewise /tmp/gcsB) and eyeball: pending 40% opacity -> solid, peer bubbles leading with truncated 0x sender label, self trailing with none, identical history order after restart, "Send failed" toast on a disconnected send. Automated coverage already exists (flutter_test 26/26 + Task 1); this smoke is UX polish confirmation only and does NOT gate the phase.
  </action>
  <acceptance_criteria>
    - If performed: result ("smoke passed" or notes) recorded in 03-06-SUMMARY.md. If skipped: SUMMARY states skipped-non-gating with the automated coverage pointer.
  </acceptance_criteria>
  <verify>
    <human-check>Optional — user's discretion.</human-check>
  </verify>
  <done>SUMMARY records performed-and-passed, or skipped-non-gating with rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loopback libp2p transport | Two real nodes exchange gossip/CRDT traffic on localhost; test asserts on delivered content |
| local disk (rocksdb files) | At-rest opacity verified by binary scan of both nodes' live db directories |
| human operator (Task 2) | Visual verification only; no code changes; non-gating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Information Disclosure | at-rest opacity assertion | mitigate | Binary scan asserts ZERO marker occurrences across ALL regular files under both db dirs (not just .sst) — level-db WAL/log files included via directory_iterator recursion. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs. |
</threat_model>

<verification>
- `cd build/OSX/Debug && ninja test_gcs_messaging_multinode && ctest -R "test_gcs_messaging_multinode" --output-on-failure` green
- Existing suites unregressed: test_gcs_messaging, test_gcs_crypto, test_gcs_ffi*
</verification>

<success_criteria>
Cross-node live delivery, converged identical histories (both directions), zero-plaintext at-rest scan on both nodes, and restart replay of decrypted history — all proven by an automated two-node integration test in the SuperGenius multinode tradition; optional human smoke recorded or explicitly skipped as non-gating.
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/03-messaging/03-06-SUMMARY.md` when done
</output>
