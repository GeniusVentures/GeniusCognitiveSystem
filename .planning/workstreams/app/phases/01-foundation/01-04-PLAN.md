---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - test/CMakeLists.txt
  - test/test_wait_condition.hpp
  - test/test_gcs_core_smoke.cpp
  - test/test_gcs_ffi.cpp
autonomous: true
requirements: [CORE-05]
must_haves:
  truths:
    - "test/test_gcs_core_smoke.cpp proves CORE-05 substrate: gcs::CoreSession initializes via injected GossipPubSub (port 0), AddBroadcastTopic + AddListenTopic succeed on 'gcs/chat/smoke-test', and Put→Get round-trips"
    - "test/test_gcs_ffi.cpp proves FFI surface under OPTION C semantics: gcs_init(configBytes, configLength) parses a serialized gcs.chat.GcsConfig (D-29 codec-tagged bytes) and returns nullptr gracefully — no crash, no exception — on garbage bytes, unsupported codec, or GeniusSDK-node unavailability; argument-validation / shutdown-safe-on-null tests do NOT require a live node"
    - "The moved test/test_gcs_global_db.cpp is registered in test/CMakeLists.txt and links gcs_storage (not neoswarm_storage)"
    - "All tests use WaitForCondition (condition_variable polling) — zero sleep_for / sleep_until anywhere in test/"
    - "ctest --test-dir build/OSX/Debug -R gcs --output-on-failure is green"
  artifacts:
    - path: "test/CMakeLists.txt"
      provides: "GTest discovery + gcs_test macro + registration of test_gcs_global_db, test_gcs_core_smoke, test_gcs_ffi"
      contains: "gcs_test(test_gcs_core_smoke"
    - path: "test/test_wait_condition.hpp"
      provides: "WaitForCondition template (copied verbatim from the moved test_gcs_global_db.cpp)"
      contains: "WaitForCondition"
    - path: "test/test_gcs_core_smoke.cpp"
      provides: "CORE-05 lifecycle + topic + Put/Get round-trip test"
      contains: "gcs/chat/smoke-test"
    - path: "test/test_gcs_ffi.cpp"
      provides: "FFI init/echo/shutdown smoke test"
      contains: "gcs_init"
  key_links:
    - from: "test/test_gcs_core_smoke.cpp"
      to: "gcs_core"
      via: "target_link_libraries via gcs_test macro"
      pattern: "gcs_core;gcs_storage"
    - from: "test/test_gcs_ffi.cpp"
      to: "gcs_ffi"
      via: "target_link_libraries via gcs_test macro"
      pattern: "gcs_ffi"
    - from: "test/CMakeLists.txt"
      to: "test/test_gcs_global_db.cpp"
      via: "gcs_test(test_gcs_global_db ...) registration"
      pattern: "gcs_test\\(test_gcs_global_db"
---

<objective>
Stand up the GCS-root `test/` tree with smoke-test binaries that prove Phase 1's success
criteria 2, 3, and 4 from ROADMAP.md (GlobalDB init + pub/sub round-trip; FFI init + echo;
GossipSub topic create/join). Registers the moved `test_gcs_global_db.cpp` (from 01-01) alongside
two new tests.

Purpose: These are the objective, CI-runnable proofs that the Phase 1 wiring works. Without them
the phase has no verification story and `/gsd:verify-work` cannot pass.

Output: `test/` directory with 4 files + registration of the moved test; `ctest -R gcs` green
locally on OSX; CI runs these in Plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/STATE.md
@.planning/workstreams/app/phases/01-foundation/01-CONTEXT.md
@.planning/workstreams/app/phases/01-foundation/01-RESEARCH.md
@.planning/workstreams/app/phases/01-foundation/01-PATTERNS.md

@GNUS-NEO-SWARM/test/CMakeLists.txt
@test/test_gcs_global_db.cpp
@src/lib/gcs_core.hpp
@src/ffi/gcs_core.h

<interfaces>
<!-- The test seam on CoreSession (from Plan 02). Smoke tests use the injected-pubsub overload only. -->
From src/lib/gcs_core.hpp:
```cpp
outcome::result<void> Initialize(std::shared_ptr<sgns::ipfs_pubsub::GossipPubSub> pubsub);
```

The root test/ add_subdirectory hook: cmake/CommonBuildParameters.cmake already does
  add_subdirectory(${PROJECT_ROOT}/test ...)
when BUILD_TESTING is on and test/ is a directory (IS_DIRECTORY guard). 01-01 created test/ with
test_gcs_global_db.cpp; this plan adds test/CMakeLists.txt so the hook actually configures.

The GossipPubSub bring-up recipe (from the moved test/test_gcs_global_db.cpp, lines ~140-158):
  - KeyPairFileStorage keyStore(keyDir) → GetKeyPair()
  - make_shared<GossipPubSub>(keyPair)
  - pubsub->Start(0, {}, "0.0.0.0", {}).get()  // port 0 = ephemeral

The mandatory soralog one-time setup (same file). Omitting it crashes GossipPubSub construction
with "Logging system is not ready" (RESEARCH Pitfall 2).

The moved test/test_gcs_global_db.cpp exercises sgns::neoswarm::storage::GcsGlobalDb DIRECTLY —
it is registered here and links gcs_storage (not neoswarm_storage).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/CMakeLists.txt + test/test_wait_condition.hpp (test scaffolding + register moved test)</name>
  <files>test/CMakeLists.txt, test/test_wait_condition.hpp</files>
  <read_first>
    - GNUS-NEO-SWARM/test/CMakeLists.txt (the GTest discovery + helper-macro pattern)
    - test/test_gcs_global_db.cpp (the moved test — its WaitForCondition implementation to extract; also confirms the link libs it needs)
    - .planning/workstreams/app/phases/01-foundation/01-PATTERNS.md §§ "test/CMakeLists.txt" and "test/test_wait_condition.hpp"
  </read_first>
  <action>
    Create two files.

    FILE 1: test/CMakeLists.txt
      - Copy the GTest-discovery block from the neoswarm analog verbatim (find_package(GTest QUIET)
        with THIRDPARTY_BUILD_DIR fallback via find_path/find_library; creates GTest::GTest and
        GTest::Main imported targets; WARNING + return() if not found).
      - Rename the helper macro from `neoswarm_test` to `gcs_test`.
      - Drop the SUPERGENIUS_TEST_DATA_DIR define.
      - Call enable_testing() before the macro definition.
      - At the bottom, register THREE tests:
          gcs_test(test_gcs_global_db   test_gcs_global_db.cpp   "gcs_storage;neoswarm_common;sgns::crdt_globaldb")
          gcs_test(test_gcs_core_smoke  test_gcs_core_smoke.cpp  "gcs_core;gcs_storage;neoswarm_common")
          gcs_test(test_gcs_ffi         test_gcs_ffi.cpp         "gcs_ffi;gcs_proto")
        Note: link gcs_storage (NOT neoswarm_storage). gcs_storage PUBLIC-links neoswarm_common +
        crdt_globaldb + GeniusSDK_shared, so test_gcs_global_db resolves common/error symbols.
        test_gcs_ffi ALSO links gcs_proto so it can serialize a gcs::chat::GcsConfig via
        gcs_chat.pb.h for the config-bytes gcs_init signature (D-29).

    FILE 2: test/test_wait_condition.hpp
      - Copy the WaitForCondition implementation from test/test_gcs_global_db.cpp verbatim into a
        standalone header (Doxygen header, guard GCS_TEST_WAIT_CONDITION_HPP, namespace gcs::test,
        kWaitTimeout=25s, kPollInterval=10ms, inline WaitForCondition). Do not modify the logic.

    Defer build verification to Task 2/3 (the smoke .cpp files do not exist yet).
  </action>
  <verify>
    <automated>test -f test/CMakeLists.txt && test -f test/test_wait_condition.hpp && grep -q "gcs_test(test_gcs_core_smoke" test/CMakeLists.txt && grep -q "gcs_test(test_gcs_global_db" test/CMakeLists.txt && grep -q "WaitForCondition" test/test_wait_condition.hpp && ! grep -q "neoswarm_storage" test/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    - test/CMakeLists.txt exists with gcs_test macro and THREE test registrations (incl. test_gcs_global_db)
    - test/test_wait_condition.hpp exists with kWaitTimeout/kPollInterval and inline WaitForCondition
    - No mention of neoswarm_test, neoswarm_storage, or SUPERGENIUS_TEST_DATA_DIR
  </acceptance_criteria>
  <done>Test scaffolding in place; moved test registered; ready for the actual test bodies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test/test_gcs_core_smoke.cpp (CORE-05 substrate proof)</name>
  <files>test/test_gcs_core_smoke.cpp</files>
  <read_first>
    - test/test_gcs_global_db.cpp (FULL — the structural template: soralog YAML block, KeyPairFileStorage, WaitForCondition usage)
    - test/test_wait_condition.hpp (from Task 1)
    - src/lib/gcs_core.hpp (CoreSession interface from Plan 02)
    - .planning/workstreams/app/phases/01-foundation/01-PATTERNS.md § "test/test_gcs_core_smoke.cpp"
  </read_first>
  <action>
    Create test/test_gcs_core_smoke.cpp. Mirror the moved test's structure but exercise
    gcs::CoreSession (not GcsGlobalDb directly).

    File structure:
      1. Doxygen header
      2. Includes (order): "lib/gcs_core.hpp", "test_wait_condition.hpp", standard headers, gtest,
         "crdt/globaldb/keypair_file_storage.hpp", "ipfs_pubsub/gossip_pubsub.hpp", libp2p log
         configurators, soralog YAML configurator
      3. Anonymous namespace constants: kListenIp = "0.0.0.0", kLoggingYaml (minimal console sink,
         error level, group "gcs_core_smoke_test" with libp2p+Gossip children), kSmokeTopic = "gcs/chat/smoke-test"
      4. GcsCoreSmokeTest fixture (::testing::Test):
         - static SetUpTestSuite() — the soralog YAML block (MANDATORY, verbatim from the moved test)
         - SetUp() — unique temp dir (steady_clock salt); TearDown() — remove via error_code overload
         - MakeStartedPubSub(keyDir) → shared_ptr<GossipPubSub> on port 0
      5. Two TEST_F cases:
         TEST_F(GcsCoreSmokeTest, LifecycleWithInjectedPubSub)
           - pubsub = MakeStartedPubSub(...); cfg.m_dbPath = m_tempPath + "/db"
           - CoreSession session(cfg); ASSERT Initialize(pubsub).has_value(); EXPECT IsRunning()==true
           - WaitForCondition(dbPath exists); session.Shutdown(); EXPECT IsRunning()==false; pubsub->Stop()
         TEST_F(GcsCoreSmokeTest, CrdtPutGetRoundTripsOnGcsChatTopic)
           - Same setup; ASSERT AddBroadcastTopic(kSmokeTopic).has_value(); ASSERT AddListenTopic(kSmokeTopic).has_value()
           - ASSERT session.Put("smoke-key", "smoke-value").has_value(); auto r = session.Get("smoke-key");
             ASSERT r.has_value(); EXPECT_EQ(r.value(), "smoke-value"); Shutdown + Stop

    Strict rules: NO std::this_thread::sleep_for / sleep_until — use gcs::test::WaitForCondition only.
    Allman braces, space inside parens, m_ prefix, kCamelCase, 120-char max, C++17.
  </action>
  <verify>
    <automated>grep -c "sleep_for\|sleep_until" test/test_gcs_core_smoke.cpp | grep -q "^0$" && cmake --build build/OSX/Debug -j 2>&1 | tail -10 | grep -E "error|FAILED" ; ctest --test-dir build/OSX/Debug -R test_gcs_core_smoke --output-on-failure 2>&1 | tail -15 | grep -E "Passed|Failed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "sleep_for\|sleep_until" returns 0
    - Build succeeds; ctest -R test_gcs_core_smoke reports 2/2 passed
    - Both tests use WaitForCondition at least once; file contains kSmokeTopic = "gcs/chat/smoke-test"
  </acceptance_criteria>
  <done>CORE-05 substrate smoke test green on OSX Debug.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create test/test_gcs_ffi.cpp (FFI config-bytes init / arg validation / shutdown)</name>
  <files>test/test_gcs_ffi.cpp</files>
  <read_first>
    - GNUS-NEO-SWARM/test/ffi/test_genius_elm_ffi.cpp (full file — the FFI test template)
    - src/ffi/gcs_core.h (the C API from Plan 03 — four functions, codec-tagged bytes per D-27/D-29)
    - src/proto/gcs_chat.proto (GcsConfig + Codec the init bytes must carry)
    - .planning/workstreams/app/phases/01-foundation/01-PATTERNS.md § "test/test_gcs_ffi.cpp"
      (NOTE: PATTERNS predates D-29 — this plan's test list is authoritative where they differ)
  </read_first>
  <action>
    Create test/test_gcs_ffi.cpp. Exercises the gcs_ffi C ABI exactly as Dart will.

    Structure:
      1. Doxygen header
      2. Includes: "ffi/gcs_core.h", "gcs_chat.pb.h" (serialize GcsConfig — gcs_proto linked via
         the gcs_test macro), <string>, gtest
      3. GcsFFI fixture with TearDown that calls gcs_shutdown(m_handle) if non-null (avoids pthread
         abort at exit — GcsGlobalDb's destructor touches asio::io_context internals)
      4. Members/helpers:
         GcsSession* m_handle = nullptr;
         MakeConfigBytes(dbPath, codec) — builds a gcs::chat::GcsConfig, SerializeAsString(), and
           returns the std::string (caller passes .data()/.size() as the codec-tagged bytes);
         anonymous-namespace constant kGarbageConfig = "\xff\xff\xff\xff" (parse-failure input);
         kCommandTopic = "gcs/command", kEventTopic = "gcs/event"

    Tests (OPTION C locked at planning time: with a valid PROTOBUF GcsConfig, gcs_init's Initialize
    calls GeniusSDKGetNode() which returns nullptr in the test env → gcs_init returns nullptr
    gracefully):
      TEST_F(GcsFFI, InitWithValidConfigWithoutNodeReturnsNullptrGracefully) —
        bytes = MakeConfigBytes(m_tempPath + "/db", gcs::chat::CODEC_PROTOBUF);
        m_handle = gcs_init(reinterpret_cast<const uint8_t*>(bytes.data()), bytes.size());
        EXPECT_EQ(m_handle, nullptr)
      TEST_F(GcsFFI, InitRejectsNullConfigBytes) — gcs_init(nullptr, 8) → nullptr AND
        non-null pointer with length 0 → nullptr
      TEST_F(GcsFFI, InitRejectsGarbageConfigBytes) — kGarbageConfig (4 bytes) → nullptr
      TEST_F(GcsFFI, InitRejectsUnsupportedCodec) — MakeConfigBytes(path, gcs::chat::CODEC_JSON)
        parses but violates the per-store binding (D-29: GCS Phase 1 is PROTOBUF only) → nullptr
      TEST_F(GcsFFI, RepeatedInitCallsWithoutNodeAllReturnNullptr) — three valid-config calls,
        all nullptr
      TEST_F(GcsFFI, ShutdownNullptrIsSafeNoOp) — gcs_shutdown(nullptr); SUCCEED()
      TEST_F(GcsFFI, PublishValidatesArgumentsWithoutNode) —
        EXPECT_EQ(gcs_publish(nullptr, kCommandTopic, bytes.data(), bytes.size()), GCS_ERROR_INVALID_ARGUMENT);
        EXPECT_EQ(gcs_publish(nullptr, nullptr, bytes.data(), bytes.size()), GCS_ERROR_INVALID_ARGUMENT);
        EXPECT_EQ(gcs_publish(nullptr, kCommandTopic, nullptr, 0), GCS_ERROR_INVALID_ARGUMENT)
      TEST_F(GcsFFI, SubscribeValidatesArgumentsWithoutNode) —
        EXPECT_EQ(gcs_subscribe(nullptr, kEventTopic, 1234), GCS_ERROR_INVALID_ARGUMENT);
        EXPECT_EQ(gcs_subscribe(nullptr, nullptr, 1234), GCS_ERROR_INVALID_ARGUMENT);
        EXPECT_EQ(gcs_subscribe(nullptr, kEventTopic, 0), GCS_ERROR_INVALID_ARGUMENT)

    REMOVED from this plan (owned by Plan 05): any gcs_subscribe live-port round-trip test; any
    EXPECT_NE(m_handle, nullptr) init-success assertion. Full init-success + subscribe + command
    publish proof is Plan 05's Dart spike.

    Top-of-file Doxygen states the option-C contract and Plan 05 ownership of the live-port tests.

    Style: same as Task 2 (no sleep_for, Allman, m_ prefix, kCamelCase).
  </action>
  <verify>
    <automated>grep -c "sleep_for\|sleep_until" test/test_gcs_ffi.cpp | grep -q "^0$" && cmake --build build/OSX/Debug -j 2>&1 | tail -5 | grep -E "error|FAILED" ; ctest --test-dir build/OSX/Debug -R test_gcs_ffi --output-on-failure 2>&1 | tail -15 | grep -E "Passed|Failed|tests passed"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 tests pass (gtest)
    - No test references gcs_on_message / gcs_join_topic / gcs_string_free (symbols no longer exist — grep confirms zero hits)
    - No test asserts EXPECT_NE on gcs_init return (option C: nullptr expected)
    - Init tests cover the three failure classes: null args, garbage bytes, unsupported codec
    - TearDown calls gcs_shutdown conditionally (safe on null); no sleep_for/until
    - Top-of-file Doxygen documents option-C + Plan 05 ownership
  </acceptance_criteria>
  <done>FFI surface smoke-tested; ABI proven loadable and callable from C++ test code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test binary → gcs_core/gcs_ffi | Test code exercises the FFI handle + CRDT seams with synthetic/null inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | gcs_init null-handle path | accept | Tests assert graceful null (no crash) — this is a test of the FFI mitigation, not new attack surface. |
| T-01-04-02 | Denial of Service | WaitForCondition timeouts | mitigate | kWaitTimeout=25s bounds every async wait; no unbounded sleeps. |
| T-01-04-03 | Tampering | test temp dirs | mitigate | Unique per-test temp dirs (steady_clock salt) + TearDown removal — no cross-test file clobber. |
| T-01-04-SC | Tampering | package installs | accept | No npm/pip/cargo installs. |
</threat_model>

<verification>
- `ctest --test-dir build/OSX/Debug -R gcs --output-on-failure` — all GCS tests pass
- `grep -r "sleep_for\|sleep_until" test/` returns zero hits
- `ls build/OSX/Debug/gcs_src/test/` shows the test binaries
</verification>

<success_criteria>
- ROADMAP success criterion 2 (GlobalDB CRDT init + pub/sub round-trip): green
- ROADMAP success criterion 4 (GossipSub topic create/join): green
- FFI boundary proven null-safe and argument-validated under option C (no live GeniusSDK node)
- Moved test_gcs_global_db registered and passing against gcs_storage
- test/ tree is ready for CI in Plan 06
- ROADMAP success criterion 3 (Flutter↔C++ FFI round-trip) is NOT claimed here — Plan 05 owns it
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>
