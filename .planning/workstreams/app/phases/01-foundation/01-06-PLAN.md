---
phase: 01-foundation
plan: 06
type: execute
wave: 5
depends_on: ["01-03", "01-04"]
files_modified:
  - .github/workflows/cmake.yml
autonomous: false
requirements: [CORE-05]
user_setup:
  - service: github_selfhosted_runners
    why: "Workflow picks self-hosted runners via GNUS_TOKEN_1 (org+repo runner merge in resolve-runners job)"
    env_vars:
      - name: GNUS_TOKEN_1
        source: "GitHub repo Settings -> Secrets and variables -> Actions (must already exist; copied from SuperGenius/GeniusSDK convention)"
    dashboard_config:
      - task: "Confirm self-hosted runners with labels sg-ubuntu-linux, sg-arm-linux, SG-WIN11, gv-OSX-Large are registered to GeniusVentures/GeniusCogntiveSystem (or org)"
        location: "GitHub repo Settings -> Actions -> Runners"
must_haves:
  truths:
    - "D-06: .github/workflows/cmake.yml exists at GCS repo root, copies the SuperGenius resolve-runners + matrix pattern, and triggers on push to develop+main, PRs to develop+main, and workflow_dispatch"
    - "D-07: Workflow downloads three prebuilt dependency releases (thirdparty, SuperGenius, GeniusSDK) — never builds them from source"
    - "Matrix covers all 5 platforms (Android arm64-v8a+armeabi-v7a, iOS, OSX, Linux x86_64+aarch64, Windows) × Debug/Release"
    - "D-08: CI builds and ctests the C++ core only (no Flutter app build this phase); ctest runs on Linux, OSX, Windows Release; NOT on Android/iOS (cross-compiles)"
    - "zkLLVM has no explicit CI step (D-09) — cmaketemplate's auto-download handles headers-only consumption"
  artifacts:
    - path: ".github/workflows/cmake.yml"
      provides: "Complete CI workflow (resolve-runners + build/test/release matrix)"
      contains: "resolve-runners"
  key_links:
    - from: ".github/workflows/cmake.yml"
      to: "GeniusVentures/thirdparty + GeniusVentures/SuperGenius + GeniusVentures/GeniusSDK releases"
      via: "gh release download with tag <Target>[-<ABI>]-<branch>-<BuildType>"
      pattern: "gh release download"
---

<objective>
Author `.github/workflows/cmake.yml` — the CI workflow that proves Phase 1 success criterion 5:
"C++ core builds and tests on self-hosted runners for macOS, Linux, Windows, iOS, Android."

Purpose: A near-verbatim copy of `../GeniusSDK/.github/workflows/cmake.yml` (consumer-repo template)
with four deltas:
  1. Add a GeniusSDK release download step (GCS consumes GeniusSDK prebuilt).
  2. Delete the zkLLVM download step (D-09).
  3. Change working-directory to ${{github.workspace}} (GCS checkout IS the workspace root).
  4. Add ctest steps copied from `../SuperGenius/.github/workflows/cmake.yml`.

Note: the C++ core now includes root src/lib/gcs_storage (D-25) + src/lib/gcs_core + src/ffi; the
GNUS-NEO-SWARM submodule is still checked out because gcs_storage links neoswarm_common.

Output: A single workflow file. The first CI run validates release freshness (esp. `GeniusSDKGetNode`
symbol in the GeniusSDK develop release — RESEARCH Assumption A3 + Pitfall 1).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/STATE.md
@.planning/workstreams/app/phases/01-foundation/01-CONTEXT.md
@.planning/workstreams/app/phases/01-foundation/01-RESEARCH.md
@.planning/workstreams/app/phases/01-foundation/01-PATTERNS.md

@../GeniusSDK/.github/workflows/cmake.yml
@../SuperGenius/.github/workflows/cmake.yml

<interfaces>
<!-- Release tag naming (verified via gh release list): -->
Tag format: <Target>[-<ABI>]-<branch>-<BuildType>
  - ABI segment only for Android (arm64-v8a, armeabi-v7a) and Linux (x86_64, aarch64)
  - Examples: OSX-develop-Debug, Linux-x86_64-develop-Release, Android-arm64-v8a-develop-Debug
Asset file name: <Target>[-<ABI>]-<BuildType>.tar.gz

Runner labels (D-06):
  self-hosted: sg-ubuntu-linux (Linux x86_64 + Android), sg-arm-linux (Linux aarch64),
               SG-WIN11 (Windows), gv-OSX-Large (OSX + iOS)
  fallback:    ubuntu-latest, ubuntu-24.04-arm, windows-2022, macos-latest
Container: ghcr.io/geniusventures/debian-bullseye:latest on Linux/Android

CI must pass -DTHIRDPARTY_BUILD_DIR explicitly (Pitfall 3): the extracted tarball has no .git, so
get_third_party_dir's remote check would fail without it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .github/workflows/cmake.yml (GeniusSDK template + 4 deltas)</name>
  <files>.github/workflows/cmake.yml</files>
  <read_first>
    - ../GeniusSDK/.github/workflows/cmake.yml (FULL file — the template)
    - ../SuperGenius/.github/workflows/cmake.yml lines 700-740 (the ctest steps to copy)
    - .planning/workstreams/app/phases/01-foundation/01-PATTERNS.md § ".github/workflows/cmake.yml"
    - .planning/workstreams/app/phases/01-foundation/01-RESEARCH.md § "CI/CD (D-06..D-09)"
  </read_first>
  <action>
    Create .github/workflows/cmake.yml. Structure (sections in order):
    1. Trigger + permissions (copy GeniusSDK lines 1-26): push/PR to develop+main, workflow_dispatch.
    2. resolve-runners job (copy GeniusSDK lines 28-125 verbatim; secrets.GNUS_TOKEN_1; org+repo runner merge).
    3. build job matrix (copy GeniusSDK lines 138-191): 5 targets × Debug/Release, ABI includes, container.
    4. Self-hosted cleanup (copy GeniusSDK lines 214-267; keep the zkLLVM cleanup line — stale dirs may exist).
    5. Checkout GCS (uses actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd; submodules: "recursive";
       NO path: override). Comment noting GCS submodules: GNUS-NEO-SWARM, build (cmaketemplate), gendoc-template, src/app/scaffold.
    6. Download thirdparty release (copy GeniusSDK lines 286-353). Sets THIRDPARTY_BUILD_DIR.
    7. DELETE the zkLLVM download step (GeniusSDK lines 355-409) per D-09; add a comment.
    8. Download SuperGenius release (copy GeniusSDK lines 411-450). Sets SUPERGENIUS_DIR.
    9. NEW STEP — Download GeniusSDK release (insert after SuperGenius; tag/asset pattern per PATTERNS.md;
       sets GENIUS_SDK_DIR; $(pwd -W) on Windows).
    10. Per-platform host setup (copy GeniusSDK lines 452-502; drop rustup lines — GCS has no Rust).
    11. CMake configure (working-directory ${{github.workspace}}; drop -DZKLLVM_BUILD_DIR; keep
        -DTHIRDPARTY_BUILD_DIR; -DBUILD_TESTS=ON; per-platform flags per RESEARCH; iOS uses build/iOS auto-toolchain — no explicit toolchain flag).
    12. Build step (working-directory ${{github.workspace}}/${{env.BUILD_DIRECTORY}}).
    13. Install step (copy GeniusSDK lines 534-536).
    14. ctest steps (from SuperGenius lines 703-740): Linux dbus-run-session wrapper; OSX plain ctest;
        Windows Release ctest; NO ctest on Android/iOS.
    15. Release upload (copy GeniusSDK lines 538-593; replace repo refs with GeniusCogntiveSystem;
        package libgcs_ffi + libgcs_core + headers).

    Style: YAML, 2-space indent, match template quoting.
  </action>
  <verify>
    <automated>test -f .github/workflows/cmake.yml && grep -q "resolve-runners" .github/workflows/cmake.yml && grep -q "GeniusVentures/GeniusSDK" .github/workflows/cmake.yml && ! grep -q "zkLLVM release\|Download zkLLVM" .github/workflows/cmake.yml && grep -cE "ctest" .github/workflows/cmake.yml | grep -qE "^[3-9]|^[1-9][0-9]" && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/cmake.yml'))" && echo "yaml-valid"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .github/workflows/cmake.yml; YAML parses cleanly
    - resolve-runners job present; GeniusSDK download step present
    - No zkLLVM download step (comment allowed; step gone)
    - At least 3 ctest invocations (Linux, OSX, Windows Release); none under Android/iOS
    - working-directory is ${{github.workspace}} or ${{github.workspace}}/${{env.BUILD_DIRECTORY}} — never /GeniusSDK
    - Trigger includes push + PR to develop+main, workflow_dispatch
  </acceptance_criteria>
  <done>Workflow file authored and YAML-valid; ready for push + first CI run.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Push to develop and observe first CI run</name>
  <files>.github/workflows/cmake.yml</files>
  <action>
    Prepare the push: confirm Wave 1-3 plans (01-01..01-05) are merged to develop locally; confirm
    src/lib/gcs_storage/ exists and the gcs_storage target builds (01-01/01-02 merged); stage
    .github/workflows/cmake.yml. Then hand off for the human action (push requires explicit user
    permission; CI observation requires the GitHub Actions UI / `gh run watch`).
  </action>
  <verify>
    <automated>MISSING — human-action gate: the human pushes to origin/develop and reports the CI run URL + matrix-cell results per <how-to-verify></automated>
  </verify>
  <done>First CI run observed; all matrix cells green (or cross-compile-only cells documented as skipping ctest).</done>
  <what-built>
    The complete CI workflow from Task 1, plus preceding plans (01-01..01-05) merged to develop.
    The first CI run on push proves:
      - Self-hosted runners pick up the job (or fall back to GitHub-hosted)
      - All three dependency releases download and extract
      - CMake configure resolves all paths with -DTHIRDPARTY_BUILD_DIR
      - gcs_storage + gcs_core + gcs_ffi build (the moved GcsGlobalDb under root src/lib/gcs_storage)
      - All 5 platforms build; ctest passes on Linux, OSX, Windows Release
      - Release artifacts upload
  </what-built>
  <how-to-verify>
    Steps for the user (push requires explicit user permission):
      1. Confirm Wave 1-3 plans (01-01..01-05) are merged to develop locally.
      2. Confirm src/lib/gcs_storage/ exists and the gcs_storage target builds (01-01/01-02 merged).
         (No GNUS-NEO-SWARM submodule pointer bump is needed — the accessors now live in root.)
      3. Push to origin/develop.
      4. Watch the CI run: `gh run watch` or GitHub Actions UI.
      5. On failure:
           - "Undefined symbols: GeniusSDKGetNode" → GeniusSDK develop release stale; re-release + re-run (Pitfall 1).
           - "Cannot find thirdparty directory" → -DTHIRDPARTY_BUILD_DIR not threaded through; fix.
           - "gcs_storage target not found" → src/lib/gcs_storage not configured; check add_subdirectory(lib/gcs_storage).
           - Runner label not found → check self-hosted runner registration.
      6. Confirm all matrix cells green (or documented cross-compile-only cells skipping ctest).
  </how-to-verify>
  <resume-signal>Type "ci-green" with the run URL, or describe which matrix cells failed and the error snippets.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → release artifacts | CI downloads prebuilt tarballs from GitHub releases and executes their contents during build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering | gh release download (thirdparty/SuperGenius/GeniusSDK) | mitigate | Pinned release tag + asset pattern (no `latest`); artifacts come from GeniusVentures org releases only. |
| T-01-06-02 | Elevation of Privilege | secrets.GNUS_TOKEN_1 | mitigate | Token used only by resolve-runners job scoped to runner-merge; never echoed or passed to untrusted steps. |
| T-01-06-03 | Tampering | self-hosted runner stale state | mitigate | Cleanup step (copy SuperGenius) removes stale build/zkLLVM dirs before each run. |
| T-01-06-04 | Info Disclosure | CI logs | accept | Build logs may include paths; no secrets/secrets-derivative data logged. |
| T-01-06-SC | Tampering | npm/pip/cargo installs | accept | No npm/pip/cargo installs in CI (prebuilt tarballs). |
</threat_model>

<verification>
- Workflow file passes `python3 -c "import yaml; yaml.safe_load(...)"` syntax check
- First CI run on push to develop: all matrix cells build; ctest green on Linux, OSX, Windows Release;
  no ctest on Android/iOS
- Release artifacts uploaded per cell
</verification>

<success_criteria>
- ROADMAP success criterion 5 ("CI/CD pipeline builds and tests on self-hosted runners for macOS,
  Linux, Windows, iOS, Android") is green
- All 5 platforms produce installable artifacts
- The GCS repo now has the same CI infrastructure maturity as SuperGenius and GeniusSDK
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/01-foundation/01-06-SUMMARY.md` when done.
</output>
