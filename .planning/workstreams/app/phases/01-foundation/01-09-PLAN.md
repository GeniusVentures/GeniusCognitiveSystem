---
phase: 01-foundation
plan: 09
type: execute
wave: 4
depends_on: ["01-08"]
files_modified:
  - src/app/templates/components/chat_message_code_block_vars.json
  - src/app/templates/components/chat_message_code_block.dart.jinja2
  - src/app/templates/components/chat_message_code_block_cubit.dart.jinja2
  - src/app/templates/components/chat_message_code_block_state.dart.jinja2
  - src/app/templates/components/chat_message_media_vars.json
  - src/app/templates/components/chat_message_media.dart.jinja2
  - src/app/templates/components/chat_message_media_cubit.dart.jinja2
  - src/app/templates/components/chat_message_media_state.dart.jinja2
  - src/app/CMakeLists.txt
  - src/app/lib/generated/chat/chat_message_code_block.dart
  - src/app/lib/generated/chat/chat_message_code_block_cubit.dart
  - src/app/lib/generated/chat/chat_message_code_block_state.dart
  - src/app/lib/generated/chat/chat_message_media.dart
  - src/app/lib/generated/chat/chat_message_media_cubit.dart
  - src/app/lib/generated/chat/chat_message_media_state.dart
autonomous: true
requirements: [CORE-05]
must_haves:
  truths:
    - "D-20: code_block and media are their OWN composites (NOT wrapped in bubble chrome) — iMessage model, top-level flow items"
    - "D-17: both composites are generated from templates + vars, append-only axes (same engine.py pattern proven on the bubble in 01-08)"
    - "code_block carries a `code` payload; media carries a `media` payload"
  artifacts:
    - path: "src/app/templates/components/chat_message_code_block_vars.json"
      provides: "Code block composite vars (payload code)"
      contains: '"payload": "code"'
    - path: "src/app/templates/components/chat_message_code_block.dart.jinja2"
      provides: "Code block composite (top-level, no bubble chrome)"
      contains: "CodeBlock"
    - path: "src/app/templates/components/chat_message_media_vars.json"
      provides: "Media composite vars (payload media)"
      contains: '"payload": "media"'
    - path: "src/app/templates/components/chat_message_media.dart.jinja2"
      provides: "Media composite (top-level, no bubble chrome)"
      contains: "Media"
  key_links:
    - from: "src/app/CMakeLists.txt"
      to: "src/app/templates/components/"
      via: "composite codegen loop extended with code_block + media"
      pattern: "chat_message_code_block"
---

<objective>
Scale the D-17 codegen pattern (proven on the bubble in 01-08) to the two remaining top-level
payload composites: `chat_message_code_block` and `chat_message_media`.

Purpose: D-20 locks the iMessage model — code and media are top-level flow items, NOT wrapped in
bubble chrome. This plan generates their Dart widget/cubit/state triples data-driven. The flow
envelope (sealed ChatFlowItem dispatch) is a SEPARATE concern and moves to 01-10, keeping this plan
focused on the two payload composites.

Output: code_block + media composite triples generated into `src/app/lib/generated/`, wired into
the codegen loop (which already owns the bubble from 01-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/STATE.md
@.planning/workstreams/app/phases/01-foundation/01-CONTEXT.md
@.planning/workstreams/app/phases/01-foundation/01-RESEARCH.md
@.planning/workstreams/app/phases/01-foundation/01-UI-SPEC.md

@src/app/templates/components/chat_message_bubble_vars.json
@src/app/templates/components/chat_message_bubble.dart.jinja2
@src/app/CMakeLists.txt

<interfaces>
<!-- Established in 01-08: the bubble-only codegen loop + engine.py invocation pattern. -->
The same loop is extended with two more `_comp` entries (code_block, media). Each new composite
renders its widget/cubit/state triple into ${GENERATED_DIR}/chat/ with per-stamp DEPENDS.

Composite _vars.json shape (mirrors the bubble's; payload differs):
```json
{ "widget_class_name": "ChatMessageCodeBlock", "file_stem": "chat_message_code_block", "payload": "code" }
{ "widget_class_name": "ChatMessageMedia",     "file_stem": "chat_message_media",     "payload": "media" }
```

The flow envelope (sealed ChatFlowItem + ChatMessageFlow list) is authored in 01-10, NOT here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author code_block + media composite templates + vars (top-level flow items)</name>
  <files>src/app/templates/components/chat_message_code_block_vars.json, src/app/templates/components/chat_message_code_block.dart.jinja2, src/app/templates/components/chat_message_code_block_cubit.dart.jinja2, src/app/templates/components/chat_message_code_block_state.dart.jinja2, src/app/templates/components/chat_message_media_vars.json, src/app/templates/components/chat_message_media.dart.jinja2, src/app/templates/components/chat_message_media_cubit.dart.jinja2, src/app/templates/components/chat_message_media_state.dart.jinja2</files>
  <read_first>
    - src/app/templates/components/chat_message_bubble_vars.json (vars fixture to mirror)
    - src/app/templates/components/chat_message_bubble.dart.jinja2 (template style to mirror)
    - .planning/workstreams/app/phases/01-foundation/01-CONTEXT.md (D-20 — NOT wrapped in bubble chrome)
  </read_first>
  <action>
    Create the code_block and media composite triples under src/app/templates/components/.

    chat_message_code_block_vars.json:
      { "widget_class_name": "ChatMessageCodeBlock", "file_stem": "chat_message_code_block",
        "payload": "code" }
    chat_message_code_block.dart.jinja2:
      - Renders a code payload WITHOUT bubble chrome. Wrap the scaffold `ScaffoldCodeBlock` atom WHEN
        it lands (scaffold Phases 9-11, additive); until then emit a code-surface placeholder that
        imports scaffold atoms and reserves the wiring seam (do NOT hardcode a hand-rolled code block).

    chat_message_media_vars.json:
      { "widget_class_name": "ChatMessageMedia", "file_stem": "chat_message_media",
        "payload": "media" }
    chat_message_media.dart.jinja2:
      - Renders a media payload (image/attachment card) WITHOUT bubble chrome, top-level in the flow.

    Each also has a _cubit and _state template mirroring the bubble's pattern (same role/state axes as
    relevant; code_block/media carry their own payload type).

    Strict rules: no bubble chrome around code/media; StrictUndefined-compatible; append-only; no
    runtime type switch; import scaffold atoms only (never edit scaffold lib/).
  </action>
  <verify>
    <automated>test -f src/app/templates/components/chat_message_code_block.dart.jinja2 && test -f src/app/templates/components/chat_message_media.dart.jinja2 && grep -q '"payload": "code"' src/app/templates/components/chat_message_code_block_vars.json && grep -q '"payload": "media"' src/app/templates/components/chat_message_media_vars.json</automated>
  </verify>
  <acceptance_criteria>
    - code_block + media vars files exist with payload code/media
    - code_block.dart.jinja2 and media.dart.jinja2 exist (no bubble chrome — top-level items)
    - cubit + state templates present for both
  </acceptance_criteria>
  <done>code_block + media composites authored as top-level flow items.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the codegen loop with code_block/media and render both</name>
  <files>src/app/CMakeLists.txt, src/app/lib/generated/chat/chat_message_code_block.dart, src/app/lib/generated/chat/chat_message_code_block_cubit.dart, src/app/lib/generated/chat/chat_message_code_block_state.dart, src/app/lib/generated/chat/chat_message_media.dart, src/app/lib/generated/chat/chat_message_media_cubit.dart, src/app/lib/generated/chat/chat_message_media_state.dart</files>
  <read_first>
    - src/app/CMakeLists.txt (current state from 01-08 — bubble loop + app_generate_chat_components)
    - .planning/workstreams/app/phases/01-foundation/01-RESEARCH.md § "UI Strand — D-17 Engine Driver" (the full _chat_composites list)
  </read_first>
  <action>
    Extend src/app/CMakeLists.txt: generalize the bubble loop to iterate the composite list
    (code_block, media) — bubble already exists, so add the two new `_comp` entries to the same loop
    (mirror RESEARCH D-17 `_chat_composites` list). Each new composite renders its widget/cubit/state
    triple into ${GENERATED_DIR}/chat/ with per-stamp DEPENDS.

    Update `app_generate_chat_components` aggregate to DEPEND on the bubble + code_block + media
    stamps (flow stamp is added in 01-10).

    Render manually (dev-loop) for the two new composites using engine.py (same invocation shape as
    01-08 Task 2, substituting the component name). Inspect generated output:
      - code_block/media generated files contain no bubble chrome
    Fix StrictUndefined drift errors by editing templates (never hand-edit generated files).
  </action>
  <verify>
    <automated>test -f src/app/lib/generated/chat/chat_message_code_block.dart && test -f src/app/lib/generated/chat/chat_message_media.dart && grep -q "chat_message_code_block" src/app/CMakeLists.txt && grep -q "chat_message_media" src/app/CMakeLists.txt && ! grep -q "chat_message_flow" src/app/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    - Codegen loop now covers bubble + code_block + media (flow added in 01-10)
    - app_generate_chat_components depends on the three composite triples
    - Generated code_block/media are top-level items (no bubble chrome)
    - No chat_message_flow reference in this plan's CMakeLists output
    - No hand-edited generated files
  </acceptance_criteria>
  <done>code_block + media composites generated; flow envelope deferred to 01-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template author → generated code | Jinja2 templates + vars rendered into committed Dart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09-01 | Tampering | engine.py --template path | mitigate | engine.py rejects `..`/absolute template paths (path-traversal guard). |
| T-01-09-02 | Info Disclosure | media/code payloads | accept | Plaintext by design; app-layer encryption deferred (roadmap v1.1). |
| T-01-09-SC | Tampering | package installs | accept | No npm/pip/cargo installs. |
</threat_model>

<verification>
- Generated code_block/media compile via `dart analyze` once 01-11 wires the shell
- Both composites regenerate drift-free (git diff clean on generated output)
</verification>

<success_criteria>
The code_block and media composites are generated data-driven as top-level flow items (no bubble
chrome), completing the D-20 payload taxonomy's leaf composites. The flow envelope (sealed dispatch)
is authored separately in 01-10.
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/01-foundation/01-09-SUMMARY.md` when done.
</output>
