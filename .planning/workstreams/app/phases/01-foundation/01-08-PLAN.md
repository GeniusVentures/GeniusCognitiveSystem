---
phase: 01-foundation
plan: 08
type: execute
wave: 3
depends_on: ["01-07"]
files_modified:
  - src/app/templates/components/chat_message_bubble.dart.jinja2
  - src/app/templates/components/chat_message_bubble_cubit.dart.jinja2
  - src/app/templates/components/chat_message_bubble_state.dart.jinja2
  - src/app/templates/components/chat_message_bubble_vars.json
  - src/app/CMakeLists.txt
  - src/app/lib/generated/chat/chat_message_bubble.dart
  - src/app/lib/generated/chat/chat_message_bubble_cubit.dart
  - src/app/lib/generated/chat/chat_message_bubble_state.dart
autonomous: true
requirements: [CORE-05]
must_haves:
  truths:
    - "D-17: the chat_message_bubble composite is generated from our own .jinja2 templates + _vars.json via scaffold engine.py (never hand-switched Dart)"
    - "The bubble template generates one variant widget class per role (ChatMessageBubbleUserSelf, ChatMessageBubbleUserPeer, ChatMessageBubbleAssistant, ChatMessageBubbleSystem) from the roles axis"
    - "The state axis (pending, streaming, thinking, complete, error) drives within-bubble chrome, not a runtime role switch"
    - "D-24/D-26: the C++ half is the protobuf ChatMessageState generated by 01-03 from src/proto/gcs_chat.proto — this plan generates ONLY the Dart widget triple (no jinja2 C++ struct, no nlohmann_json)"
    - "Append-only: roles/states arrays are added-to, never renamed/removed/reordered"
  artifacts:
    - path: "src/app/templates/components/chat_message_bubble_vars.json"
      provides: "Variant axes: roles [user_self,user_peer,assistant,system], states [pending,streaming,thinking,complete,error], payload text"
      contains: "user_self"
    - path: "src/app/templates/components/chat_message_bubble.dart.jinja2"
      provides: "Bubble widget template (one class per role)"
      contains: "ChatMessageBubble"
    - path: "src/app/lib/generated/chat/chat_message_bubble.dart"
      provides: "Generated bubble widget (4 role variants + state chrome)"
      contains: "ChatMessageBubbleUserSelf"
  key_links:
    - from: "src/app/CMakeLists.txt"
      to: "src/app/templates/components/chat_message_bubble_vars.json"
      via: "engine.py --vars + --template components/chat_message_bubble*.dart.jinja2"
      pattern: "chat_message_bubble"
    - from: "src/app/templates/components/chat_message_bubble_vars.json"
      to: "src/proto/gcs_chat.proto"
      via: "roles/states axes mirror MessageRole/MessageState enum values (D-24 both-halves — same names)"
      pattern: "user_self"
---

<objective>
Author the FIRST data-driven chat composite — `chat_message_bubble` (roles × states) — as Jinja2
templates + `_vars.json` rendered by scaffold `engine.py`, generating the Dart widget/cubit/state
triple only.

Purpose: D-17 replaces hand-switched Dart with data-driven codegen. D-24 locks "always generate
both halves", but D-26 REINTERPRETS the C++ half as the protobuf `ChatMessageState` generated by
01-03 from `src/proto/gcs_chat.proto` — the C++ half is NO LONGER a jinja2-generated struct. This
plan proves the pattern on ONE composite (the bubble) before scaling to code_block/media in 01-09
and the flow envelope in 01-10, per RESEARCH's explicit "author bubble template + vars, render,
review output as one plan before scaling" guidance.

Output: bubble composite triple generated into `src/app/lib/generated/`, wired into the
`src/app/CMakeLists.txt` codegen loop (Dart side only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/STATE.md
@.planning/workstreams/app/phases/01-foundation/01-CONTEXT.md
@.planning/workstreams/app/phases/01-foundation/01-RESEARCH.md
@.planning/workstreams/app/phases/01-foundation/01-UI-SPEC.md
@.planning/workstreams/app/phases/01-foundation/01-PATTERNS.md

@src/app/scaffold/CMakeLists.txt
@src/app/scaffold/templates/components/card_vars.json
@src/app/scaffold/templates/components/card.dart.jinja2
@src/proto/gcs_chat.proto

<interfaces>
<!-- Engine invocation facts (RESEARCH D-17 Engine Driver, verified against engine.py). -->
- `--template` is relative to a template dir (e.g. `components/chat_message_bubble.dart.jinja2`);
  path-traversal-guarded (rejects `..` and absolute paths) — templates live under src/app/templates/.
- Multi `--template-dir` is repeatable; FileSystemLoader searches in order, first match wins — pass
  OUR templates dir FIRST, then scaffold's.
- `--tokens` loads design_tokens.json into `tokens`; `--vars` merges ON TOP of tokens.
- StrictUndefined: any missing template var fails the build (drift guard).
- Scaffold `card_vars.json` is the canonical minimal fixture: keys `widget_class_name` + `file_stem`.

Composite _vars.json shape (D-17/D-18/D-19/D-20):
```json
{
  "widget_class_name": "ChatMessageBubble",
  "file_stem": "chat_message_bubble",
  "roles": ["user_self", "user_peer", "assistant", "system"],
  "states": ["pending", "streaming", "thinking", "complete", "error"],
  "payload": "text"
}
```
The role/state axis values mirror the `MessageRole`/`MessageState` enum names in
src/proto/gcs_chat.proto (D-24 both-halves): user_self, user_peer, assistant, system / pending,
streaming, thinking, complete, error. The C++ half IS that proto (01-03) — this plan does NOT
generate a C++ struct.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author chat_message_bubble _vars.json + three Dart templates (widget/cubit/state)</name>
  <files>src/app/templates/components/chat_message_bubble_vars.json, src/app/templates/components/chat_message_bubble.dart.jinja2, src/app/templates/components/chat_message_bubble_cubit.dart.jinja2, src/app/templates/components/chat_message_bubble_state.dart.jinja2</files>
  <read_first>
    - src/app/scaffold/templates/components/card_vars.json (canonical vars fixture)
    - src/app/scaffold/templates/components/card.dart.jinja2 (template style: how widget_class_name/file_stem are used)
    - src/app/scaffold/templates/components/card_cubit.dart.jinja2 (if present — cubit template pattern)
    - .planning/workstreams/app/phases/01-foundation/01-UI-SPEC.md (bubble variant axes + states)
    - src/proto/gcs_chat.proto (OPTIONAL name-mirror check — created by 01-03 in the SAME wave, may not exist yet; the MessageRole/MessageState enum names are already locked in the <interfaces> _vars.json block above)
  </read_first>
  <action>
    Create the four files under src/app/templates/components/.

    FILE 1 — chat_message_bubble_vars.json (exact content):
      {
        "widget_class_name": "ChatMessageBubble",
        "file_stem": "chat_message_bubble",
        "roles": ["user_self", "user_peer", "assistant", "system"],
        "states": ["pending", "streaming", "thinking", "complete", "error"],
        "payload": "text"
      }

    FILE 2 — chat_message_bubble.dart.jinja2:
      - Iterate `{% for role in roles %}` generating one widget class per role:
        `ChatMessageBubbleUserSelf`, `ChatMessageBubbleUserPeer`, `ChatMessageBubbleAssistant`,
        `ChatMessageBubbleSystem` (map role "user_self" -> "UserSelf", etc.).
      - Each class renders the bubble chrome (scaffold `ScaffoldSurface` card atom) around a text
        payload; alignment/color vary by role (self = trailing + own color, peer/assistant/system =
        leading + role color per UI-SPEC 60/30/10 contract).
      - Import `package:frontend_scaffold/...` atoms; resolve theme via `context.palette` / `context.dimens`.
      - The state axis is consumed via a per-state sub-widget/parameter (NOT a runtime enum/switch) —
        generate small state helpers (e.g. `_ThinkingDots`, `_ErrorOverlay`) selected structurally.

    FILE 3 — chat_message_bubble_cubit.dart.jinja2:
      - Generate `ChatMessageBubbleCubit` extending the scaffold Cubit/Bloc base (flutter_bloc ^9.0.0).
      - Cubit state fields mirror the role/state axes; no message synthesis (D-04 — FFI-event-driven).

    FILE 4 — chat_message_bubble_state.dart.jinja2:
      - Generate the state class (role + state + text) consumed by the widget.

    Strict rules: StrictUndefined — every var used in a template MUST be present in _vars.json or
    tokens. Append-only arrays (never reorder/rename existing role/state entries). No runtime
    role/state enum + switch (generated variant classes only). No hand-written switch over roles.
    NO C++ half template here (D-26 — the proto is the C++ half).
  </action>
  <verify>
    <automated>test -f src/app/templates/components/chat_message_bubble_vars.json && test -f src/app/templates/components/chat_message_bubble.dart.jinja2 && grep -q '"roles"' src/app/templates/components/chat_message_bubble_vars.json && grep -q '"states"' src/app/templates/components/chat_message_bubble_vars.json && grep -q "for role in roles" src/app/templates/components/chat_message_bubble.dart.jinja2</automated>
  </verify>
  <acceptance_criteria>
    - _vars.json has roles (4 values) + states (5 values) + payload text
    - bubble.dart.jinja2 iterates roles generating 4 variant classes (no runtime switch)
    - cubit + state templates present and reference the same axes
    - StrictUndefined-compatible (no undefined vars)
    - No C++ struct template authored in this plan
  </acceptance_criteria>
  <done>Bubble composite templates authored (Dart triple only).</done>
</task>

<task type="auto">
  <name>Task 2: Wire the bubble codegen loop into src/app/CMakeLists.txt and render the Dart triple</name>
  <files>src/app/CMakeLists.txt, src/app/lib/generated/chat/chat_message_bubble.dart, src/app/lib/generated/chat/chat_message_bubble_cubit.dart, src/app/lib/generated/chat/chat_message_bubble_state.dart</files>
  <read_first>
    - src/app/CMakeLists.txt (current state from 01-07 — app_pub_get/app_analyze/app_test targets)
    - src/app/scaffold/CMakeLists.txt §6 lines 204-319 (the stamp-triple loop to mirror)
    - .planning/workstreams/app/phases/01-foundation/01-RESEARCH.md § "UI Strand — D-17 Engine Driver"
  </read_first>
  <action>
    Extend src/app/CMakeLists.txt (after the scaffold add_subdirectory, before app_pub_get) with a
    bubble-only composite loop mirroring RESEARCH D-17 Engine Driver. Use CMake list form for the
    optional dir flags (Pitfall list-form bug 1762a45). The loop renders three stamps for the bubble
    (widget/cubit/state) into `${GENERATED_DIR}/chat/`:

      set(_chat_templates_dir "${CMAKE_CURRENT_SOURCE_DIR}/templates/components")
      set(_bubble_vars "${_chat_templates_dir}/chat_message_bubble_vars.json")
      foreach(_kind "" "_cubit" "_state")
          # add_custom_command with --template "components/chat_message_bubble${_kind}.dart.jinja2"
          #   --tokens ${DESIGN_TOKENS} --output "${GENERATED_DIR}/chat/chat_message_bubble${_kind}.dart"
          #   --template-dir "${CMAKE_CURRENT_SOURCE_DIR}/templates"   (ours FIRST)
          #   --template-dir "${CMAKE_CURRENT_SOURCE_DIR}/scaffold/templates"
          #   --vars ${_bubble_vars}
          # DEPENDS template + tokens + engine + vars
      endforeach()
      add_custom_target(generate_composite_chat_message_bubble DEPENDS <three stamps>)

    There is NO separate C++ half add_custom_command (D-26 — the C++ half is the proto generated by
    01-03). Do NOT reference cpp/chat_message_state.hpp.jinja2 anywhere.

    Add aggregate target `app_generate_chat_components` DEPENDS on the bubble stamps only (01-09/01-10
    extend this aggregate with code_block/media/flow). Update `app_analyze` and `app_test` to
    `DEPENDS app_pub_get app_generate_chat_components`.

    Render manually to review output (dev-loop):
      cd src/app && python3 scaffold/tools/engine.py --template "components/chat_message_bubble.dart.jinja2" --tokens scaffold/design_tokens.json --output lib/generated/chat/chat_message_bubble.dart --template-dir templates --template-dir scaffold/templates --vars templates/components/chat_message_bubble_vars.json
    (repeat for _cubit, _state). Inspect the generated Dart: four role classes, no runtime switch.
    Fix any StrictUndefined drift errors (a vars/template mismatch is a build error — fix the template,
    never hand-edit generated output).

    Do NOT hand-edit generated files; regenerate + drift-check per src/app/scaffold/CLAUDE.md.
  </action>
  <verify>
    <automated>test -f src/app/lib/generated/chat/chat_message_bubble.dart && grep -q "ChatMessageBubbleUserSelf" src/app/lib/generated/chat/chat_message_bubble.dart && grep -q "app_generate_chat_components" src/app/CMakeLists.txt && ! grep -q "chat_message_state.hpp\|nlohmann" src/app/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    - src/app/CMakeLists.txt has the bubble codegen loop + app_generate_chat_components aggregate (Dart-only)
    - app_analyze/app_test now DEPEND app_generate_chat_components
    - Generated bubble.dart contains 4 role classes (ChatMessageBubbleUserSelf present)
    - No cpp/chat_message_state.hpp or nlohmann_json reference in this plan's output
    - No hand-editing of generated files
  </acceptance_criteria>
  <done>Bubble composite (Dart triple) generated and wired; pattern proven for 01-09 scaling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template author → generated code | Jinja2 templates + _vars.json are rendered into committed Dart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Tampering | engine.py --template path | mitigate | engine.py `_validate_template_name` rejects `..` and absolute paths (path-traversal guarded). |
| T-01-08-02 | Tampering | template var drift | mitigate | StrictUndefined makes any vars/template mismatch a build error (no silent wrong output). |
| T-01-08-SC | Tampering | package installs | accept | No npm/pip/cargo installs (jinja2 is a system-python dep, already present). |
</threat_model>

<verification>
- Generated files exist and contain the expected markers (4 role classes)
- `dart analyze` (via app_analyze) passes on the generated bubble once 01-11 wires the shell
- Re-running engine.py produces no drift (git diff clean on generated output)
</verification>

<success_criteria>
The chat_message_bubble composite is generated data-driven (never hand-switched Dart); its four role
variants + five-state chrome are template-derived. The C++ half is the protobuf ChatMessageState
(01-03), so this plan ships only the Dart triple. The codegen loop is wired into src/app/CMakeLists.txt.
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/01-foundation/01-08-SUMMARY.md` when done.
</output>
