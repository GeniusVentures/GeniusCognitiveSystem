---
phase: 01-foundation
plan: 07
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/CMakeLists.txt
  - src/app/pubspec.yaml
autonomous: true
requirements: [CORE-05]
must_haves:
  truths:
    - "D-16: src/app/CMakeLists.txt is created this plan (gate + cache vars + scaffold include + pub/analyze/test targets); the one-line add_subdirectory(app) in src/CMakeLists.txt is owned by 01-03"
    - "src/app/CMakeLists.txt gates all app targets behind FRONTEND_BUILD_ENABLED (default OFF) so the C++-only CI path (D-08) configures cleanly"
    - "TEMPLATES_DIR, GENERATED_DIR, FRONTEND_TARGET cache vars are set BEFORE add_subdirectory(scaffold)"
    - "pubspec drops flutter_chat_ui/flutter_chat_core, KEEPS neoswarm_ffi (D-03), and adds flutter_bloc + ffigen + protobuf + protoc_plugin"
    - "GCS targets never DEPEND on frontend_all / frontend_generate_api (GCS has no api-specs)"
  artifacts:
    - path: "src/app/CMakeLists.txt"
      provides: "FRONTEND_BUILD_ENABLED gate + cache vars + scaffold include + pub/analyze/test targets"
      contains: "FRONTEND_BUILD_ENABLED"
    - path: "src/app/pubspec.yaml"
      provides: "Dart deps: drops legacy chat kit, keeps neoswarm_ffi, adds flutter_bloc + ffigen + protobuf + protoc_plugin"
      contains: "flutter_bloc"
  key_links:
    - from: "src/app/CMakeLists.txt"
      to: "src/app/scaffold/CMakeLists.txt"
      via: "add_subdirectory(scaffold) after cache vars set"
      pattern: "add_subdirectory\\(.*scaffold"
---

<objective>
Establish the D-16 build wiring: `src/app/CMakeLists.txt` gates everything behind
`FRONTEND_BUILD_ENABLED` (default OFF), and `pubspec.yaml` is rewritten to drop the legacy chat kit
(KEEP neoswarm_ffi per D-03) and add the scaffold + flutter_bloc + ffigen + protobuf + protoc_plugin
dependencies. (The one-line `add_subdirectory(app)` in `src/CMakeLists.txt` is owned by 01-03, which
already coordinates that file's proto/ffi subdirectory lines.)

Purpose: This is the UI strand's build foundation. The gate keeps the C++-only CI path (D-08)
unaffected when dart/flutter are absent, and the cache-var ordering is the #1 wiring pitfall
(RESEARCH Pitfall U1: cache vars set AFTER add_subdirectory(scaffold) silently don't apply).

Accepted broken-window (Pitfall U6): dropping flutter_chat_ui/flutter_chat_core here makes
`flutter analyze`/`flutter test` fail (src/app/lib/main.dart still imports them, and
src/app/test/widget_test.dart still references MyApp) until 01-11 rewrites main.dart +
widget_test.dart together. Accepted because the C++-only CI (FRONTEND_BUILD_ENABLED=OFF) never runs
app_analyze/app_test between wave 2 and wave 6; 01-11 closes the window. Do NOT also edit main.dart
here — that file belongs to 01-11 (avoid the reverse overlap).

Output: `src/app/CMakeLists.txt` (build wiring) + rewritten `pubspec.yaml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/STATE.md
@.planning/workstreams/app/phases/01-foundation/01-CONTEXT.md
@.planning/workstreams/app/phases/01-foundation/01-RESEARCH.md
@.planning/workstreams/app/phases/01-foundation/01-PATTERNS.md

@src/CMakeLists.txt
@src/app/pubspec.yaml
@src/app/scaffold/CMakeLists.txt

<interfaces>
<!-- Scaffold cache vars consumed BEFORE add_subdirectory(scaffold) (RESEARCH D-16 Build Wiring). -->
GCS sets exactly three: TEMPLATES_DIR, GENERATED_DIR, FRONTEND_TARGET (all CACHE STRING).
Scaffold also reads ENGINE_SCRIPT, DESIGN_TOKENS, API_SPECS_DIR (leave to scaffold defaults).

Key verified facts:
- cmake/CommonBuildParameters.cmake already does add_subdirectory(${PROJECT_ROOT}/src ...), so the
  chain src/CMakeLists.txt → add_subdirectory(app) → add_subdirectory(scaffold) needs ZERO changes to cmake/ or build/.
- Scaffold CMakeLists is standalone-safe (project() guard) and additive under a parent.
- Scaffold reads: TEMPLATES_DIR, GENERATED_DIR, FRONTEND_TARGET, API_SPECS_DIR, ENGINE_SCRIPT, DESIGN_TOKENS.
- Pitfall U4: never invoke frontend_generate_api / frontend_all / frontend_build_* (GCS has no api-specs/).
- List-form flag bug (1762a45): pass optional dir flags in CMake list form (--template-dir "${DIR}"),
  never as a single space-embedded string.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite src/app/pubspec.yaml (drop legacy kit, add flutter_bloc + ffigen)</name>
  <files>src/app/pubspec.yaml</files>
  <read_first>
    - src/app/pubspec.yaml (the file being modified — full read)
    - .planning/workstreams/app/phases/01-foundation/01-CONTEXT.md (D-10, D-11, D-14)
    - .planning/workstreams/app/phases/01-foundation/01-RESEARCH.md § "UI Strand — D-16 Build Wiring"
  </read_first>
  <action>
    Edit src/app/pubspec.yaml. Keep `name: flutter_app` (avoid import churn). Make EXACTLY these changes:

    dependencies:
      - KEEP: flutter (sdk), cupertino_icons ^1.0.8, ffi ^2.2.0
      - KEEP: neoswarm_ffi: path: ../GNUS-NEO-SWARM/neoswarm_ffi   (D-03 — do NOT remove)
      - ADD: flutter_bloc: ^9.0.0
      - ADD: protobuf (package:protobuf — D-26 Dart decode; `flutter pub add protobuf`)
      - KEEP: frontend_scaffold: path: scaffold
      - REMOVE: flutter_chat_core: ^2.0.0
      - REMOVE: flutter_chat_ui: ^2.0.0

    dev_dependencies:
      - KEEP: flutter_test (sdk), flutter_lints ^5.0.0
      - ADD: ffigen: ^11.0.0
      - ADD: protoc_plugin (provides protoc-gen-dart; `dart pub add --dev protoc_plugin`)

    Do NOT touch the `flutter:` section (uses-material-design: true stays). Do NOT change `environment: sdk: ^3.8.1`.

    Then resolve deps: `cd src/app && flutter pub get` (or `dart pub get`). Confirm no resolution
    errors from the removed legacy packages.
  </action>
  <verify>
    <automated>grep -q "flutter_bloc: \^9.0.0" src/app/pubspec.yaml && grep -q "ffigen: \^11.0.0" src/app/pubspec.yaml && grep -q "protobuf" src/app/pubspec.yaml && grep -q "protoc_plugin" src/app/pubspec.yaml && ! grep -q "flutter_chat_ui\|flutter_chat_core" src/app/pubspec.yaml && grep -q "neoswarm_ffi" src/app/pubspec.yaml && grep -q "frontend_scaffold:" src/app/pubspec.yaml</automated>
  </verify>
  <acceptance_criteria>
    - pubspec has flutter_bloc ^9.0.0, ffigen ^11.0.0, protobuf, protoc_plugin
    - flutter_chat_ui, flutter_chat_core are GONE; neoswarm_ffi is KEPT (D-03)
    - frontend_scaffold (path: scaffold) present
    - flutter pub get resolves without error
  </acceptance_criteria>
  <done>Dart deps rewired to scaffold + flutter_bloc + ffigen + protobuf + protoc_plugin (neoswarm_ffi kept).</done>
</task>

<task type="auto">
  <name>Task 2: Create src/app/CMakeLists.txt (FRONTEND_BUILD_ENABLED gate + cache vars + scaffold include)</name>
  <files>src/app/CMakeLists.txt</files>
  <read_first>
    - src/app/scaffold/CMakeLists.txt lines 12-14, 84-93 (standalone guard + cache vars)
    - .planning/workstreams/app/phases/01-foundation/01-RESEARCH.md § "UI Strand — D-16 Build Wiring" (the full wiring shape)
    - .planning/workstreams/app/phases/01-foundation/01-PATTERNS.md § "src/app/CMakeLists.txt"
  </read_first>
  <action>
    Create src/app/CMakeLists.txt (new file; NO cmake_minimum_required, NO project()) with EXACTLY this shape:

      # GCS Flutter app — gated behind dart/flutter detection (D-16, D-08).
      option(FRONTEND_BUILD_ENABLED "Enable Flutter/Dart app targets" OFF)
      if(NOT FRONTEND_BUILD_ENABLED)
          return()
      endif()

      find_program(DART_EXECUTABLE dart)
      find_program(FLUTTER_EXECUTABLE flutter)
      find_program(PYTHON3_EXECUTABLE python3)
      if(NOT DART_EXECUTABLE OR NOT FLUTTER_EXECUTABLE)
          message(WARNING "FRONTEND_BUILD_ENABLED=ON but dart/flutter not found — skipping app targets.")
          return()
      endif()

      # Cache vars MUST be set BEFORE add_subdirectory(scaffold) (Pitfall U1).
      set(TEMPLATES_DIR "${CMAKE_CURRENT_SOURCE_DIR}/templates" CACHE STRING "GCS chat composite templates")
      set(GENERATED_DIR "${CMAKE_CURRENT_SOURCE_DIR}/lib/generated" CACHE STRING "GCS generated composite output (committed)")
      set(FRONTEND_TARGET "flutter" CACHE STRING "Scaffold codegen target")

      if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/scaffold/CMakeLists.txt")
          add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/scaffold scaffold)
      endif()

      add_custom_target(app_pub_get
          COMMAND ${DART_EXECUTABLE} pub get
          WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
          COMMENT "Resolving app Dart dependencies"
      )
      add_custom_target(app_analyze
          COMMAND ${DART_EXECUTABLE} analyze --fatal-infos
          WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
          DEPENDS app_pub_get
      )
      add_custom_target(app_test
          COMMAND ${FLUTTER_EXECUTABLE} test
          WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
          DEPENDS app_pub_get
      )

    NOTE: app_analyze/app_test DEPEND only on app_pub_get for now. The `DEPENDS app_generate_chat_components`
    dependency is added in 01-08 when the composite codegen loop lands (avoids a dangling target reference).
    Do NOT invoke or depend on frontend_all / frontend_generate_api (GCS has no api-specs — Pitfall U4).
  </action>
  <verify>
    <automated>grep -q "option(FRONTEND_BUILD_ENABLED" src/app/CMakeLists.txt && grep -q "set(TEMPLATES_DIR" src/app/CMakeLists.txt && grep -q "set(GENERATED_DIR" src/app/CMakeLists.txt && grep -q "set(FRONTEND_TARGET" src/app/CMakeLists.txt && grep -q "add_subdirectory(.*scaffold" src/app/CMakeLists.txt && ! grep -q "frontend_all\|frontend_generate_api" src/app/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    - src/app/CMakeLists.txt exists with FRONTEND_BUILD_ENABLED option (default OFF)
    - Three cache vars set BEFORE add_subdirectory(scaffold)
    - app_pub_get / app_analyze / app_test targets present (DEPENDS app_pub_get only)
    - No reference to frontend_all / frontend_generate_api
  </acceptance_criteria>
  <done>App build gating + scaffold include in place; app targets ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev / dart pub get → app | Dart packages resolved at build/dev time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Tampering | pubspec dependency pins | mitigate | flutter_bloc ^9.0.0, ffigen ^11.0.0, protobuf, protoc_plugin are established pub.dev packages; scaffold + neoswarm_ffi are pinned path deps — no unpinned git URL deps. |
| T-01-07-02 | Tampering | FRONTEND_BUILD_ENABLED gate bypass | accept | Gate is a build-time convenience, not a security boundary; app targets only run with dart/flutter present. |
| T-01-07-SC | Tampering | npm/pip/cargo installs | accept | No npm/pip/cargo installs (dart pub is out of scope for the npm/pip/cargo gate). |
</threat_model>

<verification>
- With `-DFRONTEND_BUILD_ENABLED=ON` and dart/flutter present, `app_pub_get`/`app_analyze`/`app_test`
  targets are visible (configures; full app build is exercised in later plans)
</verification>

<success_criteria>
D-16 build wiring is complete: the app is gated behind FRONTEND_BUILD_ENABLED (OFF default) in
src/app/CMakeLists.txt, cache vars are ordered correctly before add_subdirectory(scaffold), and pubspec
has the correct dependency set for the scaffold + flutter_bloc + ffigen + protobuf stack. (The top-level
src/CMakeLists.txt add_subdirectory(app) wiring is owned by 01-03.)
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/01-foundation/01-07-SUMMARY.md` when done.
</output>
