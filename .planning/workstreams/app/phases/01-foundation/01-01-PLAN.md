---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/gcs_storage/gcs_global_db.hpp
  - src/lib/gcs_storage/gcs_global_db.cpp
  - src/lib/gcs_storage/CMakeLists.txt
  - src/CMakeLists.txt
  - test/test_gcs_global_db.cpp
  - test/CMakeLists.txt
autonomous: true
requirements: [CORE-05]
must_haves:
  truths:
    - "GcsGlobalDb lives under root src/lib/gcs_storage/ and is built as its own gcs_storage CMake target (NOT neoswarm_storage)"
    - "GcsGlobalDb exposes AddBroadcastTopic, AddListenTopic, Put, Get as public pass-throughs to the underlying sgns::crdt::GlobalDB"
    - "All four accessors return outcome::failure(Error::GcsDbError) when called before Initialize() (m_running is false)"
    - "The moved test_gcs_global_db.cpp lives in the root test/ tree and compiles against gcs_storage (not the submodule copy)"
  artifacts:
    - path: "src/lib/gcs_storage/gcs_global_db.hpp"
      provides: "Public pass-through declarations on GcsGlobalDb"
      contains: "AddBroadcastTopic"
    - path: "src/lib/gcs_storage/gcs_global_db.cpp"
      provides: "Pass-through implementations guarding on m_running"
      contains: "m_db->AddBroadcastTopic"
    - path: "src/lib/gcs_storage/CMakeLists.txt"
      provides: "gcs_storage STATIC target linking crdt_globaldb + GeniusSDK_shared + neoswarm_common"
      contains: "add_library(gcs_storage"
    - path: "src/CMakeLists.txt"
      provides: "add_subdirectory wiring to pull gcs_storage into the GCS build"
      contains: "add_subdirectory(lib/gcs_storage)"
    - path: "test/CMakeLists.txt"
      provides: "Stub test-dir CMake (enable_testing placeholder; 01-04 replaces with gcs_test registration)"
      contains: "enable_testing"
  key_links:
    - from: "src/lib/gcs_storage/gcs_global_db.cpp"
      to: "sgns::crdt::GlobalDB"
      via: "m_db->AddBroadcastTopic / AddListenTopic / Put / Get"
      pattern: "m_db->(AddBroadcastTopic|AddListenTopic|Put|Get)"
    - from: "src/lib/gcs_storage/CMakeLists.txt"
      to: "neoswarm_common"
      via: "target_link_libraries(gcs_storage PUBLIC neoswarm_common)"
      pattern: "target_link_libraries\\(gcs_storage PUBLIC neoswarm_common"
---

<objective>
Relocate `GcsGlobalDb` from the GNUS-NEO-SWARM submodule into the GCS root repo under
`src/lib/gcs_storage/` as its own `gcs_storage` CMake target, and extend it with four data-plane
pass-through accessors (`AddBroadcastTopic`, `AddListenTopic`, `Put`, `Get`) so Phase 1 can
smoke-test CORE-05's CRDT substrate (topic wiring + Put/Get round-trip) without exposing the
underlying `sgns::crdt::GlobalDB` pointer.

Purpose: D-25 (locked) corrects a placement mistake — `GcsGlobalDb` is GCS's storage component,
misplaced in the neoswarm submodule by neoswarm workstream Phase 03-01 before `feature/app-restructure`
established `src/lib/` as the app C++ home. This plan makes `src/lib/gcs_storage/` the single
authoritative home, so 01-02 links `gcs_core` to `gcs_storage` (not `neoswarm_storage`).

Output: `gcs_storage` static lib target, four new pass-throughs on GcsGlobalDb, and the moved
test file in root `test/`. The submodule's stale copy is left in place (neoswarm Phase 03-02/03-03
removes it via the pointer-update note); GCS stops using it entirely.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/STATE.md
@.planning/workstreams/app/ROADMAP.md
@.planning/workstreams/app/phases/01-foundation/01-CONTEXT.md
@.planning/workstreams/app/phases/01-foundation/01-RESEARCH.md
@.planning/workstreams/app/phases/01-foundation/01-PATTERNS.md

@GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp
@GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp
@GNUS-NEO-SWARM/src/storage/CMakeLists.txt
@GNUS-NEO-SWARM/src/common/error.hpp
@GNUS-NEO-SWARM/test/storage/test_gcs_global_db.cpp

<interfaces>
<!-- Existing GcsGlobalDb API surface (already in submodule). Plan MOVES + EXTENDS; do not change signatures. -->
From GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp (namespace `sgns::neoswarm::storage`):
```cpp
class GcsGlobalDb {
public:
    struct Config { std::string m_dbPath; /* ... */ };
    outcome::result<void> Initialize();                                                   // production (GeniusSDKGetNode())
    outcome::result<void> Initialize(std::shared_ptr<sgns::ipfs_pubsub::GossipPubSub> pubsub); // test seam
    void Shutdown() noexcept;                                                            // NOTE: returns void, not outcome
    bool IsRunning() const noexcept;
};
```
IMPORTANT verified facts (do NOT repeat prior-plan guesses):
- Namespace is `sgns::neoswarm::storage` (NOT `sgns::crdt::GcsGlobalDb` / `sgns::gcs`).
- `Shutdown()` returns `void` (noexcept) — there is NO `outcome::result<void> Shutdown()`.
- Header guard is `NEOSWARM_STORAGE_GCS_GLOBAL_DB_HPP`; self-include is `"storage/gcs_global_db.hpp"`.
- Include path for outcome is `<libp2p/outcome/outcome.hpp>` with `namespace outcome = libp2p::outcome` (via `common/error.hpp`).

Underlying GlobalDB API to wrap:
```cpp
outcome::result<void> AddBroadcastTopic(const std::string& topicName);
outcome::result<void> AddListenTopic(const std::string& topicName);
outcome::result<void> Put(const HierarchicalKey& key, const Buffer& value);
outcome::result<Buffer> Get(const HierarchicalKey& key) const;
```
Error mapping: use `Error::GcsDbError` (from neoswarm `common/error.hpp`) for both "not running" and any underlying failure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move GcsGlobalDb class into src/lib/gcs_storage/ and fix header/include/guard</name>
  <files>src/lib/gcs_storage/gcs_global_db.hpp, src/lib/gcs_storage/gcs_global_db.cpp</files>
  <read_first>
    - GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp (source being moved — full read)
    - GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp (source being moved — full read; note existing includes and m_running usage)
    - GNUS-NEO-SWARM/src/common/error.hpp (Error enum; confirm GcsDbError = 22)
    - .planning/workstreams/app/phases/01-foundation/01-CONTEXT.md (D-25)
  </read_first>
  <action>
    GNUS-NEO-SWARM is a git SUBMODULE (gitlink mode 160000); its files are NOT tracked in the GCS
    root index, so a literal `git mv` across the submodule boundary is impossible. Perform the move
    as a copy-into-root + provenance record:

    1. Create `src/lib/gcs_storage/` and copy the two class files verbatim:
         mkdir -p src/lib/gcs_storage
         cp GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp src/lib/gcs_storage/gcs_global_db.hpp
         cp GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp src/lib/gcs_storage/gcs_global_db.cpp
       Record provenance for the SUMMARY: `git -C GNUS-NEO-SWARM log --oneline -- src/storage/gcs_global_db.cpp | head -5`
       (the neoswarm submodule retains the original history; the root copy starts fresh — document this).

    2. In `src/lib/gcs_storage/gcs_global_db.hpp` make EXACTLY these edits (no other changes):
       - Header guard `#ifndef NEOSWARM_STORAGE_GCS_GLOBAL_DB_HPP` / `#define` / `#endif` →
         `#ifndef GCS_STORAGE_GCS_GLOBAL_DB_HPP` / `#define` / `#endif`.
       - KEEP namespace `sgns::neoswarm::storage` unchanged (minimal diff; namespace migration is a
         separate follow-up, not this phase).
       - Do NOT change any existing method signature.

    3. In `src/lib/gcs_storage/gcs_global_db.cpp` make EXACTLY these edits:
       - Self-include `#include "storage/gcs_global_db.hpp"` → `#include "gcs_storage/gcs_global_db.hpp"`.
       - No other include or logic change in this task (accessors are Task 2).

    C++ style (neoswarm): Allman braces, space inside parens, `m_` member prefix, camelCase args,
    120-char max, C++17 ceiling. Do NOT rename files or symbols.
  </action>
  <verify>
    <automated>test -f src/lib/gcs_storage/gcs_global_db.hpp && test -f src/lib/gcs_storage/gcs_global_db.cpp && grep -q "GCS_STORAGE_GCS_GLOBAL_DB_HPP" src/lib/gcs_storage/gcs_global_db.hpp && grep -q 'gcs_storage/gcs_global_db.hpp' src/lib/gcs_storage/gcs_global_db.cpp && grep -q "namespace sgns::neoswarm::storage" src/lib/gcs_storage/gcs_global_db.hpp && ! grep -q "NEOSWARM_STORAGE_GCS_GLOBAL_DB_HPP" src/lib/gcs_storage/gcs_global_db.hpp</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist under src/lib/gcs_storage/
    - Header guard is GCS_STORAGE_GCS_GLOBAL_DB_HPP (old guard absent)
    - Self-include is "gcs_storage/gcs_global_db.hpp" (not "storage/...")
    - Namespace remains sgns::neoswarm::storage (unchanged)
    - No method signature changed; no logic changed in this task
  </acceptance_criteria>
  <done>Class relocated to root src/lib/gcs_storage/ with corrected guard + include; ready to extend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add four data-plane pass-through accessors to GcsGlobalDb</name>
  <files>src/lib/gcs_storage/gcs_global_db.hpp, src/lib/gcs_storage/gcs_global_db.cpp</files>
  <read_first>
    - src/lib/gcs_storage/gcs_global_db.hpp (declarations site — full read)
    - src/lib/gcs_storage/gcs_global_db.cpp (implementation site — full read; note the existing m_running guard idiom in Shutdown)
    - GNUS-NEO-SWARM/src/common/error.hpp (Error::GcsDbError)
    - .planning/workstreams/app/phases/01-foundation/01-PATTERNS.md § "pass-through accessors"
  </read_first>
  <action>
    Declare in the `public:` section of `GcsGlobalDb`, immediately AFTER `IsRunning()`:

      outcome::result<void>        AddBroadcastTopic( const std::string &topicName );
      outcome::result<void>        AddListenTopic( const std::string &topicName );
      outcome::result<void>        Put( const std::string &key, const std::string &value );
      outcome::result<std::string> Get( const std::string &key );

    Each with a Doxygen block containing \brief (name the wrapped GlobalDB call), \param[in] per
    parameter, and \return ("outcome::success on success; Error::GcsDbError if not running or the
    underlying call fails").

    Implement at the END of the .cpp, each following the existing Shutdown() guard shape:

      outcome::result<void> GcsGlobalDb::AddBroadcastTopic( const std::string &topicName )
      {
          if ( !m_running.load() )
          {
              return outcome::failure( Error::GcsDbError );
          }
          return m_db->AddBroadcastTopic( topicName );
      }

    AddListenTopic: identical shape.

    Put: convert `std::string key` → the GlobalDB `HierarchicalKey` type and `std::string value` →
    the GlobalDB `Buffer` type at the call site, then `return m_db->Put(keyTyped, valueTyped);`.
    Copy the EXACT construction idiom from an existing SuperGenius call site of `GlobalDB::Put`
    (search `../SuperGenius` for `->Put(`); if no unambiguous call site exists, STOP and ask the user.

    Get: `auto buf = m_db->Get(HierarchicalKey{key});` then convert the returned Buffer to
    `std::string` (use the Buffer-to-string idiom from the same SuperGenius usage). Propagate any
    underlying `outcome::failure` as `outcome::failure( Error::GcsDbError )`.

    Rules: guard EVERY method with `if ( !m_running.load() )` returning Error::GcsDbError; map ANY
    underlying failure to Error::GcsDbError; Allman braces; space inside parens; no new includes
    unless the HierarchicalKey/Buffer conversion requires one (if so, add the SAME include the
    SuperGenius call site uses). Do NOT add logging (keep diff minimal).
  </action>
  <verify>
    <automated>grep -c "GcsGlobalDb::AddBroadcastTopic\|GcsGlobalDb::AddListenTopic\|GcsGlobalDb::Put\|GcsGlobalDb::Get" src/lib/gcs_storage/gcs_global_db.cpp | grep -q "^4$" && grep -c "m_running.load()" src/lib/gcs_storage/gcs_global_db.cpp | grep -q "^[4-9]\|^[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly four new member-function definitions added
    - Every definition starts with the m_running.load() guard returning Error::GcsDbError
    - Every definition forwards to the matching m_db-> method
    - Return types: outcome::result<void> x3, outcome::result<std::string> for Get
    - No sleep_for, no OS ifdefs, no magic numbers
  </acceptance_criteria>
  <done>Four pass-throughs declared + implemented on the moved GcsGlobalDb with correct guards.</done>
</task>

<task type="auto">
  <name>Task 3: Create gcs_storage CMake target and wire src/CMakeLists.txt</name>
  <files>src/lib/gcs_storage/CMakeLists.txt, src/CMakeLists.txt</files>
  <read_first>
    - GNUS-NEO-SWARM/src/storage/CMakeLists.txt (FULL 98 lines — the linkage/include blocks to copy)
    - src/CMakeLists.txt (the file being appended to — currently 16 lines)
    - .planning/workstreams/app/phases/01-foundation/01-RESEARCH.md § "Build System Wiring (Q1)" (D-25 CMake wiring)
  </read_first>
  <action>
    Create `src/lib/gcs_storage/CMakeLists.txt` (new file; NO cmake_minimum_required, NO project()).
    Replicate neoswarm_storage's linkage, adapted for the moved file:

      add_library(gcs_storage STATIC
          gcs_global_db.cpp
      )

      # Self-include resolves "gcs_storage/gcs_global_db.hpp" (parent dir = src/lib/).
      target_include_directories(gcs_storage PUBLIC
          $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/..>
      )

      # neoswarm_common provides common/error.hpp + common/logging.hpp + the OUTCOME_HPP_DEFINE_ERROR_2
      # symbol from common/error.cpp (the moved .cpp includes common/error.hpp and common/logging.hpp).
      target_link_libraries(gcs_storage PUBLIC neoswarm_common)

      # SuperGenius CRDT GlobalDB — hard required (copy neoswarm_storage lines 14-18, rename target).
      if(TARGET sgns::crdt_globaldb)
          target_link_libraries(gcs_storage PUBLIC sgns::crdt_globaldb)
      else()
          message(FATAL_ERROR "sgns::crdt_globaldb target not found — SuperGenius must be configured before gcs_storage")
      endif()

      # GeniusSDK — hard required (copy neoswarm_storage lines 30-47, incl. build-tree dylib redirect,
      # rename target references neoswarm_storage -> gcs_storage).

      # Then copy VERBATIM neoswarm_storage lines 50-97 (include dirs only, rename target):
      #   SuperGenius include, ipfs-pubsub/ipfs-lite/libp2p/boost, GENIUS_SDK_DIR/src, wallet-core,
      #   ZKLLVM (if DEFINED), SuperGenius evmrelay, MNN_INCLUDE_DIR.

    NOTE: drop `neoswarm_proto` (the moved .cpp includes no proto headers — verified include list has
    no proto/ or api/ includes). If the build later reports unresolved proto symbols, that is a
    signal to re-add it, but do NOT pre-add it speculatively.

    Then edit `src/CMakeLists.txt`: append EXACTLY this line as the LAST line (after the existing
    gcs_core block):

      add_subdirectory(lib/gcs_storage)

    Do NOT modify the existing gcs_core target block. Do NOT add `add_subdirectory(app)` or
    `add_subdirectory(ffi)` here (those are 01-07 / 01-03 respectively).
  </action>
  <verify>
    <automated>grep -q "add_library(gcs_storage STATIC" src/lib/gcs_storage/CMakeLists.txt && grep -q "target_link_libraries(gcs_storage PUBLIC neoswarm_common)" src/lib/gcs_storage/CMakeLists.txt && grep -q "sgns::crdt_globaldb" src/lib/gcs_storage/CMakeLists.txt && grep -q "add_subdirectory(lib/gcs_storage)" src/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/gcs_storage/CMakeLists.txt exists with add_library(gcs_storage STATIC gcs_global_db.cpp)
    - Links neoswarm_common + sgns::crdt_globaldb (hard-required FATAL_ERROR) + sgns::GeniusSDK_shared
    - All thirdparty/SuperGenius/GeniusSDK/zkLLVM/evmrelay/MNN include blocks present (copied)
    - neoswarm_proto is NOT linked
    - src/CMakeLists.txt ends with add_subdirectory(lib/gcs_storage); gcs_core block untouched
  </acceptance_criteria>
  <done>gcs_storage target defined and wired into the GCS build hub.</done>
</task>

<task type="auto">
  <name>Task 4: Move the test file into root test/ tree (include fix only)</name>
  <files>test/test_gcs_global_db.cpp, test/CMakeLists.txt</files>
  <read_first>
    - GNUS-NEO-SWARM/test/storage/test_gcs_global_db.cpp (source being moved — full read)
    - src/lib/gcs_storage/gcs_global_db.hpp (the header it now includes)
  </read_first>
  <action>
    Copy the test into the root test tree (same submodule-boundary rationale as Task 1):
      mkdir -p test
      cp GNUS-NEO-SWARM/test/storage/test_gcs_global_db.cpp test/test_gcs_global_db.cpp

    Fix ONLY the include that references the moved header: any
    `#include "storage/gcs_global_db.hpp"` → `#include "gcs_storage/gcs_global_db.hpp"`.

    Also create a STUB test/CMakeLists.txt so the add_subdirectory(test) hook
    (cmake/CommonBuildParameters.cmake, gated on BUILD_TESTS) never fails configure before 01-04
    lands the real file. Stub content: one comment line + `enable_testing()` (01-04 overwrites it).

    Do NOT rewrite test logic, test names, or the WaitForCondition usage in this task. The real
    test/CMakeLists.txt registration happens in 01-04 (this plan only stages the file + a stub).
    The submodule's original test file is left in place for neoswarm 03-02/03-03 to remove.
  </action>
  <verify>
    <automated>test -f test/test_gcs_global_db.cpp && grep -q 'gcs_storage/gcs_global_db.hpp' test/test_gcs_global_db.cpp && ! grep -q 'storage/gcs_global_db.hpp' test/test_gcs_global_db.cpp</automated>
  </verify>
  <acceptance_criteria>
    - test/test_gcs_global_db.cpp exists
    - Its include now references gcs_storage/gcs_global_db.hpp (old storage/ path absent)
    - No test logic changed
  </acceptance_criteria>
  <done>Moved test available at root test/test_gcs_global_db.cpp; 01-04 registers it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer network → GcsGlobalDb | Untrusted data enters via GossipPubSub topics (AddListenTopic) and CRDT sync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | GcsGlobalDb::AddListenTopic / Get | accept | Phase 1 topics are local test topics (`gcs/chat/smoke-test`); no authz exists yet. Topic authorization is a Phase 3 concern (D-25 note). |
| T-01-02 | Info Disclosure | GcsGlobalDb::Get | accept | No PII at rest; CRDT store holds plaintext test records only. Encryption is deferred (roadmap v1.1). |
| T-01-03 | Elevation of Privilege | GcsGlobalDb pass-throughs | mitigate | The four accessors are NOT exposed on the FFI ABI in Phase 1 (only gcs_core → FFI); the m_running guard blocks use before init. |
| T-01-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this plan (C++ deps are prebuilt thirdparty releases per D-07). |
</threat_model>

<verification>
After all four tasks:
- `ls src/lib/gcs_storage/` shows gcs_global_db.{hpp,cpp} + CMakeLists.txt
- `grep -c "add_subdirectory(lib/gcs_storage)" src/CMakeLists.txt` == 1
- The four accessors are present with m_running guards
- Full build is validated in 01-04 (smoke tests); this plan does not itself run a configure — it
  leaves gcs_storage unexercised until 01-02 links gcs_core and 01-04 registers tests.
</verification>

<success_criteria>
GcsGlobalDb lives in root src/lib/gcs_storage/ behind a gcs_storage target, exposes the four
data-plane pass-throughs with correct not-running guards, and the moved test is staged in root
test/. The neoswarm submodule copy is no longer referenced by GCS (neoswarm 03-02/03-03 owns its
eventual removal).
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
