---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp
  - GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp
autonomous: true
requirements: [CORE-05]
must_haves:
  truths:
    - "GcsGlobalDb exposes AddBroadcastTopic, AddListenTopic, Put, Get as public pass-throughs to the underlying sgns::crdt::GlobalDB"
    - "All four accessors return outcome::failure(Error::GcsDbError) when called before Initialize()"
    - "The submodule change is committed on a GNUS-NEO-SWARM branch (not on the GCS repo directly)"
  artifacts:
    - path: "GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp"
      provides: "Public pass-through declarations on GcsGlobalDb"
      contains: "AddBroadcastTopic"
    - path: "GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp"
      provides: "Pass-through implementations guarding on m_running"
      contains: "m_db->AddBroadcastTopic"
  key_links:
    - from: "GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp"
      to: "sgns::crdt::GlobalDB"
      via: "m_db->AddBroadcastTopic / AddListenTopic / Put / Get"
      pattern: "m_db->(AddBroadcastTopic|AddListenTopic|Put|Get)"
---

<objective>
Add minimal pass-through accessors (`AddBroadcastTopic`, `AddListenTopic`, `Put`, `Get`) to
`GcsGlobalDb` in the GNUS-NEO-SWARM submodule so Phase 1 can smoke-test CORE-05's CRDT substrate
(topic wiring + Put/Get round-trip) from the GCS root without exposing the underlying
`sgns::crdt::GlobalDB` pointer.

Purpose: Unblocks the GCS-side smoke test (Plan 04) and CORE-05 success criterion 2 ("GlobalDB CRDT
instance can be initialized and a test op can be published/subscribed locally"). Approved per
Open Question 2 in 01-RESEARCH.md (user has accepted a neoswarm branch + PR for this).

Output: 2 files modified in GNUS-NEO-SWARM; committed on a neoswarm feature branch; submodule
pointer bump on GCS `develop` is deferred to the end of the phase (after Plan 04's smoke test
proves the accessors work).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/STATE.md
@.planning/workstreams/app/ROADMAP.md
@.planning/workstreams/app/phases/01-foundation/01-CONTEXT.md
@.planning/workstreams/app/phases/01-foundation/01-RESEARCH.md
@.planning/workstreams/app/phases/01-foundation/01-PATTERNS.md

@GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp
@GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp
@GNUS-NEO-SWARM/src/common/error.hpp

<interfaces>
<!-- Existing GcsGlobalDb API surface (already in tree). Plan EXTENDS this; do not change signatures. -->

From GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp:
```cpp
class GcsGlobalDb {
public:
    struct Config { std::string m_dbPath; /* ... */ };
    outcome::result<void> Initialize();                                            // production (uses GeniusSDKGetNode())
    outcome::result<void> Initialize(std::shared_ptr<sgns::ipfs_pubsub::GossipPubSub> pubsub);  // test seam
    outcome::result<void> Shutdown();
    bool IsRunning() const;
private:
    std::shared_ptr<sgns::crdt::GlobalDB> m_db;
    std::atomic<bool> m_running{false};
    // ...
};
```

Underlying GlobalDB API to wrap (from ../SuperGenius/src/crdt/globaldb/globaldb.hpp:89-225):
```cpp
outcome::result<void> AddBroadcastTopic(const std::string& topicName);
outcome::result<void> AddListenTopic(const std::string& topicName);
outcome::result<void> Put(const HierarchicalKey& key, const Buffer& value);
outcome::result<Buffer> Get(const HierarchicalKey& key) const;
```

Error mapping (from GNUS-NEO-SWARM/src/common/error.hpp): use `Error::GcsDbError` for both
"not running" and any underlying GlobalDB failure (D-14 in neoswarm Phase 3 conventions).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add four pass-through accessors to GcsGlobalDb (hpp declarations + doxygen)</name>
  <files>GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp</files>
  <read_first>
    - GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp (the file being modified — full read)
    - GNUS-NEO-SWARM/src/common/error.hpp (Error enum; confirm GcsDbError exists)
    - .planning/workstreams/app/phases/01-foundation/01-PATTERNS.md § "GNUS-NEO-SWARM submodule — pass-through accessors" (exact signature shape and Doxygen block)
  </read_first>
  <action>
    Per PATTERNS.md "GNUS-NEO-SWARM submodule" section and RESEARCH Open Question 2.

    In the `public:` section of `GcsGlobalDb`, immediately AFTER the existing `IsRunning()` declaration,
    insert four new method declarations with full Doxygen headers (project rule: every public API):

      outcome::result<void> AddBroadcastTopic( const std::string &topicName );
      outcome::result<void> AddListenTopic( const std::string &topicName );
      outcome::result<void> Put( const std::string &key, const std::string &value );
      outcome::result<std::string> Get( const std::string &key );

    Each Doxygen block must state:
      - \brief line naming the wrapped GlobalDB call (e.g. "Add a broadcast topic — pass-through to sgns::crdt::GlobalDB::AddBroadcastTopic")
      - \param[in] entries for each parameter
      - \return stating: "outcome::success on success, Error::GcsDbError if not running or if the underlying call fails"

    Do NOT change existing declarations. Do NOT add new private members. Do NOT add new includes —
    std::string and outcome::result are already in scope per the existing file.

    Follow the neoswarm C++ style: Allman braces, space inside parens, `m_` prefix member naming,
    camelCase args, 120-char lines, C++17 ceiling.
  </action>
  <verify>
    <automated>grep -c "AddBroadcastTopic\|AddListenTopic\|::Put(\|::Get(" GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp | grep -q "^[4-9]\|^[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - File contains exactly four new declarations with names AddBroadcastTopic, AddListenTopic, Put, Get
    - Each declaration is preceded by a Doxygen comment block containing \brief, \param[in], \return
    - No existing declaration is modified or removed
    - Return types match exactly: outcome::result&lt;void&gt; for the first three, outcome::result&lt;std::string&gt; for Get
  </acceptance_criteria>
  <done>Four public pass-throughs declared on GcsGlobalDb with Doxygen; no other lines changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the four pass-throughs in gcs_global_db.cpp</name>
  <files>GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp</files>
  <read_first>
    - GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp (the file being modified — full read; note the existing m_running guard pattern used by Shutdown)
    - GNUS-NEO-SWARM/src/storage/gcs_global_db.hpp (declarations from Task 1)
    - ../SuperGenius/src/crdt/globaldb/globaldb.hpp (underlying signatures for HierarchicalKey / Buffer construction)
  </read_first>
  <action>
    Append four new member function definitions at the END of GNUS-NEO-SWARM/src/storage/gcs_global_db.cpp,
    each following the exact guard shape already used by `Shutdown()` (check m_running, bail with
    Error::GcsDbError if false), then forward to m_db.

    Implementation shape (per PATTERNS.md):

      outcome::result<void> GcsGlobalDb::AddBroadcastTopic( const std::string &topicName )
      {
          if ( !m_running.load() )
          {
              return outcome::failure( Error::GcsDbError );
          }
          return m_db->AddBroadcastTopic( topicName );
      }

    AddListenTopic: identical shape.

    Put: convert std::string to GlobalDB types at the call site:
      - HierarchicalKey from key string
      - Buffer from value string (Buffer{}.put(value) or the GlobalDB-idiomatic conversion — copy the exact pattern from any existing SuperGenius call site of GlobalDB::Put; if unsure, ASK the user before guessing)

    Get: call m_db->Get( HierarchicalKey{ key } ), on success convert the returned Buffer to std::string
    (Buffer::toString() or the equivalent idiom — verify against SuperGenius usage).

    Match the project's error-mapping rule: ANY underlying failure (not just !m_running) maps to
    Error::GcsDbError (D-14 in neoswarm). If the underlying call returns outcome::failure, propagate
    outcome::failure( Error::GcsDbError ).

    Allman braces, space inside parens, C++17 only. Do NOT add log statements unless the existing
    file already logs at this level (keep the diff minimal).
  </action>
  <verify>
    <automated>cd GNUS-NEO-SWARM && git diff --stat HEAD && grep -c "GcsGlobalDb::AddBroadcastTopic\|GcsGlobalDb::AddListenTopic\|GcsGlobalDb::Put\|GcsGlobalDb::Get" src/storage/gcs_global_db.cpp | grep -q "^4$"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly four new member-function definitions added
    - Every definition starts with the m_running.load() guard returning Error::GcsDbError
    - Every definition calls the matching m_db-> method
    - Allman braces; space inside parens; 120-char max line length
    - File still compiles (verified by the submodule's existing build, or at minimum by syntax inspection if submodule-only build is unavailable — note this in the plan summary)
  </acceptance_criteria>
  <done>Four implementations committed on the neoswarm feature branch; ready for GCS-side consumption.</done>
</task>

<task type="auto">
  <name>Task 3: Commit accessors on a neoswarm feature branch (no GCS-side pointer bump yet)</name>
  <files>GNUS-NEO-SWARM (submodule .git metadata only)</files>
  <read_first>
    - GNUS-NEO-SWARM/.git (current branch state)
    - .planning/workstreams/app/phases/01-foundation/01-CONTEXT.md (D-03: neoswarm changes stay isolated this phase)
  </read_first>
  <action>
    Branch stacking decision (locked at planning time, 2026-08-15): the GNUS-NEO-SWARM
    submodule is currently on `feature/app-restructure`. The passthrough branch
    `feature/gcs-globaldb-passthrough` is created FROM the current `feature/app-restructure`
    HEAD — i.e. stacked on it. Reason: `feature/app-restructure` holds the approved repo
    restructure and the neoswarm workstream is mid-Phase-3 on it; branching from `develop`
    would lose that context and the two workstreams share the submodule. The passthrough
    branch merges to `develop` alongside or after `feature/app-restructure` — it does NOT
    need to land first, and it must NOT be rebased onto `develop` while `app-restructure`
    is in flight.

    Inside GNUS-NEO-SWARM (concrete commands — no conditionals):

      1. Confirm we are on the stacking base:
           git -C GNUS-NEO-SWARM branch --show-current
         Expected output: `feature/app-restructure`. If the output is anything else, STOP
         and confirm with the user before proceeding — the stacking assumption is broken.
      2. Create and switch to the passthrough branch from current HEAD:
           git -C GNUS-NEO-SWARM checkout -b feature/gcs-globaldb-passthrough
      3. Stage the two modified files:
           git -C GNUS-NEO-SWARM add src/storage/gcs_global_db.hpp src/storage/gcs_global_db.cpp
      4. Commit with a conventional message:
           git -C GNUS-NEO-SWARM commit -m "feat(storage): add AddBroadcastTopic/AddListenTopic/Put/Get pass-throughs to GcsGlobalDb"
      5. Do NOT push yet — the user reviews and pushes per project rule ("Do not commit changes
         without explicit user permission" applies at push time; the local commit is the deliverable).

    Do NOT modify the GCS-root submodule pointer (no `git add GNUS-NEO-SWARM` in the parent repo).
    The pointer bump happens at end of phase after Plan 04's smoke test proves the new API works.
  </action>
  <verify>
    <automated>git -C GNUS-NEO-SWARM log -1 --oneline | grep -q "pass-throughs to GcsGlobalDb"</automated>
  </verify>
  <acceptance_criteria>
    - GNUS-NEO-SWARM is on branch feature/gcs-globaldb-passthrough
    - feature/gcs-globaldb-passthrough's merge-base with feature/app-restructure equals feature/app-restructure's HEAD (i.e. it is stacked on it, NOT on develop)
    - HEAD commit message contains "pass-throughs to GcsGlobalDb"
    - Commit touches exactly two files (gcs_global_db.hpp, gcs_global_db.cpp)
    - GCS-root `git status` shows GNUS-NEO-SWARM as modified-submodule (expected; pointer NOT staged)
  </acceptance_criteria>
  <done>Local commit on neoswarm feature branch; submodule pointer unchanged in GCS root.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `git -C GNUS-NEO-SWARM diff develop..HEAD --stat` shows exactly 2 files changed
- The four accessors compile cleanly as part of the GCS parent build (validated in Plan 04 when
  test_gcs_core_smoke calls them; this plan does not itself run a build — that requires CoreSession
  wiring from Plan 02)
</verification>

<success_criteria>
GcsGlobalDb's public API now includes the four pass-throughs, callable from gcs_core, with
correct not-running guards, and the change is isolated on a neoswarm feature branch pending
user review + push.
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
