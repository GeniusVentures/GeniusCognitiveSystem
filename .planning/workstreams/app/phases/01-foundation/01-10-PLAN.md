---
phase: 01-foundation
plan: 10
type: execute
wave: 5
depends_on: ["01-08", "01-09"]
files_modified:
  - src/app/templates/components/chat_message_flow_vars.json
  - src/app/templates/components/chat_message_flow.dart.jinja2
  - src/app/templates/components/chat_message_flow_cubit.dart.jinja2
  - src/app/templates/components/chat_message_flow_state.dart.jinja2
  - src/app/CMakeLists.txt
  - src/app/lib/generated/chat/chat_message_flow.dart
  - src/app/lib/generated/chat/chat_message_flow_cubit.dart
  - src/app/lib/generated/chat/chat_message_flow_state.dart
autonomous: true
requirements: [CORE-05]
must_haves:
  truths:
    - "The flow envelope generates a Dart 3 sealed class ChatFlowItem with one subclass per payload type (text_bubble / code_block / media) for exhaustive compile-time dispatch"
    - "ChatMessageFlow renders a List<ChatFlowItem> via an exhaustive switch over the sealed subclasses (no runtime enum/switch, no default fallthrough)"
    - "text_bubble -> ChatMessageBubble* role variant (01-08); code_block -> ChatMessageCodeBlock (01-09); media -> ChatMessageMedia (01-09)"
    - "Append-only: item_types are added-to, never removed/reordered/renamed (D-17)"
  artifacts:
    - path: "src/app/templates/components/chat_message_flow_vars.json"
      provides: "Flow item-type dispatch axis"
      contains: "item_types"
    - path: "src/app/templates/components/chat_message_flow.dart.jinja2"
      provides: "Flow list composite generating the ChatFlowItem sealed class + ChatMessageFlow list widget"
      contains: "ChatFlowItem"
    - path: "src/app/lib/generated/chat/chat_message_flow.dart"
      provides: "Generated sealed ChatFlowItem + 3 subclasses + ChatMessageFlow widget"
      contains: "sealed class ChatFlowItem"
  key_links:
    - from: "src/app/templates/components/chat_message_flow.dart.jinja2"
      to: "src/app/lib/generated/chat/chat_message_bubble.dart"
      via: "ChatFlowItemTextBubble references the bubble variant"
      pattern: "ChatMessageBubble"
    - from: "src/app/CMakeLists.txt"
      to: "src/app/templates/components/"
      via: "composite codegen loop extended with flow"
      pattern: "chat_message_flow"
---

<objective>
Author the interleave-capable `chat_message_flow` envelope: a Dart 3 sealed class `ChatFlowItem`
with one subclass per payload type, and a `ChatMessageFlow` list widget that dispatches each item
exhaustively at compile time.

Purpose: D-20 locks the iMessage model — text bubbles, code blocks, and media are all top-level flow
items in a heterogeneous, interleaved list. The sealed-class dispatch makes per-item rendering
exhaustive (no runtime type switch, no silent miss when a new item type lands). The leaf composites
are already generated (bubble in 01-08; code_block/media in 01-09); this plan is the envelope that
binds them.

Output: flow composite triple generated into `src/app/lib/generated/`, wired into the codegen loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/app/STATE.md
@.planning/workstreams/app/phases/01-foundation/01-CONTEXT.md
@.planning/workstreams/app/phases/01-foundation/01-RESEARCH.md
@.planning/workstreams/app/phases/01-foundation/01-UI-SPEC.md

@src/app/templates/components/chat_message_bubble_vars.json
@src/app/templates/components/chat_message_bubble.dart.jinja2
@src/app/CMakeLists.txt

<interfaces>
<!-- Established in 01-08/01-09: the composite codegen loop + engine.py invocation pattern. -->
The same loop is extended with one more `_comp` entry (flow). Flow composite vars must declare the
item-type dispatch axis, and the flow template emits:
```dart
sealed class ChatFlowItem { /* ... */ }
final class ChatFlowItemTextBubble extends ChatFlowItem { /* role + state + text */ }
final class ChatFlowItemCodeBlock extends ChatFlowItem { /* code payload */ }
final class ChatFlowItemMedia extends ChatFlowItem { /* media payload */ }
```
Dart 3 sealed classes make the render switch exhaustive at compile time. Append-only: item_types are
added-to, never removed/reordered/renamed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author chat_message_flow composite (sealed ChatFlowItem envelope, D-20/D-17)</name>
  <files>src/app/templates/components/chat_message_flow_vars.json, src/app/templates/components/chat_message_flow.dart.jinja2, src/app/templates/components/chat_message_flow_cubit.dart.jinja2, src/app/templates/components/chat_message_flow_state.dart.jinja2</files>
  <read_first>
    - .planning/workstreams/app/phases/01-foundation/01-RESEARCH.md § "Variant mapping" (sealed class dispatch)
    - .planning/workstreams/app/phases/01-foundation/01-CONTEXT.md (D-20 — interleave-capable flow)
    - src/app/templates/components/chat_message_bubble.dart.jinja2 (referenced variant)
  </read_first>
  <action>
    Create the chat_message_flow composite triple.

    chat_message_flow_vars.json:
      { "widget_class_name": "ChatMessageFlow", "file_stem": "chat_message_flow",
        "item_types": ["text_bubble", "code_block", "media"] }

    chat_message_flow.dart.jinja2:
      - Generate a Dart 3 `sealed class ChatFlowItem` with one `final class` subclass per item_type:
        ChatFlowItemTextBubble, ChatFlowItemCodeBlock, ChatFlowItemMedia.
      - Generate `ChatMessageFlow` list widget that takes `List<ChatFlowItem>` and renders each item
        via an exhaustive `switch` over the sealed subclasses (compile-time exhaustiveness — no
        default fallthrough). text_bubble -> the generated ChatMessageBubble* role variant;
        code_block -> ChatMessageCodeBlock; media -> ChatMessageMedia.
      - The flow is a `CustomScrollView`/scrollable anchored to bottom per UI-SPEC message-flow contract.

    _cubit/_state templates: ChatMessageFlowCubit holds the `List<ChatFlowItem>`; state is the list
    (no local message synthesis — D-04).

    Strict rules: sealed class (Dart 3), exhaustive switch, no runtime enum/switch over payload
    type, append-only item_types.
  </action>
  <verify>
    <automated>test -f src/app/templates/components/chat_message_flow.dart.jinja2 && grep -q "item_types" src/app/templates/components/chat_message_flow_vars.json && grep -q "ChatFlowItem" src/app/templates/components/chat_message_flow.dart.jinja2</automated>
  </verify>
  <acceptance_criteria>
    - flow vars declares item_types [text_bubble, code_block, media]
    - flow.dart.jinja2 generates sealed ChatFlowItem + 3 subclasses + ChatMessageFlow list widget
    - Exhaustive switch (no default) over sealed subclasses
    - cubit + state templates present
  </acceptance_criteria>
  <done>Flow envelope composite authored with exhaustive sealed dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the codegen loop with flow and render</name>
  <files>src/app/CMakeLists.txt, src/app/lib/generated/chat/chat_message_flow.dart, src/app/lib/generated/chat/chat_message_flow_cubit.dart, src/app/lib/generated/chat/chat_message_flow_state.dart</files>
  <read_first>
    - src/app/CMakeLists.txt (current state from 01-09 — bubble + code_block + media loop)
    - .planning/workstreams/app/phases/01-foundation/01-RESEARCH.md § "UI Strand — D-17 Engine Driver"
  </read_first>
  <action>
    Extend src/app/CMakeLists.txt: add the flow `_comp` entry to the composite loop (mirror RESEARCH
    D-17 `_chat_composites` list). Render its widget/cubit/state triple into ${GENERATED_DIR}/chat/.

    Update `app_generate_chat_components` aggregate to DEPEND on all four composites' stamps
    (bubble + code_block + media + flow).

    Render manually (dev-loop) using engine.py (same invocation shape as 01-09 Task 2, substituting
    the flow component name). Inspect generated output:
      - chat_message_flow.dart contains sealed ChatFlowItem + ChatFlowItemTextBubble/CodeBlock/Media
    Fix StrictUndefined drift errors by editing templates (never hand-edit generated files).
  </action>
  <verify>
    <automated>test -f src/app/lib/generated/chat/chat_message_flow.dart && grep -q "ChatFlowItem" src/app/lib/generated/chat/chat_message_flow.dart && grep -q "sealed class ChatFlowItem" src/app/lib/generated/chat/chat_message_flow.dart && grep -q "chat_message_flow" src/app/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    - Codegen loop now covers all four composites (bubble + code_block + media + flow)
    - app_generate_chat_components depends on all four triples
    - Generated flow.dart has sealed ChatFlowItem + 3 subclasses
    - No hand-edited generated files
  </acceptance_criteria>
  <done>Flow envelope generated; all four composites dispatch the full D-20 item taxonomy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template author → generated code | Jinja2 templates + vars rendered into committed Dart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10-01 | Tampering | sealed dispatch completeness | mitigate | Dart 3 sealed class + exhaustive switch — a new item_type without a subclass is a compile error, not a silent miss. |
| T-01-10-02 | Denial of Service | unbounded ChatFlowItem list | mitigate | ChatMessageFlowCubit caps list length (constexpr kMaxFlowItems) dropping oldest. |
| T-01-10-SC | Tampering | package installs | accept | No npm/pip/cargo installs. |
</threat_model>

<verification>
- Generated flow.dart compiles (sealed exhaustive switch) via `dart analyze` once 01-11 wires the shell
- Flow composite regenerates drift-free (git diff clean on generated output)
</verification>

<success_criteria>
The chat_message_flow envelope is generated data-driven with a sealed ChatFlowItem hierarchy and an
exhaustive switch binding the bubble/code_block/media composites — completing the D-18/D-19/D-20
taxonomy as an interleave-capable flow.
</success_criteria>

<output>
Create `.planning/workstreams/app/phases/01-foundation/01-10-SUMMARY.md` when done.
</output>
