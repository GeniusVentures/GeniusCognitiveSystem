name: Renumber context governance chapters

on:
  push:
    branches:
      - docs/context-efficiency-contract

permissions:
  contents: write

jobs:
  renumber:
    if: github.event.head_commit.message != 'docs: place context governance as chapter 17'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/context-efficiency-contract
          fetch-depth: 0

      - name: Renumber architecture chapters
        shell